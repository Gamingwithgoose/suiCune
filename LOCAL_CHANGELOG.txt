LOCAL CHANGELOG
===============

2026-08-28 - Phase 0 baseline and architecture audit

Task worked on
--------------
Established a buildable, launchable baseline and traced the current startup,
main-loop, rendering, audio, input, save, RTC, memory, ROM/data lookup,
scripting, map/object, and networking/link paths. No runtime architecture was
converted during this audit.

Pre-existing working-tree state
-------------------------------
- `lib/libmobile` was already dirty when this session began. It remains
  a pre-existing local modification and was not modified by this session.
- Initial branch/commit: `main` at `2d9149c5` ("Fixed long-standing bugs
  related to TMs/HMs...").
- `git submodule status` could not run in the available devkitPro shell because
  its git-submodule helper could not source `git-sh-setup`. Direct `git diff`
  inspection still established that the recorded submodule commit is unchanged
  and only the submodule worktree is dirty.

Files modified
--------------
- None.

Files created
-------------
- `LOCAL_CHANGELOG.txt`: Phase 0 audit and reproducible baseline record.
- `suiCune.exe`: ignored release-build output produced by the successful CMake
  build.
- `lib/liblibmobile.a`: ignored release-build output produced by the successful
  CMake build.

Files deleted
-------------
- None from the project baseline.
- `bindings.json` and `mobile.bin` were generated by the launch smoke test and
  immediately removed after the test because they did not exist beforehand.

Exact purpose of each change
----------------------------
- `LOCAL_CHANGELOG.txt` is the sole project handoff/report and records verified
  architecture, build, and test findings.
- The executable and static archive are normal ignored build products and
  provide the known build artifact for this baseline.

Architecture inventory
----------------------

Startup and main loop
~~~~~~~~~~~~~~~~~~~~~
- The process entry point is `main` in `tools/emu/peanut_sdl.c`.
- Startup initializes PhysFS, SDL video/controller/audio, Game Boy state,
  cartridge RAM, RTC, constant/map/script lookup tables, JSON-backed tables,
  configurable input, the emulated APU, the emulated LCD callback, SDL renderer
  and texture, native sockets, and the emulated Mobile Adapter.
- `USE_ROM_FILE` is currently hardcoded to 0. The normal build does not require
  an external Game Boy ROM file, but it still initializes a `struct gb_s`
  cartridge/CPU/memory model with hardcoded Crystal cartridge metadata.
- Reset paths use `setjmp`/`longjmp`, set emulated CPU PC/SP values, and call
  translated initialization functions.
- The outer application loop is native C: while SDL has not requested quit, it
  calls `Intro_Jumptable()`. `gb_run_frame()` is present but commented out in
  this path. Frame progress ultimately reaches `gb_finish_frame()` and
  `sdl_loop()` through translated game functions such as `DelayFrame()`.
- The loop is therefore a hybrid: SDL owns the host loop and pacing, while
  translated game flow remains coupled to Game Boy CPU registers, addresses,
  hardware registers, and frame/LCD semantics.

Function dispatch and ROM-address identity
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
- `functions.c` initializes `redirectFunc[ROM_SIZE]` and
  `convertedFunc[ROM_SIZE]`, indexed by addresses in a 2 MiB ROM-shaped space.
  The audit counted 525 active `CONVERTED(...)`/`REDIRECTED(...)` table entries.
- `funcmap.h`, `global.h`, and the dispatch tables preserve fixed Game Boy ROM
  addresses as runtime identities. Numerous converted macros and functions
  still read/write `gb.cpu_reg`, invoke `gb_read`/`gb_write`, and emulate stack
  and flag behavior.
- The CPU interpreter and opcode machinery remain compiled and callable even
  though the normal outer loop does not call `gb_run_frame()` directly.
- No external ROM is authoritative in the default build, but ROM banking,
  banked addresses, CPU registers, and address-indexed redirection remain
  authoritative runtime concepts. This is not a completed native ROM/data
  conversion.

Memory model
~~~~~~~~~~~~
- `struct gb_s gb` owns emulated WRAM, VRAM, HRAM/OAM/register state, cartridge
  RAM, RTC registers, CPU registers, MBC state, and LCD state.
- `wram` and `vram` are struct views mapped over arrays inside `gb`; HRAM is
  represented by a generated global structure. `global.h` is a generated map of
  original RAM/ROM addresses.
- `gb_read` and `gb_write` remain the central address decoder for ROM banking,
  VRAM banks, cartridge RAM/RTC, WRAM/echo RAM, OAM, HRAM, joypad, timers,
  interrupts, LCD registers, DMA, CGB palettes, audio registers, and WRAM-bank
  selection.
- Audit counts demonstrate the breadth of the dependency: `wram->` occurs
  17,959 times across 315 C/header files; `hram.` occurs 2,902 times across 214
  files; `gb_read(` occurs 634 times across 83 files; `gb_write(` occurs 708
  times across 79 files; `gb.cpu_reg` occurs 934 times across 6 files.
- Some logical state has already moved into C structures, notably global
  `PlayerData gPlayer` and `PokemonData gPokemon`, but their layouts and many
  fields still mirror WRAM/SRAM organization and fixed hardware-era limits.

Rendering
~~~~~~~~~
- Gameplay code loads converted PNG/1bpp/2bpp assets into emulated VRAM and
  constructs tilemaps, palettes, and OAM-compatible sprite data.
- The LCD renderer in `tools/emu/peanut_sdl.c` rasterizes every 160-pixel
  scanline from emulated VRAM, BG/window tile maps, CGB palette registers, LCDC
  state, and OAM using Game Boy priority and addressing rules.
- `lcd_draw_line` converts indexed LCD output into a 160x144 RGB555 framebuffer.
  SDL uploads that framebuffer to one streaming texture and presents it.
- Native SDL is therefore the presentation backend, not an authoritative native
  world/UI/entity renderer. OAM limits, tile addressing, VRAM banks, scanlines,
  and Game Boy LCD registers remain authoritative.

Audio
~~~~~
- Music/SFX data is loaded from native asset files into buffers, and much of the
  music parser is translated C operating on channel structs.
- The translated engine still writes Game Boy NR10-NR52 and wave RAM addresses
  through `gb_write` and reads them through `gb_read`.
- Those addresses are routed to `tools/emu/minigb_apu`, whose stated and actual
  purpose is emulation of the four-channel Game Boy APU. Its SDL callback
  produces host PCM audio.
- SDL audio output is native, but Game Boy APU registers and an emulated APU
  remain the authoritative sound generator. Native audio conversion is not
  complete.

Input
~~~~~
- `util/input.c` is a useful native front end: it maps SDL keyboard/controller
  events to semantic A/B/Start/Select/directional actions and supports a JSON
  binding file.
- The result is packed into the Game Boy joypad bitfield. `home/joypad.c` then
  emulates JOYP register selection/inversion and copies results into HRAM/WRAM
  joypad fields, including legacy auto-input behavior.
- Device acquisition/remapping is native; gameplay input consumption is still
  coupled to Game Boy joypad-register and HRAM semantics.

Save system and serialization
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
- The host save path is `read_cart_ram_file`/`write_cart_ram_file` in
  `tools/emu/peanut_sdl.c`.
- Save size is hardcoded to Crystal's 0x8000-byte cartridge RAM size. Two raw
  images (`baserom.sav` and `baserom.sav2`) are allocated and written.
- `OpenSRAM`/`CloseSRAM` still operate by writing MBC3 enable, latch, and bank
  registers. Many gameplay systems directly access banked SRAM addresses.
- `util/serialize.c` explicitly serializes/deserializes selected runtime C
  structures to the original SRAM byte layout. This is better than raw native
  struct serialization and supports unaligned-host safety, but Crystal SRAM
  layout remains the persistent authoritative format.
- The serializer covers player, Pokemon, current-map, options, boxes, objects,
  and related records. Its tests verify byte-for-byte agreement with SRAM, not a
  versioned native save format.
- No native save version, schema migration layer, or format-independent native
  persistence was found.

RTC
~~~
- `util/rtc.c` uses libc `time()`/`localtime()` and records a host timestamp, so
  host clock acquisition is already partly native.
- Host time is converted back into `gb.cart_rtc` MBC3 register fields, and the
  adventure start timestamp is stored in spare bytes inside Crystal-compatible
  player SRAM data.
- `home/time.c` and `engine/rtc/rtc.c` continue to latch/select/read/write MBC3
  RTC registers through `gb_read`/`gb_write` and use HRAM clock fields.
- The RTC is therefore a native time source feeding a legacy emulated RTC and
  save layout, not a completed native RTC subsystem.

Content and asset lookup
~~~~~~~~~~~~~~~~~~~~~~~~
- PhysFS/native file loading is used for graphics, audio binaries, JSON data,
  and optional asset archives. `data/json_load.c` loads several content tables,
  including base stats and wild encounters, into C allocations.
- Many other content tables and assets are compiled as C arrays and direct C
  pointers. This is meaningful progress away from a literal ROM file.
- At the same time, fixed ROM addresses, bank/address macros, GB pointer
  conversion (`GBToRAMAddr`, `gb_pointer`, `MBANK`), generated address maps, and
  ROM-address dispatch tables remain widely used.
- Core expandable IDs remain byte-sized in `constants/types.h`:
  `species_t`, `dex_t`, `item_t`, and `move_t` are all `uint8_t`.
- The runtime is consequently hybrid: native asset files/C tables coexist with
  Game Boy address identity and byte-width content ceilings.

Scripting
~~~~~~~~~
- A native C script state object (`struct Script`) holds a C function pointer,
  position, arguments, and a fixed call stack. Map/event scripts are largely
  expressed as C state-machine functions using scripting macros.
- `ScriptEvents()` dispatches the current C function; the old byte-command
  `RunScriptCommand()` switch is present primarily as commented translated
  reference rather than the active general path.
- Save compatibility still preserves original GB script pointers for map
  objects. `data/script_pointers.c` builds a hash mapping map/index keys and GB
  pointer values to native C script pointers because original Crystal stores
  event script pointers in save data.
- Scripting has substantial native-C structure, but script identity and saved
  map-object data are still restricted by GB pointer/save compatibility.

Maps and overworld objects
~~~~~~~~~~~~~~~~~~~~~~~~~~
- Map headers, attributes, events, connections, blocks, and scripts are compiled
  as C structures and direct pointers. `MapGroupPointers` selects map groups,
  and map setup uses C function-pointer command tables.
- The overworld loop is a C state machine (`OverworldLoop`, `EnterMap`,
  `HandleMap`) that runs map setup, object updates, background work, and script
  events.
- Map group/number fields remain byte-sized and some serialized fields retain GB
  addresses. Connection calculations still target WRAM-backed overworld block
  buffers.
- Entity authority is still the fixed Crystal arrays: `NUM_OBJECT_STRUCTS` is 13
  and `NUM_OBJECTS` is 16. `FindFirstEmptyObjectStruct`, object visibility,
  movement, rendering, collision, and serialization all operate on those fixed
  slots in `gPlayer`.
- This is not yet a dynamic native entity system and cannot satisfy future MMO
  entity requirements without replacement.

Networking, link, and mobile
~~~~~~~~~~~~~~~~~~~~~~~~~~~~
- `util/network.c` contains native socket/SDL_net transport and initializes
  UDP/TCP endpoints. This is genuine host networking infrastructure.
- The transport is installed into `gb.gb_serial_tx`/`gb.gb_serial_rx`, and the
  gameplay link code retains Crystal serial/link synchronization, byte exchange,
  time-capsule, trade, and link-room semantics.
- The mobile path uses `libmobile` to emulate a Game Boy Mobile Adapter and its
  serial behavior, while using host sockets underneath.
- Native sockets exist, but the authoritative game protocol is still Game Boy
  serial/Mobile Adapter architecture. This is not an MMO protocol or a native
  remote-player/entity system.

Architectural changes made
--------------------------
- None. Phase 0 was intentionally read-only apart from this changelog and normal
  ignored build outputs.

Obsolete Game Boy/emulation dependency removed
-----------------------------------------------
- None.

Temporary migration code introduced
------------------------------------
- None. No removal condition is required.

Compatibility implications
--------------------------
- No source/runtime behavior was changed.
- The successful baseline used the default CMake feature set: networking,
  Mobile Adapter support, SDL2_net, and PhysFS enabled; Release configuration.
- Existing Crystal save and translated-runtime compatibility remain unchanged.

Save-format implications
------------------------
- None from this session. The audit confirms the current authoritative format is
  still two raw 0x8000-byte Crystal-compatible cartridge-RAM images, with
  explicit serializers targeting the legacy byte layout.

Build commands executed
-----------------------
- `make -n RELEASE=1`
  Result: dry run only; the devkitPro MSYS environment lacked `sdl2-config`, so
  this environment is not a usable baseline without path/package correction.
- `cmake -S /c/Users/maver/OneDrive/Documents/GitHub/suiCune -B
  /c/Users/maver/Documents/suiCune-phase0-build -G
  "Unix Makefiles" -DFEATURE_NETWORKING=OFF -DFEATURE_MOBILE=OFF
  -DUSE_PHYSFS=OFF -DUSE_SDLNET=OFF -DCMAKE_BUILD_TYPE=Release`
  Result: configuration failed because that devkitPro environment could not find
  SDL2 through pkg-config.
- With `C:\msys64\ucrt64\bin` prepended to PATH:
  `cmake.exe -S C:\Users\maver\OneDrive\Documents\GitHub\suiCune -B
  C:\Users\maver\Documents\suiCune-phase0-ucrt-build3
  -G Ninja -DCMAKE_BUILD_TYPE=Release`
  Result: successful default-feature configuration. CMake emitted two developer
  warnings for mismatched arguments on `if`/`endif` blocks at lines 36-44.
- With the same PATH:
  `cmake.exe --build
  C:\Users\maver\Documents\suiCune-phase0-ucrt-build3
  --parallel 8`
  Result: success, 984/984 steps; produced `suiCune.exe` (10,757,380 bytes) and
  `lib/liblibmobile.a`.

Test commands executed
----------------------
- `ctest.exe --test-dir
  C:\Users\maver\Documents\suiCune-phase0-ucrt-build3
  --output-on-failure`
  Result: no CTest tests are defined.
- Hidden launch smoke test of `suiCune.exe` from the repository working
  directory for 10 seconds, then termination of that exact process.
  Result: process remained alive. Logs verified RTC/save initialization, default
  input mapping, DirectSound audio initialization, SDL_net initialization,
  Mobile Adapter configuration, and intro timer activity. No stderr crash or
  assertion occurred during the interval.
- The only in-tree automated tests found are serialization round-trip checks in
  `util/serialize.c`. They run automatically only in a Debug executable during
  startup. This Release baseline did not execute them.

Build/test results
------------------
- Default-feature Windows Release build: PASS.
- Ten-second startup/intro smoke test: PASS.
- Automated test suite: NOT AVAILABLE (zero CTest tests).
- Interactive menu/visual/gameplay verification: NOT PERFORMED in this Phase 0
  command-line session.

Regressions discovered or outstanding
-------------------------------------
- No new regression was introduced by this audit.
- Project-context correction recorded on 2026-08-28: this repository is an
  independent suiCune project. Defects, behavior, assets, and TODOs from Project
  Celebi, Polished Crystal, Gen2Recomp, or earlier projects do not apply unless
  they are explicitly brought into scope. The current suiCune intro is correct.

Unresolved blockers and risks
-----------------------------
- There is no automated behavioral/regression harness beyond startup-only Debug
  serialization checks. Rendering, menus, audio, input, saves, and gameplay need
  reproducible tests before risky subsystem replacement.
- The dirty `lib/libmobile` submodule must remain untouched until its pre-existing
  changes are understood or explicitly placed in scope.
- CMake writes runtime and library products into the source tree even for an
  out-of-source build. They are ignored, but this weakens build isolation.
- The normal release baseline compiles the CPU/APU/LCD/MBC compatibility
  architecture even where translated C bypasses part of it, making dependency
  removal require call-site audits rather than simple file deletion.
- The Debug serialization checks were not executed in this Release baseline.

Next recommended native-conversion task
---------------------------------------
1. Begin Phase 1 with a narrowly scoped semantic-ID inventory and migration
   plan for `species_t`, `dex_t`, `move_t`, and `item_t`. Trace every storage,
   table, function, script parameter, network representation, and serializer
   boundary before widening any field.
2. Treat the existing Crystal SRAM serializer as an explicit legacy boundary;
   do not widen legacy bytes or allow save compatibility to dictate new native
   runtime widths. A later native save schema must be versioned and explicit.
3. In parallel with future subsystem work, establish headless/unit-level tests
   for serializers, map/script lookup, and semantic IDs so architectural changes
   can be verified without relying only on manual playthroughs.


2026-08-28 - Phase 1 16-bit content ID foundation
================================================================

Task worked on
--------------
Established semantic native content-ID types, separated confirmed Crystal byte
records from native data, widened selected native content databases and player
bag state, and made native-to-legacy serialization and gameplay handoffs
explicit. The current intro behavior was not changed; its only edit initializes
the newly native bag terminator correctly.

Pre-existing working-tree state
-------------------------------
- `lib/libmobile` was dirty before this session and remains a pre-existing local change. It was
  not modified.
- `LOCAL_CHANGELOG.txt` already existed from Phase 0 and was maintained as the
  sole project handoff document.

Files modified
--------------
- `constants/types.h`: added semantic IDs, width assertions, native and legacy
  sentinels, explicit legacy record fields, native content-table fields, native
  bag entries, and a byte-exact Mystery Gift trainer import structure.
- `wram.h`: marked mobile offer and base-data fields as explicit legacy IDs so
  the exact WRAM overlay does not change size.
- `util/serialize.c`, `util/serialize.h`: separated semantic and legacy field
  descriptors; added checked native-ID narrowing; mapped the native item-list
  terminator to Crystal's `0xff`; updated native bag offsets and registered-item
  serialization; added an explicit legacy species deserializer.
- `engine/items/items.c`, `engine/items/items.h`: migrated bag/key-item APIs and
  traversal to `ItemId`, structured item/quantity entries, and a 16-bit native
  terminator while retaining 8-bit quantities.
- `engine/overworld/select_menu.c`: updated direct bag access for structured
  entries and rejected unrepresentable registered items at the legacy WRAM
  handoff.
- `engine/events/kurt.c`, `engine/events/kurt.h`: migrated bag-facing Apricorn
  lookup to `ItemId` and the native bag terminator.
- `engine/menus/intro_menu.c`: initialized the native item-pocket terminator;
  no intro presentation, sequence, asset, or behavior was modified.
- `data/items/apricorn_balls.c`, `data/items/buena_prizes.c`,
  `data/items/heal_hp.c`, `data/items/heal_status.c`,
  `data/battle_anims/ball_colors.c`: changed widened native item-table
  terminators to `ITEM_LIST_END`.
- `engine/items/item_effects.c`, `engine/items/item_effects.h`,
  `engine/battle_anims/functions.c`: migrated corresponding native item-table
  lookups to `ItemId` and bounded sentinel-aware traversal.
- `engine/smallflag.c`, `engine/smallflag.h`, `home/pokedex_flags.c`,
  `home/pokedex_flags.h`: widened flag-array indices and the Pokédex API to
  `DexId`, removing the prior narrowing cast.
- `data/json_load.c`: loads native wild species as checked `SpeciesId` values
  and rejects Dex values that cannot enter the still-legacy base-data record.
- `engine/overworld/wildmons.c`, `engine/overworld/wildmons.h`,
  `engine/events/overworld.c`: carry encounter/roamer/fishing species as
  `SpeciesId` and range-check before legacy battle WRAM assignment.
- `engine/battle/read_trainer_party.c`: consumes native 16-bit trainer species,
  moves, and items; imports Mystery Gift's byte record explicitly; fails fast
  before an expanded ID can enter legacy party data.
- `engine/battle/read_trainer_attributes.c`: range-checks native trainer items
  before legacy battle-WRAM assignment.
- `engine/pokemon/evolve.c`: consumes native evolution species and learnset move
  IDs, traverses learnsets with a native-size index, and checks conversion into
  still-legacy Pokemon records.
- `engine/events/npc_trade.c`, `engine/events/npc_trade.h`: consumes native NPC
  trade species/items and checks conversion into legacy trade/Pokemon records.
- `engine/overworld/events.c`: rejects native hidden-item IDs that cannot enter
  the legacy script/WRAM handoff instead of truncating them.
- `engine/pokemon/mail.c`: checks native mail item IDs before writing the legacy
  held-item field.
- `home/names.c`, `home/names.h`: migrated species, move, and item name lookup
  APIs to semantic IDs without requiring byte-sized parameters.
- `engine/battle/core.c`: corrected the roamer species pointer helper for native
  `SpeciesId` storage.
- `engine/phone/phone.c`, `engine/phone/phone.h`: renamed the byte-oriented phone
  trainer pair to `LegacyTrainerIdentity`, freeing `TrainerId` for the semantic
  native type.
- `mobile/mobile_46.c`: uses the explicit legacy species deserializer for mobile
  trade byte fields.

Files created
-------------
- None.

Files deleted
-------------
- None.

Architectural changes made
--------------------------
- Added native semantic foundations: 16-bit `SpeciesId`, `DexId`, `MoveId`,
  `ItemId`, `FormId`, `AbilityId`, `SpriteId`, `TrainerClassId`, and `TrainerId`;
  8-bit `NatureId` and `TypeId`; 32-bit `PersonalityValue` and `NetPlayerId`.
- Kept `species_t`, `dex_t`, `move_t`, and `item_t` as explicitly documented
  byte-oriented compatibility aliases. Globally widening them was rejected
  because legacy arrays would then be traversed with incorrect 16-bit strides.
- Native wild/fishing/trainer/evolution/learnset/NPC-trade/item tables now carry
  semantic IDs. Native player bag entries separate `ItemId` from 8-bit quantity
  instead of storing both in an alternating item-typed array; aligned anonymous
  pocket blocks preserve the existing generic pocket-access API safely.
- Pokédex bit indexing now accepts `DexId`; the backing array still scales from
  `NUM_POKEMON` and remains part of the current Crystal-compatible save model.
- Crystal SRAM/mobile/Mystery Gift records have explicit legacy byte types.
  Native serializer fields narrow only after a range check; unsupported expanded
  values fail instead of being silently corrupted.

Obsolete Game Boy/emulation dependency removed
-----------------------------------------------
- Removed the byte-width content-ID constraint from confirmed native content
  tables, content-name lookup APIs, player bag entries, and Pokédex flag indices.
- Removed the alternating `[item, quantity, item, quantity]` typed-array model
  from native player bag storage.
- Game Boy byte limits remain authoritative in live `BoxMon`, `PartyMon`,
  `BattleMon`, WRAM battle fields, Crystal save/link/mobile records, and several
  script interfaces. Those dependencies were made explicit, not claimed removed.

Temporary migration code introduced
------------------------------------
- The old lowercase aliases remain byte-sized compatibility types. Removal
  condition: delete each alias only after all callers in its live party/battle,
  WRAM, script, link, and save-format domain have migrated to either a semantic
  native ID or an explicit `Legacy*Id`.
- Checked native-to-legacy conversions remain at trainer, encounter, evolution,
  NPC-trade, registered-item, and serializer boundaries. Removal condition:
  remove a conversion only when its destination is no longer a byte-oriented
  Crystal record, or move it to a dedicated legacy import/export boundary.

Compatibility implications
--------------------------
- Existing Crystal IDs 0-255 remain representable. Native content tables and
  bag state can hold wider IDs, but unconverted live Pokemon/battle/WRAM paths
  deliberately reject IDs above 255 rather than truncate them.
- Native item ID 255 cannot round-trip through the Crystal save format because
  Crystal uses `0xff` as its list terminator. The native terminator is `0xffff`,
  and serialization maps it explicitly to/from the legacy terminator.
- No intro visuals, assets, flow, title behavior, options behavior, music
  behavior, RTC behavior, renderer, audio subsystem, or network protocol was
  changed.

Save-format implications
------------------------
- The on-disk format remains Crystal-compatible and byte-sized; no save version
  or native save schema was introduced.
- Native IDs are range-checked during legacy serialization. An expanded value
  that cannot be represented aborts serialization instead of corrupting a save.
- Loading widens representable legacy bytes into native IDs. Legacy item-list
  byte `0xff` maps to native `ITEM_LIST_END` (`0xffff`).

Build commands executed
-----------------------
- None.

Test commands executed
----------------------
- None. Source searches, diff inspection, and `git diff --check` are the only
  validation performed.

Build/test results
------------------
Build/runtime validation: NOT PERFORMED — source-only migration pass.

Regressions discovered
----------------------
- None confirmed through source inspection. Runtime behavior was not tested.

Unresolved blockers and risks
-----------------------------
- Live `BoxMon`/`PartyMon`/`BattleMon` data and many WRAM fields are still exact
  Crystal byte records. Species, move, and held-item expansion cannot be called
  complete until native Pokemon/battle structures replace those records and all
  consumers migrate.
- Base-stat `dexNo` and held-item bytes remain constrained by the packed Crystal
  base-data layout copied into WRAM. A separate native base-stat record/import
  boundary is required before expanded Dex/item IDs are end-to-end there.
- Script arguments, link/mobile formats, marts and several legacy sentinel
  arrays still use byte aliases. The final audit found 442 `species_t`, 476
  `move_t`, 226 `item_t`, and one `dex_t` occurrence; these are an explicit
  migration inventory, not a completion claim.
- Remaining `uint8_t species/move/item` declarations audited in this session are
  legacy WRAM values, menu option indices, or boolean results rather than native
  content storage. Broad `0xff`, `255`, and `(uint8_t)` results include hardware,
  masks, flags, counters, and legacy formats and require subsystem-level review,
  not blind replacement.
- No compiler or runtime validation was performed during this source-only session. A later
  should review and build this working tree before additional dependent changes.

Next recommended native-conversion task
---------------------------------------
1. Separate `BoxMon`/`PartyMon`/`BattleMon` into explicit Crystal import/save/link
   records and native runtime Pokemon structures, then migrate one complete path
   (party creation, move access, held item, battle staging, and save conversion).
2. Introduce a native base-stat/content record with `DexId`, `ItemId`, and
   `TypeId`, leaving `BaseData` only at the Crystal import/WRAM boundary.
3. After the live Pokemon split, migrate move/item script operands explicitly
   instead of widening bytecode or retaining lowercase aliases indefinitely.


2026-08-28 - Phase 1 native content records and API propagation
===========================================================================================

Task worked on
--------------
Continued the Phase 1 semantic-ID migration from the exact existing working
tree. This was stopped at the handoff point after completing
the currently active BaseData, growth/stat, TM/HM, mart, script-API, and battle
query edits. The original large Phase A-I request is only partially complete.

Starting repository state
-------------------------
- The working tree already contained the in-progress Phase 1 edits represented
  by the source files listed below. They were preserved and completed as one
  coherent continuation rather than discarded or replaced.
- `lib/libmobile` was already dirty and remains an untouched pre-existing local change.
- No branch, commit, tag, push, reset, restore, clean, or history operation was
  performed.

Files modified
--------------
- `LOCAL_CHANGELOG.txt`: appended this complete session handoff; it remains the
  only report file.
- `constants/types.h`: centralized checked semantic-to-legacy ID conversions;
  made `BaseData` a native, unpacked content record with `DexId`, `TypeId`, and
  `ItemId`; added byte-exact `LegacyBaseData`; removed the unused duplicate
  `BaseMon` declaration.
- `data/json_load.c`: loads native mart IDs and base-data Dex/item IDs with
  16-bit validation, added the omitted base-data held-item loader, and validates
  byte-sized TypeId input.
- `data/items/marts.c`, `data/items/marts.h`: changed the native default-mart
  table to `ItemId`.
- `data/moves/tmhm_moves.c`, `data/moves/tmhm_moves.h`: changed the native
  TM/HM move table to `MoveId` and exposed an explicit native-size count.
- `home/pokemon.c`, `home/pokemon.h`: added native `GetSpeciesBaseData`, added
  checked native-to-legacy BaseData export, retained `GetBaseData` only as the
  explicit WRAM-cache boundary, and widened species-facing cry/Pokemon APIs.
- `home/names.c`: made expanded/unsupported item, TM/HM, and move name requests
  bounded instead of indexing current Crystal tables out of range.
- `engine/pokemon/experience.c`, `engine/pokemon/experience.h`: added growth-rate
  and species-based native experience calculations; retained the WRAM-based
  entry point for legacy callers.
- `engine/pokemon/move_mon.c`, `engine/pokemon/move_mon.h`: widened party creation
  entry points to `SpeciesId`; validates legacy record representability before
  mutating party state; uses native BaseData for experience, egg steps, and stat
  calculations; carries base stats and level explicitly; retained the legacy
  WRAM stat wrapper for link-era callers; corrected null stat-exp pointer
  handling in the no-stat-exp path.
- `engine/pokemon/types.c`, `engine/pokemon/types.h`: use native species BaseData
  for type display and semantic `MoveId`/`TypeId` parameters with current-table
  bounds.
- `engine/pokemon/breeding.c`: reads egg groups from native BaseData and traverses
  the TM/HM table using `size_t` plus an explicit count.
- `engine/pokemon/evolve.c`: uses the centralized checked species/move legacy
  conversions at the still-packed Pokemon-record boundary.
- `engine/pokemon/print_move_description.c`,
  `engine/pokemon/print_move_description.h`: accept `MoveId` and bound lookup to
  the currently available move-description table.
- `engine/pokemon/caught_data.c`: removed an unused legacy BaseData WRAM-cache
  load from the bug-contest transfer path.
- `engine/events/daycare.c`: uses native BaseData for egg growth rate and steps.
- `engine/events/npc_trade.c`: uses centralized checked species/item conversion
  at the still-legacy trade/Pokemon-record boundary.
- `engine/events/bug_contest/caught_mon.c`: removed the obsolete BaseData WRAM
  cache load now that party generation queries native BaseData.
- `engine/battle/core.c`: migrated selected battle initialization, type/stat,
  held-item, catch-rate, base-exp, and effectiveness queries to native BaseData;
  held items still entering `BattleMon` are checked at that legacy boundary.
- `engine/battle/move_effects/beat_up.c`: reads attacker/defender base stats
  directly from native BaseData rather than the shared WRAM cache.
- `engine/battle/read_trainer_party.c`: replaced repeated casts/checks with the
  centralized checked conversions at legacy party-record construction.
- `engine/link/time_capsule.c`: validates type data from native BaseData while
  preserving the byte-oriented Time Capsule record.
- `engine/pokedex/pokedex.c`: performs type filtering from native BaseData.
- `engine/items/tmhm2.c`, `engine/items/tmhm2.h`: migrated species/move APIs to
  semantic IDs, use native BaseData compatibility bits, use a native-size index,
  and bound TM/HM lookup by the explicit table count.
- `engine/items/tmhm.c`: explicitly converts a native TM/HM move back to the
  still-byte-oriented legacy UI API and uses `MoveId` for name-only state.
- `engine/items/mart.c`, `engine/items/mart.h`: migrated native mart sources to
  `ItemId`; separated normal and priced-mart pointers instead of type-punning;
  validates capacity and representability at the legacy scrolling-menu record;
  preserves room for its byte terminator; indexes selected bargain/rooftop sale
  prices through the correctly typed priced-mart source.
- `engine/overworld/scripting.c`, `engine/overworld/scripting.h`: widened safe
  native C script-facing item/species APIs (names, give/take/check item,
  give/check Pokemon, and egg/gift operations) while keeping actual byte WRAM
  handoffs checked and localized.
- `engine/debug/color_picker.c`: uses `MoveId` for a TM/HM name-only local.

Files created
-------------
- None.

Files deleted
-------------
- None.

Completed work
--------------
- `BaseData` is now a native content record. Its Dex number and held items no
  longer inherit the packed Crystal byte widths, and normal migrated consumers
  no longer copy the WRAM base-data cache before querying species properties.
- `LegacyBaseData` is the explicit exact-layout representation, with a
  `BASE_DATA_SIZE` assertion and a centralized checked export routine.
- Native experience/stat calculation paths now receive actual growth/base data
  and level instead of depending on mutable WRAM cache state.
- Native mart sources and TM/HM move tables carry semantic IDs and native-size
  counts. Their genuinely byte-oriented menu/UI destinations are checked.
- The listed native C scripting interfaces now accept semantic IDs without any
  global script-bytecode widening.
- Several battle, breeding, daycare, Pokédex, Time Capsule validation, and
  bug-contest paths now query native BaseData directly.

Partially completed work
------------------------
- Phase C/D semantic propagation is partial. Many normal APIs still use the
  lowercase compatibility aliases because their owners are the packed live
  Pokemon/battle/WRAM model.
- Phase E is partial: safe C function signatures were widened, but script
  bytecode operands and byte WRAM variables remain legacy by design.
- Phase F is partial: native trainer content already carries semantic Pokemon
  IDs and this pass centralized conversion at party construction, but trainer
  identity and all downstream party/battle storage were not fully migrated.
- Phase G is partial: touched TM/HM and mart traversal uses native-size counts;
  other sentinel/count tables were only classified, not broadly changed.
- Phase I is partial: native BaseData is used for Time Capsule validation, but
  packed Pokemon link/mobile/Time Capsule record conversion remains unfinished.

Untouched remaining work from the original request
--------------------------------------------------
- Phase A's authoritative native live Pokemon model was not implemented.
  `PokemonData`, `BoxMon`, `PartyMon`, `BattleMon`, party/daycare/box state, and
  battle staging remain packed Crystal-width records with explicit Legacy IDs.
- No native `FormId`, `AbilityId`, `NatureId`, or `PersonalityValue` fields were
  added to live Pokemon state because doing so without migrating ownership would
  create a prohibited parallel/sidecar model.
- Phase H save/SRAM isolation was inspected but not changed in this pass. The
  existing serializer still serializes the packed live `PokemonData` records.
- Full battle-to-party synchronization, boxed-Pokemon ownership, mail held-item
  ownership, move-learning storage, evolution storage, and link/mobile Pokemon
  conversions remain constrained by the live packed records.
- No global lowercase-alias replacement, script-engine redesign, trainer behavior
  redesign, save-format redesign, or network redesign was attempted.

Architectural changes actually made
-----------------------------------
- Native species content and exact Crystal BaseData layout now have distinct
  types and an explicit checked boundary.
- Shared stat/growth calculations can operate on native content without WRAM
  base-data cache authority.
- Normal/priced marts no longer rely on incompatible pointer reinterpretation.
- Native table traversal added explicit counts where touched rather than relying
  on a byte-valued sentinel.
- No second authoritative Pokemon runtime representation was introduced.

Remaining legacy/transitional bridges and removal conditions
-------------------------------------------------------------
- `GetBaseData` remains a native-to-`LegacyBaseData` WRAM cache adapter.
  Remove it only after every remaining `wBase*` consumer has migrated to native
  BaseData or an explicitly external Crystal boundary.
- `CalcExpAtLevel` and `CalcMonStatC` remain WRAM-based compatibility entry
  points. Remove them after remaining predef/link/legacy callers pass growth,
  base stat, and level explicitly.
- `TrySpeciesIdToLegacy`, `TryMoveIdToLegacy`, `TryItemIdToLegacy`, and
  `TryDexIdToLegacy` are permanent boundary helpers while Crystal formats are
  supported; remove individual call sites only when their destinations become
  native, not merely to hide an unrepresentable value.
- Script WRAM setters remain because `wCurItem` and `wCurPartySpecies` are byte
  fields. Remove them after native script runtime state owns those operands or
  restrict them solely to a decoded legacy-bytecode boundary.
- Mart scrolling-menu narrowing remains because that menu list is byte-stride
  legacy state. Remove it when the menu list itself stores `ItemId` and all menu
  traversal uses its native stride.
- `GetTMHMItemMove` still returns `move_t` for a legacy caller. Remove its
  narrowing after that UI/caller chain accepts `MoveId` end to end.
- Trainer, evolution, and NPC-trade conversion helpers remain at packed
  `PartyMon`/`BoxMon` construction. Remove them only after those records are
  replaced by native runtime Pokemon and conversion moves to save/link export.

Investigated but not implemented
--------------------------------
- The complete ownership/call graph for `BoxMon`, `PartyMon`, `BattleMon`, and
  `PokemonData` was inspected across raw `CopyBytes`, fixed structure lengths,
  party/box/daycare operations, serialization, link, Mobile, Time Capsule, and
  GB-address translation. An isolated widening was rejected because the same
  packed records are simultaneously live runtime state and exact compatibility
  data; changing one field would silently change strides and binary copies.
- `util/serialize.c` was inspected. It explicitly describes the current legacy
  Pokemon fields but still serializes the packed live structures, so no honest
  native load/save conversion could be added without first changing runtime
  ownership.
- Remaining lowercase IDs, `UINT8_MAX`, `0xff`, `255`, byte casts, and
  Box/Party/Battle/BaseData occurrences were searched and classified. Hardware
  bytes, PP/levels/flags/indexes, exact protocols, and legacy terminators were
  deliberately not mass-widened.

Compatibility and save/link implications
-----------------------------------------
- Existing Crystal-range content remains representable. Expanded BaseData and
  mart/TM source IDs are retained natively until they reach a currently legacy
  destination; unrepresentable values are rejected instead of truncated.
- Crystal save layout is unchanged. Expanded live Pokemon IDs still cannot be
  saved because live Pokemon itself remains legacy; no sidecar or high-bit hack
  was introduced.
- Link, Mobile, and Time Capsule record widths are unchanged. No protocol was
  redesigned and no compatibility format was widened.

Suspected source-level risks and regressions
--------------------------------------------
- No compiler or runtime validation was permitted. Header/signature propagation
  and the intentionally changed native `BaseData` layout require a
  next build to validate all platform/compiler branches.
- `GetSpeciesBaseData` intentionally supports only the current loaded
  `NUM_POKEMON` database. Future expanded content needs an explicit database
  count/resource API; unsupported IDs currently return no record.
- Remaining legacy callers of `GetBaseData` still receive the historical
  Bulbasaur fallback for invalid non-egg IDs. Migrated native callers receive
  `NULL` instead. This difference is intentional but not runtime-tested.
- Legacy-boundary failure behavior is not yet uniform: gameplay/script helpers
  generally report failure, while impossible base-cache/mart export currently
  logs and aborts. A later boundary-policy pass should unify this only after the
  native Pokemon owner exists.
- No confirmed regression was found by source/diff inspection.

Exact stopping point and next recommended task
----------------------------------------------
- Stopped with the native BaseData/content-query conversion coherent and the
  packed live Pokemon model deliberately unchanged. The original Phase A-I
  request is partially complete, not complete.
- Next, migrate one ownership-complete slice of `PokemonData`: define the native
  runtime Box/Party Pokemon representation, make the player party/daycare state
  authoritative in that representation, and add centralized checked conversion
  to the existing packed Crystal serializer/link records. Before changing any
  stride, enumerate and migrate every raw copy/fixed-offset caller of that slice.
  Do not add a mirrored sidecar and do not widen `BoxMon` in place.

Build commands executed
-----------------------
- None.

Test commands executed
----------------------
- None. Only source search and non-destructive Git/diff inspection were used.

Build/test results
------------------
Build/runtime validation: NOT PERFORMED — source-only migration pass.


2026-08-28 - Phase 1 native Pokemon ownership slice and application icon
======================================================================================

Task worked on
--------------
Started the first contained authoritative native-Pokemon ownership slice from
`a7f13824` after the previous Phase 1 handoff. The Bug Catching Contest's retained
caught Pokemon is now stored as a native runtime Pokemon instead of a packed
Crystal `PartyMon`, while explicit temporary conversion remains only where that
Pokemon must enter still-unmigrated party/box/WRAM/save structures. Also replaced
the project application icon with the provided Celebi artwork and wired the
Windows resource into the CMake target.

Starting repository state
-------------------------
- HEAD was `a7f13824318fb9b3b4d0dfa6bd69e055c797675f` ("Continued from last job").
- `lib/libmobile` was already dirty and remains an untouched pre-existing local change.
- No branch, commit, push, reset, restore, clean, or history operation was
  performed.

Files modified
--------------
- `.gitattributes`: marks `*.ico` as binary so Git cannot line-ending-normalize
  or otherwise text-filter the Windows icon resource.
- `CMakeLists.txt`: enables the RC language on Windows and adds `icon.rc` to the
  executable target so CMake-built Windows executables embed the project icon.
- `constants/types.h`: explicitly keeps native `BaseData` at default compiler
  packing even inside the surrounding Crystal pack region; adds unpacked
  `NativeBoxMon`/`NativePartyMon` runtime records with semantic Species/Item/Move
  IDs, native 24-bit-experience representation in `uint32_t`, and host-native
  stat-exp/HP/stat values; changes the Bug Contest retained Pokemon and backed-up
  second-party species to native representations.
- `home/pokemon.c`, `home/pokemon.h`: adds centralized checked conversion between
  packed `BoxMon`/`PartyMon` records and native Pokemon records. Native-to-legacy
  conversion validates every narrowing content ID and 24-bit experience range
  before mutating the legacy destination.
- `util/serialize.c`: teaches the temporary Crystal serializer how to project a
  native Bug Contest Pokemon back into the existing 32-byte BoxMon / 48-byte
  PartyMon save representation without making the native runtime structure
  packed; adds explicit 24-bit big-endian field handling and a native species
  list-entry serializer for the Bug Contest backup terminator.
- `engine/events/bug_contest/caught_mon.c`: the caught contest Pokemon becomes
  native immediately after the still-legacy party generator returns.
- `engine/pokemon/caught_data.c`: converts the native contest Pokemon to a
  temporary legacy record only when it must enter the still-legacy party/box
  path; raw copying between differently shaped native/legacy records was
  removed.
- `engine/events/bug_contest/contest_2.c`: stores the backed-up second party
  species as `SpeciesId` with a native terminator and performs checked conversion
  only when restoring the still-legacy party list.
- `engine/events/bug_contest/judging.c`: contest scoring now reads host-native HP
  and stat values directly; conversion to the byte-sized WRAM judging result is
  explicit and checked.
- `engine/events/bug_contest/display_stats.c`: explicitly creates the temporary
  big-endian numeric view required by the still-legacy number-printing API.
- `icon.png`: replaced with a 32x32 transparent Celebi icon derived from the
  provided artwork using nearest-neighbor scaling.
- `icon.ico`: replaced with a multi-resolution Windows icon containing 16, 24,
  32, 48, 64, 128, and 256 pixel sizes.
- `LOCAL_CHANGELOG.txt`: appended this handoff.

Files created
-------------
- None.

Files deleted
-------------
- None.

Architectural changes actually made
-----------------------------------
- `NativeBoxMon` and `NativePartyMon` are the first explicit unpacked runtime
  Pokemon records. Species, held item, and moves use `SpeciesId`, `ItemId`, and
  `MoveId` rather than Crystal byte IDs.
- Native experience is represented as a normal `uint32_t` value instead of a
  three-byte array. Native stat experience, current/max HP, and calculated stats
  are represented as host-native `uint16_t` values instead of values stored in
  the byte order expected by the old Game Boy-oriented record.
- The Bug Contest caught Pokemon is now authoritative in `NativePartyMon` form.
  There is no mirrored native/legacy sidecar for it.
- The Bug Contest second-party-species backup now uses `SpeciesId` and the native
  `SPECIES_LIST_END` terminator rather than preserving the byte terminator as
  runtime identity.
- Native-to-legacy Pokemon conversion is centralized and validates all narrowing
  before writing the legacy destination.
- `BaseData`, which was already intended to be a native record, is now explicitly
  protected from the surrounding pack(1) region on MSVC/C++ builds; exact
  Crystal layout remains isolated to `LegacyBaseData`.

Temporary migration bridges and exact removal conditions
--------------------------------------------------------
- `BugContest_SetCaughtContestMon_generatestats` still calls
  `GeneratePartyMonStats` with a temporary packed `PartyMon`, then converts the
  result to `NativePartyMon`. Remove this bridge when party/Pokemon generation
  writes the native runtime record directly.
- `CheckPartyFullAfterContest` converts the native contest Pokemon back to a
  temporary `PartyMon` before inserting it into the current player party or box.
  Remove this bridge when player party and box ownership migrate to native
  Pokemon records.
- Bug Contest judging still narrows the native species into
  `wBugContestTemp.mon` because the WRAM judging record remains byte-shaped.
  Remove that conversion when judging/result state leaves WRAM ownership.
- `util/serialize.c` still serializes the native contest Pokemon into the old
  Crystal save byte layout. This is strictly temporary scaffolding for the
  current save system and must be deleted when the authoritative PC `save.dat`
  serializer replaces Crystal SRAM persistence.
- `NativeBoxMon::DVs` deliberately retains the existing two-byte/nibble
  interpretation because current DV/Unown/stat algorithms consume that
  representation. It is not a content-ID limit. Revisit only when those
  algorithms are migrated to a clearer native DV abstraction.

Save/compatibility implications
-------------------------------
- The existing Crystal SRAM byte layout is temporarily preserved for the Bug
  Contest fields so the current save path is not silently changed mid-migration.
- This preservation is NOT a long-term compatibility requirement. The project is
  PC-only; original hardware, `.sav`, PKHeX, MBC/SRAM, and Game Boy binary-format
  compatibility are not final targets. The temporary serializer projection exists
  only until native `save.dat` becomes authoritative.
- If a future native Bug Contest Pokemon uses an ID or experience value that the
  temporary Crystal destination cannot represent, the boundary rejects it rather
  than truncating or hiding bits.

Application icon implications
-----------------------------
- `icon.png` remains the runtime SDL window-icon source on non-Windows paths that
  use it.
- `icon.ico` is the embedded Windows resource used by `icon.rc`.
- CMake now includes that resource for Windows builds; the Makefile's existing
  `icon.o`/windres path remains unchanged.

Static source audit performed
-----------------------------
- Re-read every `gPokemon.contestMon` and `bugContestSecondPartySpecies` call site
  after the type change; no remaining call site passes the native record directly
  to an API requiring `PartyMon`/`BoxMon`.
- Checked the legacy/native serializer projections by field width: both old and
  native-projection BoxMon serialization are 32 bytes; both PartyMon forms
  serialize to 48 bytes.
- Inspected the final source diff and ran `git diff --check`; no whitespace/error
  report remained.
- Confirmed the pre-existing `lib/libmobile` dirty state was not modified.

Build commands executed
-----------------------
- None.

Test commands executed
----------------------
- None. No game, compiler, CMake configure, Make/Ninja, CTest, smoke test, or
  runtime executable was invoked.

Build/test results
------------------
Build/runtime validation: NOT PERFORMED — source-only migration pass.

Known source-level risks / validation notes
----------------------------------------------------
- This changes the in-memory `PokemonData` layout, which is intended because
  `PokemonData` is a serialized native C owner rather than an exact WRAM overlay.
  The unchanged serializer output width is intended to preserve the temporary
  old save path until `save.dat` replaces it.
- The new CMake Windows resource wiring has not been compiled in this session.
- The native Pokemon conversion helpers and serializer field descriptors have
  been source-audited but not compiler- or runtime-validated.

Next recommended native-conversion task
---------------------------------------
1. Use `NativePartyMon`/`NativeBoxMon` as the model for the authoritative player
   party migration: migrate party Pokemon ownership and party species identity
   together, rather than widening `partySpecies` alone and creating divergent
   state.
2. Move party creation/generation, move/held-item access, battle staging and
   post-battle synchronization onto the native records; keep temporary checked
   conversion only at still-unmigrated box/WRAM paths.
3. Migrate boxes and daycare after the player-party slice is coherent.
4. Once live Pokemon ownership is native, implement the versioned PC-only
   `save.dat` format and remove Crystal SRAM/MBC persistence instead of extending
   the old save format further.


2026-08-29 - Native Pokemon generation and stat-calculation boundary
==================================================================================

Task worked on
--------------
Continued from the committed native Bug Contest and Day-Care ownership slices.
Made Pokemon generation itself native so native owners no longer have to create
a packed Crystal `PartyMon` and immediately convert it. Kept one checked legacy
wrapper for the still-unmigrated player/opponent party arrays.

Starting repository state
-------------------------
- HEAD was `f9bff3dce441f1a2ce26c71e64c764d33a46d5e6` on `main`.
- The user's committed `bindings.json` change was present and was not modified.
- `lib/libmobile` was already dirty and remains an untouched pre-existing local
  change.
- No commit, push, branch, reset, restore, clean, or history operation was
  performed.

Files modified
--------------
- `engine/pokemon/move_mon.c`: made native Pokemon generation authoritative,
  added host-native stat calculation/storage, retained checked legacy output,
  made party insertion transactional, corrected party-list terminator handling,
  and removed pointer/offset assumptions from stat result storage.
- `engine/pokemon/move_mon.h`: exposed native generation and native stat
  calculation APIs.
- `engine/events/bug_contest/caught_mon.c`: creates the authoritative native
  contest Pokemon directly instead of generating a packed temporary record.
- `LOCAL_CHANGELOG.txt`: appended this handoff.

Files created
-------------
- None.

Files deleted
-------------
- None.

Architectural changes actually made
-----------------------------------
- `GenerateNativePartyMonStats` is now the primary generator. It writes
  `SpeciesId`, `ItemId`, `MoveId`, native `uint32_t` experience, host-native stat
  experience, HP, maximum HP, and battle stats directly into `NativePartyMon`.
- `GeneratePartyMonStats` is now only a checked compatibility boundary: it
  generates one native record and converts it once into the packed destination.
- Native move generation and PP initialization use `FillNativeMoves` and
  `FillNativePP`; wild battle moves/items are widened at import rather than
  dictating the native record width.
- Wild battle HP and stats are decoded from the packed battle record into host
  values when entering the native generator.
- `CalcNativeMonStats` calculates directly into host-native values. The common
  stat formula explicitly distinguishes host-native stat experience from the
  big-endian values in packed Crystal records.
- Legacy PartyMon/BattleMon stat writers now calculate into a local six-stat
  result and assign each real field explicitly; they no longer rely on pointer
  arithmetic across adjacent struct members. BattleMon calculation now updates
  the BattleMon passed by the caller rather than always targeting `wEnemyMon`.
- `TryAddMonToParty` now completes generation/conversion before incrementing the
  party count or writing names/species, so a failed legacy conversion cannot
  leave a partially inserted Pokemon.

Behavior/source corrections made
--------------------------------
- Stat-experience selection now uses HP/Attack/Defense/Speed/Special indexes
  0-4 explicitly, with Sp. Def sharing the Special stat-experience field. The
  translated C previously indexed from the real array start as though the
  pointer were still the assembly routine's conceptual `MON_STAT_EXP - 1`,
  causing shifted and potentially out-of-range reads when stat experience was
  enabled.
- Adding the sixth party Pokemon now writes the explicit `partyEnd` or
  `wOTPartyEnd` byte instead of indexing one element beyond the six-entry
  species array.

Temporary migration bridges and exact removal conditions
--------------------------------------------------------
- `GeneratePartyMonStats` remains for packed player/opponent party callers.
  Remove it when both authoritative party owners store `NativePartyMon` and
  their creation paths call `GenerateNativePartyMonStats` directly.
- `TryAddMonToParty` still inserts into packed `PartyMon` and byte-sized species
  lists after checked conversion. Replace that destination when player-party
  ownership migrates; retain conversion only for genuinely external link/mobile
  records.
- Wild capture generation still imports from packed `wEnemyMon` because the
  battle runtime remains legacy-shaped. Remove that import when BattleMon
  ownership becomes native.
- `CalcMonStats`, `CalcMonStats_PartyMon`, `CalcMonStats_BattleMon`, and
  `CalcMonStatC` remain packed/big-endian compatibility APIs. Remove each after
  its remaining party, battle, link, or predef callers use native Pokemon/stat
  state.

Compatibility and save implications
-----------------------------------
- No save format or serialized width changed in this session.
- Existing player/opponent party generation still receives the same packed
  structure through an explicit checked conversion. Expanded IDs that cannot
  enter that destination fail rather than truncate.
- The native Bug Contest Pokemon no longer depends on packed-ID
  representability during generation; the temporary Crystal save/party/box
  export boundaries remain responsible for any later representability check.

Static source audit performed
-----------------------------
- Inspected all `GeneratePartyMonStats`, `GenerateNativePartyMonStats`,
  `CalcMonStats`, `CalcNativeMonStats`, and `CalcMonStatC` call sites.
- Verified the three remaining `CalcMonStatC` callers pass the actual five-entry
  packed stat-experience array, matching the corrected explicit index logic.
- Reviewed native/legacy endian transitions for experience, HP, maximum HP,
  stats, and stat experience.
- Inspected the complete source diff and ran `git diff --check`; only unrelated
  line-ending warnings for existing files were emitted.
- Confirmed `bindings.json` and the pre-existing dirty `lib/libmobile` submodule
  were not modified.

Build commands executed
-----------------------
- None.

Test commands executed
----------------------
- None. No compiler, CMake configure/build, Make/Ninja, executable, CTest, or
  runtime smoke test was invoked.

Build/test results
------------------
Build/runtime validation: NOT PERFORMED — per user instruction.


Latest handoff marker: 2026-08-30 — native logical input ownership
---------------------------------------------------------------------
Logical game input now has a native C owner. All direct C writers of the
logical HRAM joypad mirrors were removed, and shared menu/text paths consume
the native API. The current stopping point is the remaining read-only mirror
consumers outside those shared paths.

Build/runtime validation: NOT PERFORMED — per user instruction.


Session: 2026-08-30 — Complete logical-input consumer migration
=================================================================

Task worked on
--------------
- Finished the active Phase 7 logical-input migration by auditing and
  converting every remaining live C consumer of the legacy logical HRAM input
  bytes.

Files modified
--------------
- `home/joypad.c`: removed the last logical-input HRAM mirror writer; the
  existing native logical input-frame state and semantic reset/update APIs are
  now the sole runtime authority.
- `engine/debug/color_picker.c`, `engine/events/battle_tower/battle_tower.c`,
  `engine/events/halloffame.c`, `engine/events/mom.c`,
  `engine/events/print_unown.c`, `engine/games/card_flip.c`,
  `engine/games/unown_puzzle.c`, `engine/link/lan.c`, `engine/link/link.c`,
  `engine/link/link_trade.c`, `engine/menus/debug_field_menu.c`,
  `engine/menus/debugmenu.c`, `engine/menus/intro_menu.c`,
  `engine/menus/naming_screen.c`, `engine/menus/trainer_card.c`,
  `engine/movie/evolution_animation.c`, `engine/movie/intro.c`,
  `engine/movie/splash.c`, `engine/overworld/events.c`,
  `engine/overworld/player_movement.c`, `engine/pokedex/pokedex.c`,
  `engine/pokegear/pokegear.c`, `engine/pokemon/bills_pc.c`,
  `engine/pokemon/mail_2.c`, `engine/pokemon/party_menu.c`,
  `engine/pokemon/stats_screen.c`, `engine/printer/printer.c`,
  `engine/rtc/reset_password.c`, `engine/rtc/timeset.c`, `home/flag.c`,
  `mobile/fixed_words.c`, `mobile/mobile_12.c`, `mobile/mobile_12_2.c`,
  `mobile/mobile_40.c`, `mobile/mobile_45_stadium.c`, `mobile/mobile_46.c`,
  `mobile/mobile_5c.c`, `mobile/mobile_5e.c`, and `mobile/mobile_5f.c`:
  migrated logical held, pressed, released, and last-input reads to the native
  input API without changing their button masks or control flow.
- `hram.h`, `hram.c`: replaced the four named logical-input fields with a
  four-byte `legacyLogicalInputReserved` span, retaining the validated packed
  HRAM offsets but removing these fields as executable runtime state.
- `LOCAL_CHANGELOG.txt`: recorded this completed slice.

Files created/deleted
---------------------
- None.

Completed architectural changes
-------------------------------
- All live C logical game-input consumers now read `NativeInputState` through
  `NativeInputLogicalHeld`, `NativeInputLogicalPressed`,
  `NativeInputLogicalReleased`, and `NativeInputLogicalLast`.
- `home/joypad.c` no longer mirrors logical input into HRAM. Physical
  `hJoypad*` fields remain a separate, still-packed compatibility concern.
- The old `hJoyReleased`, `hJoyPressed`, `hJoyDown`, and `hJoyLast` slots are
  no longer named runtime fields; only their four-byte location remains to
  preserve the current offset-validated HRAM representation.
- `v_StatsScreenDPad` now accepts a value rather than an address into the old
  HRAM mirror, eliminating its final pointer-shaped dependency on that state.

Temporary migration state and exact removal condition
------------------------------------------------------
- `legacyLogicalInputReserved[4]` is a layout-only bridge. Remove it when the
  packed HRAM structure and its offset validation are replaced by native game
  state, and no legacy import/debug/tooling requirement depends on this span.
- The global address-map names and historical ASM comments remain source
  references only; they are not live C runtime consumers. Remove or regenerate
  those representations only as part of the larger packed-memory retirement.

Compatibility and save-format implications
------------------------------------------
- Logical button semantics, auto-input behavior, and menu/input control flow
  are preserved. No save, link, mobile-data, or input-binding format changed.

Investigated and deliberately not implemented
----------------------------------------------
- Did not remove the entire HRAM structure or the physical `hJoypad*` fields:
  their packed layout is still used elsewhere in the legacy runtime and that
  is a distinct migration task.
- Did not begin player-party ownership conversion. The existing party model
  requires an explicit native egg/hatch state before its packed species list
  can be retired safely.

Completed work
--------------
- Native logical input-frame ownership and every live C logical-input reader
  are fully migrated for this slice.

Partially completed work
------------------------
- Native input as a whole still retains physical HRAM compatibility fields and
  lacks a remapping UI; those are separate remaining tasks.

Untouched remaining work
------------------------
- Native player-party ownership with first-class egg state, native save
  serialization, remaining packed-memory retirement, rendering/entity/map/
  script/audio conversion, and the later roadmap phases.

Build/test commands and results
-------------------------------
- None. Build/runtime validation: NOT PERFORMED — per user instruction.

Exact stopping point and next recommended task
----------------------------------------------
- Stopped after removing the final live logical HRAM input consumer and
  demoting its four legacy bytes to a layout-only reservation.
- Next: design the native player-party owner with an explicit egg/hatch field,
  then migrate party creation, hatching, storage, and serialization together.


Latest handoff marker: 2026-08-30 — logical HRAM input retired
----------------------------------------------------------------
The logical input runtime no longer depends on the four named logical HRAM
bytes. `NativeInputState` is authoritative; the former HRAM positions are a
reserved packed-layout span only.

Build/runtime validation: NOT PERFORMED — per user instruction.


Session: 2026-08-30 — Native logical input consumers: overworld and UI group
===============================================================================

Task worked on
--------------
- Continued Phase 7 by migrating the overworld input path and a coherent group
  of ordinary menus, event UIs, minigames, and movie input consumers from
  logical HRAM mirror reads to the native logical-input API.

Starting state
--------------
- The prior native physical/logical/scripted input owners were present in the
  repository. `lib/libmobile` remained the sole pre-existing dirty path and
  was not modified.

Files modified
--------------
- `engine/overworld/events.c`, `engine/overworld/player_movement.c`: migrated
  action/menu, debug-combination, encounter suppression, movement, and bike
  speed input reads to native logical state.
- `engine/menus/intro_menu.c`, `engine/menus/naming_screen.c`, and
  `engine/menus/trainer_card.c`: migrated title/debug combinations, naming,
  mail composition, and Trainer Card input reads.
- `engine/events/battle_tower/battle_tower.c`, `engine/events/halloffame.c`,
  `engine/events/mom.c`, and `engine/events/print_unown.c`: migrated their
  event-menu input reads.
- `engine/games/card_flip.c`, `engine/games/unown_puzzle.c`,
  `engine/movie/evolution_animation.c`, `engine/movie/intro.c`, and
  `engine/movie/splash.c`: migrated minigame/movie input reads.
- `engine/pokedex/pokedex.c`: migrated all Pokédex navigation, search, and
  entry input reads to native logical state.
- `LOCAL_CHANGELOG.txt`: recorded the completed consumer group.

Files created/deleted
---------------------
- None.

Completed architectural changes
-------------------------------
- The migrated gameplay paths no longer treat `hJoyDown`, `hJoyPressed`, or
  `hJoyLast` as their input authority. They read the normal native logical
  input state populated by `GetJoypad` and scripted input.
- Overworld movement copies native logical held input into its still-legacy
  `wCurInput` destination only at that explicit downstream compatibility point.

Temporary migration bridges and removal conditions
---------------------------------------------------
- Logical HRAM bytes remain output mirrors for remaining C readers and any
  layout-dependent compatibility consumer. Remove each field only after all
  remaining readers have migrated and the packed HRAM layout no longer needs
  that byte.
- `wCurInput` remains a legacy WRAM movement destination. Remove that bridge
  when native player/movement state replaces the WRAM movement model.

Partially completed work
------------------------
- Logical input ownership is native, and the overworld, ordinary menu/event,
  minigame/movie, and Pokédex consumer groups are native. PC/party, RTC, link,
  mobile, and debug consumers still read HRAM mirrors.

Untouched remaining work
------------------------
- Remaining input consumers; first-class native player-party egg/hatch state;
  native party ownership; native persistence; and all later conversion phases.

Compatibility and save-format implications
------------------------------------------
- Input bit assignments, menu repeat behavior, scripted input, and save data
  are unchanged. No serializer or persistent format changed.

Build/test commands and results
-------------------------------
- None. Build/runtime validation: NOT PERFORMED — per user instruction.

Suspected risks and exact stopping point
----------------------------------------
- This source slice has not been compiled after the changes. The next user
  build remains necessary to detect strict-warning integration issues.
- Stopped after completing the overworld/UI and Pokédex consumer groups. Next:
  migrate the PC/party, RTC, and link consumer groups before starting the
  native party ownership slice.


Build-feedback correction: 2026-08-30
--------------------------------------
- User-provided `build_windows_release.log` showed a `-Werror=parentheses`
  failure in `home/menu.c` caused by macro expressions used as bit masks after
  the native input migration.
- Corrected the required mask parentheses in `home/menu.c` and the matching
  shared scrolling-menu expression in `engine/menus/scrolling_menu.c`.
- The failed user build removed tracked `Release/` output assets before
  stopping. Those deletions are user/build-owned state and were preserved;
  this session did not restore, remove, or otherwise alter them.
- Build/runtime validation after this correction: NOT PERFORMED.


Session: 2026-08-30 — Native logical input-frame ownership
============================================================

Task worked on
--------------
- Continued Phase 7 by moving the translated game's logical input frame
  (held, pressed, released, and menu/text last input) into the native input
  subsystem.

Files modified
--------------
- `util/input.c`, `util/input.h`: added native logical-input state and its
  explicit frame, scripted-input, and reset operations.
- `home/joypad.c`, `home/joypad.h`: made native logical state authoritative;
  the legacy HRAM logical joypad fields are now synchronized output mirrors.
- `engine/printer/printer.c`, `engine/events/catch_tutorial.c`,
  `engine/movie/splash.c`, `engine/menus/intro_menu.c`,
  `engine/pokemon/mail_2.c`, and `engine/menus/options_menu.c`: replaced
  direct logical-HRAM state mutation with semantic joypad reset operations.
- `home/menu.c`, `home/scrolling_menu.c`, `home/print_text.c`, `home/text.c`,
  `engine/menus/scrolling_menu.c`, and `engine/menus/options_menu.c`:
  migrated their common menu/text input reads to native logical-input APIs.
- `LOCAL_CHANGELOG.txt`: recorded this conversion.

Files created/deleted
---------------------
- None.

Completed architectural changes
-------------------------------
- Logical game input no longer uses the packed HRAM bytes as authoritative
  state. `GetJoypad`, scripted tutorial input, and text-delay processing now
  update native C state; HRAM receives compatibility copies only.
- All direct C writers of `hJoyReleased`, `hJoyPressed`, `hJoyDown`, and
  `hJoyLast` outside `home/joypad.c` were removed.

Temporary migration state and exact removal condition
------------------------------------------------------
- The four logical HRAM joypad bytes remain output mirrors for unconverted C
  consumers and any non-C legacy-layout user. Remove them only after every
  remaining reader is migrated and the packed HRAM layout has no compatibility
  obligation for these offsets.

Compatibility and save-format implications
------------------------------------------
- Physical SDL timing, text/menu repeat behavior, scripted-input duration,
  and existing input-reset points are retained. No save or external data
  format changed.

Partially completed work
------------------------
- Common menu, scrolling-menu, text, printer, tutorial, splash, intro, mail,
  and options input paths use native logical state. Other menu, overworld,
  battle, mobile, link, and debug consumers still read logical HRAM mirrors.

Untouched remaining work
------------------------
- Remaining logical-mirror consumers, action remapping UI, native party and
  save ownership, and subsequent native-conversion roadmap phases.

Build/test commands and results
-------------------------------
- None. Build/runtime validation: NOT PERFORMED — per user instruction.

Exact stopping point and next recommended task
----------------------------------------------
- Stopped after establishing native logical input ownership and migrating the
  shared text/menu paths plus all direct external logical-mirror writers.
- Next: migrate the overworld input consumer group (`events.c`, player
  movement, and text/menu callers reached from it) to native logical APIs,
  then continue with bounded menu/battle groups.


Session: 2026-08-30 — Native input-frame ownership
====================================================

Task worked on
--------------
- Continued Phase 7 by migrating the physical-input frame state and every
  direct C consumer/writer of the legacy `hJoypad*` physical mirrors.

Files modified
--------------
- `util/input.c`, `util/input.h`: added native held/pressed/released and
  accumulated-press state, frame advancement, reset, and explicit override
  operations.
- `home/joypad.c`: makes native input state authoritative for physical frame
  deltas and writes the HRAM physical joypad bytes as compatibility mirrors.
- `engine/menus/save.c`: clears native frame state at the existing save-input
  suppression point.
- `engine/games/slot_machine.c`: uses native accumulated presses and clears
  them through the native API between reels.
- `engine/pokegear/pokegear.c`, `engine/movie/credits.c`, and
  `mobile/fixed_words.c`: migrated all direct physical held/pressed reads to
  native APIs; the EZChat override is now explicit native state.
- `LOCAL_CHANGELOG.txt`: recorded the migration.

Files created/deleted
---------------------
- None.

Completed architectural changes
-------------------------------
- Physical input frame state is now native SDL-derived state, not HRAM state.
  The remaining `hJoypadDown`, `hJoypadPressed`, `hJoypadReleased`, and
  `hJoypadSum` fields are output mirrors written only by `UpdateJoypad`.
- All direct C reads and writes of those four HRAM physical-input fields were
  removed outside the compatibility-mirror writer.

Temporary migration state and exact removal condition
------------------------------------------------------
- The four HRAM fields remain to support unconverted legacy-facing consumers
  outside C and the packed HRAM model. Remove them when all remaining direct
  mirror consumers (`hJoyDown`, `hJoyPressed`, `hJoyReleased`, and `hJoyLast`)
  have been migrated to native input snapshots and HRAM layout is retired.

Compatibility and save-format implications
------------------------------------------
- Button bit meanings, input timing, tutorial overrides, save input clearing,
  and slot-machine press accumulation are preserved. No persistent format
  changed.

Build/test commands and results
-------------------------------
- None. Build/runtime validation: NOT PERFORMED — per user instruction.

Exact stopping point and next recommended task
----------------------------------------------
- Stopped after the physical input owner and its direct C mirror consumers
  were migrated as one coherent group.
- Next: migrate the logical menu-input mirrors (`hJoyDown`, `hJoyPressed`,
  `hJoyReleased`, and `hJoyLast`) in bounded menu/text groups, then retire
  their HRAM authority.


Session: 2026-08-30 — Native SDL input source
================================================

Task worked on
--------------
- Audited whether assembly is part of the Windows executable and migrated the
  active SDL input path off the emulator's joypad register.

Files modified
--------------
- `util/input.c`, `util/input.h`: made SDL event handling own an active-high
  native action-state byte and exposed it through `NativeInputHeld`.
- `home/joypad.c`: changed `UpdateJoypad` to read that native state instead of
  selecting and reading Game Boy `rJOYP` hardware.
- `tools/emu/peanut_sdl.c`: stopped routing SDL events through
  `gb.direct.joypad`.
- `constants/types.h`: corrected the `legacyPartySpecies` comment to document
  its real `EGG` hatch-marker meaning; no layout or behavior changed.
- `LOCAL_CHANGELOG.txt`: recorded this source-level audit and migration.

Files created/deleted
---------------------
- None.

Completed architectural changes
-------------------------------
- The executable's Makefile and CMake source lists compile C sources only;
  neither invokes an assembler nor lists `.asm` inputs. The repository's ASM
  files are reference/source-material files, so their file-count percentage is
  not a measure of live executable implementation.
- SDL input is now native runtime state. The gameplay input update no longer
  depends on emulated JOYP selection, active-low conversion, or repeated
  hardware-register reads.

Legacy/transitional state and removal condition
------------------------------------------------
- `UpdateJoypad` still copies native input into HRAM-compatible pressed/down
  mirrors because current menus, battle, text, and overworld consumers read
  those fields directly. Remove those mirrors only after all such consumers
  use a native input snapshot/API; they are now outputs, not the input source.
- The emulator's `gb.direct.joypad` storage remains for emulator internals but
  is no longer fed by SDL events or read by the game input path. Remove it
  when the remaining emulator JOYP register implementation has no users.

Compatibility and save-format implications
------------------------------------------
- Action bit positions and key/controller mappings are unchanged. No save,
  link, input configuration, or party layout changed.

Partially completed work
------------------------
- Phase 7 native input has begun: physical input acquisition is native, but
  gameplay consumers still use legacy mirror bytes.
- Player-party conversion remains unstarted. `legacyPartySpecies` cannot be
  derived from packed party species because it stores the `EGG` hatch marker.

Untouched remaining work
------------------------
- Native input snapshots/actions at game consumers, input remapping UI, native
  party ownership with explicit egg state, native saves, and later roadmap
  phases.

Build/test commands and results
-------------------------------
- None. Build/runtime validation: NOT PERFORMED — per user instruction.

Exact stopping point and next recommended task
----------------------------------------------
- Stopped after replacing the live emulated JOYP input route with native SDL
  action state and retaining behavior-compatible mirrors.
- Next: migrate a bounded group of direct HRAM input consumers to a native
  snapshot API, then remove their mirror dependency. Continue choosing work by
  live runtime dependency rather than raw reference-ASM file counts.


Correction: 2026-08-30 — Party-species cache semantics
========================================================

This correction supersedes the immediately preceding "Party-species
compatibility-cache demotion" completion claims.

During source inspection, `Script_giveegg(TOGEPI, ...)` and `GiveEgg` showed
that Crystal intentionally stores the concealed species in the packed party
record while placing `EGG` in the party-species list as the hatch-state marker.
The list therefore is not yet a purely redundant cache. Rebuilding it from the
packed record would silently erase that marker and regress egg behavior/save
compatibility.

Completed work
--------------
- Retained the explicit field rename to `legacyPartySpecies` in all C users,
  clarifying that it is a legacy compatibility representation rather than a
  general native content-ID container.
- Preserved every existing party mutation, link, Mobile Adapter, Time Capsule,
  Bug Contest, reset, and serializer behavior after the inspection found the
  egg-state dependency.
- The earlier false UTF-8 diagnostic correction in `util/util_string.c`
  remains completed and is unrelated to party ownership.

Partially completed work
------------------------
- The field rename documents the real migration boundary, but it does not
  remove the legacy list or convert player-party ownership. No cache-derived
  serializer, synchronization helper, native sidecar, or party layout change
  remains in the source.

Investigated but not implemented
--------------------------------
- A native party migration must represent egg/hatch state explicitly (for
  example, a semantic egg field in a native Pokemon record) before it can
  remove `legacyPartySpecies`. Inferring egg state from the current packed
  species field is incorrect.
- The preceding attempted cache-derivation edit was fully removed in this
  session before handoff; it must not be treated as completed work.

Compatibility and save-format implications
------------------------------------------
- None beyond the C field rename. Crystal save, link, Mobile Adapter, Time
  Capsule, and egg marker bytes retain their existing behavior.

Build and test commands executed
--------------------------------
- None.

Build/runtime validation
------------------------
- NOT PERFORMED — per user instruction.

Exact stopping point and next recommended task
----------------------------------------------
- Stopped with `legacyPartySpecies` explicitly named, but still behaviorally
  required by the packed-party compatibility model.
- Next recommended Phase 1 task: design an ownership-complete native party
  record that includes a first-class egg/hatch-state field, then migrate party
  creation, hatching, serialization, and legacy link/mobile projections
  together. Do not derive the legacy party list from the packed species field.
- The original long-term conversion request remains partially complete.


Latest handoff marker: 2026-08-30
---------------------------------
The latest source inspection preserved the legacy egg marker and corrected the
party-cache handoff record. Native player-party ownership remains unfinished.

Build/runtime validation: NOT PERFORMED — per user instruction.


Session: 2026-08-29 — Windows Release build script
===================================================

Task worked on
--------------
- Added a user-invoked Windows-only Release build script.

Files modified
--------------
- `LOCAL_CHANGELOG.txt`: documented the build-script addition.

Files created
-------------
- `build_windows_release.bat`: locates an MSYS2 UCRT64 environment, creates the
  repository `Release` directory, performs a clean optimized Windows build, and
  writes the resulting executable to `Release/suiCune.exe`.

Files deleted
-------------
- None.

Purpose and compatibility implications
--------------------------------------
- The script follows the repository's documented MSYS2/Make Windows toolchain
  and requests `RELEASE=1` with parallel compilation.
- It overrides only the linker output prefix so the Windows executable is
  emitted into `Release`; it does not change project source, runtime behavior,
  save data, native architecture, or non-Windows build configuration.
- The script expects the UCRT64 GCC, Make, SDL2, SDL2_net, and PhysFS packages
  described by `README.md`. It checks the conventional `C:\msys64` location or
  an explicitly configured `MSYS2_ROOT`.

Build commands executed
-----------------------
- None.

Test commands executed
----------------------
- None.

Build/test results
------------------
Build/runtime validation: NOT PERFORMED — per user instruction. The batch file
was source-inspected only and is intended for the user to execute.


Session: 2026-08-29 — Player-party species authority consolidation
==================================================================

Task worked on
--------------
- Continued Phase 1 from the native decoded PC-box stopping point.
- Performed a bounded preparatory slice for the future native player-party
  conversion: normal gameplay and UI species reads now use the species stored
  in each authoritative `PartyMon` record and iterate by `partyCount`, rather
  than treating the parallel terminator-based `partySpecies` byte list as
  runtime authority.
- Did not begin the full `NativePartyMon` ownership conversion, save-format
  replacement, link-format conversion, or another subsystem.

Starting repository state
-------------------------
- Continued with all uncommitted native PC-box changes from the preceding
  session intact.
- `lib/libmobile` was already dirty and remains an untouched pre-existing
  user-owned change.
- No user-owned changes were reset, restored, cleaned, committed, or pushed.

Files modified in this continuation
-----------------------------------
- `LOCAL_CHANGELOG.txt`: appended this continuation record.
- `engine/battle/core.c`: player battle setup, fit-to-fight checks, egg checks,
  and experience setup read species from the selected party record.
- `engine/events/battle_tower/battle_tower.c`: Egg Ticket party scanning now
  uses `partyCount` and party-record species.
- `engine/events/battle_tower/rules.c`,
  `engine/events/battle_tower/rules.h`: Battle Tower egg and uniqueness checks
  no longer receive or traverse the duplicate party-species list.
- `engine/events/field_moves.c`: Fly icon setup reads the selected party record.
- `engine/events/halloffame.c`: Hall of Fame collection iterates `partyCount`
  and reads party-record species, then writes the Hall of Fame terminator.
- `engine/events/happiness_egg.c`: first-mon, egg, and step-happiness species
  queries use party records; corrected the inverted empty-party guard that made
  `StepHappiness` return for every nonempty party.
- `engine/events/lucky_number.c`: party lottery checks use party-record ID and
  species fields; native PC-box lottery handling remains native until the
  checked temporary legacy-script species boundary.
- `engine/events/overworld.c`: field-move, Surf, and Strength species queries
  use party records and the party count.
- `engine/events/sacred_ash.c`: fainted-party scanning uses party-record egg
  state.
- `engine/events/unown_walls.c`: the first-party Ho-Oh check uses the party
  record.
- `engine/gfx/mon_icons.c`: party-menu icon selection uses the party record.
- `engine/overworld/scripting.c`: `checkpoke` iterates the active party count
  and reads party-record species.
- `engine/pokegear/pokegear.c`: Fly-map icon selection uses the party record.
- `engine/pokemon/bills_pc.c`: party portions of PC listing and selection use
  party-record species; the obsolete party `CopySpeciesToTemp` helper was
  removed. Existing native decoded-box work in this file was preserved.
- `engine/pokemon/breeding.c`: egg-step and hatch scans use `partyCount` and
  party-record species; the transitional species-list synchronization write on
  hatch remains.
- `engine/pokemon/evolve.c`, `engine/pokemon/evolve.h`: evolution traversal no
  longer receives a pointer into the duplicate species list; it iterates by
  `partyCount` and reads the selected party record. Successful evolution still
  synchronizes the legacy list for remaining consumers.
- `engine/pokemon/health.c`: party healing iterates `partyCount` and checks egg
  state from party records.
- `engine/pokemon/mon_menu.c`: move-screen cycling, icon selection, and arrow
  availability use party indices/count and party-record species.
- `engine/pokemon/party_menu.c`: TM/HM compatibility, gender, egg, and selection
  queries use party-record species.
- `engine/pokemon/search2.c`: party species searches and egg filtering use the
  active party count and party records instead of sentinel traversal.
- `engine/pokemon/tempmon.c`: `PARTYMON` species lookup now reads the selected
  party record directly; opponent, box-SRAM, and Day-Care compatibility cases
  retain their explicit boundaries.

Files created
-------------
- None.

Files deleted
-------------
- None.

Completed work
--------------
- Consolidated normal player-party species reads in battle, party UI, field
  moves, icons, scripts, healing, breeding/hatching scans, Hall of Fame,
  evolution traversal, Battle Tower rules, the lottery, and temporary-mon
  lookup onto the corresponding `PartyMon.mon.species` field.
- Replaced the migrated sentinel-based loops with bounded `partyCount` loops.
- Removed the duplicate species-array parameter from both Battle Tower party
  uniqueness helpers and updated their declarations and callers.
- Removed the now-dead party `CopySpeciesToTemp` helper from Bills PC.
- Corrected `StepHappiness` so it returns for an empty party rather than a
  nonempty party.

Partially completed work
------------------------
- Player-party conversion remains preparatory only. `PokemonData.partyMon`
  still contains packed legacy `PartyMon` records, whose embedded species,
  moves, and item fields remain byte-sized.
- `PokemonData.partySpecies` remains a transitional duplicate list. Normal
  gameplay/UI readers were migrated in this slice, but creation, insertion,
  removal, swapping, Day-Care, contest, debug/initialization, evolution/hatch
  synchronization, and link/time-capsule paths still maintain or consume it.
- Native decoded PC boxes still project through legacy buffers for unmigrated
  PC UI/party-transfer operations, as documented in the preceding session.

Untouched remaining work
------------------------
- Full `NativePartyMon` ownership and migration of every party mutation path.
- Removal of `partySpecies` after all mutation, initialization, link, mobile,
  save, and transfer users have explicit native or legacy-boundary handling.
- Native opponent-party ownership and battle staging.
- Native versioned `save.dat` authority and removal of SRAM-layout persistence.
- Link/time-capsule format redesign or explicit import/export isolation.
- Later roadmap phases, including native game loop, renderer, audio, map,
  script, entity, content-database, expanded Pokemon model, and multiplayer.

Architectural changes actually made
-----------------------------------
- The duplicate terminator-based `partySpecies` array is no longer the normal
  read authority for the migrated gameplay and UI routines. Active membership
  comes from `partyCount`, and species comes from the corresponding party
  record.
- Evolution and Battle Tower helper APIs no longer require callers to pass the
  parallel species list solely to determine party membership or egg state.
- No new wrapper, sidecar, fallback implementation, or parallel native party
  owner was introduced.

Obsolete Game Boy/emulation dependency removed
----------------------------------------------
- Removed terminator-list traversal from the migrated normal party readers.
  The underlying packed party records and the compatibility species list have
  not yet been removed and are not claimed as native.

Temporary migration code and exact removal conditions
------------------------------------------------------
- `partySpecies` remains synchronized by party mutation paths, evolution, egg
  hatching, and contest handling. Remove it only after `NativePartyMon` is the
  sole player-party owner and link/save/mobile boundaries construct explicit
  legacy payloads from native records.
- Player party records remain packed `PartyMon`. Replace them only as one
  ownership-complete slice that migrates creation, mutation, UI, battle staging,
  stats/status, moves/items, transfer, and serialization users together.
- Checked native-box-to-legacy PC UI/transfer conversions from the prior slice
  remain. Remove them when Bills PC and player-party transfer buffers operate on
  native Pokemon records, retaining conversion only at genuine legacy export
  boundaries.

Compatibility and save-format implications
------------------------------------------
- This continuation does not intentionally change the serialized save format.
- Link, time-capsule, Crystal SRAM, and existing party mutation paths still see
  the synchronized byte list. Expanded species still cannot enter those legacy
  boundaries and must continue to fail checked conversion rather than truncate.
- The preceding native PC-box in-memory layout and serializer implications
  remain unchanged.

Investigated but not implemented
--------------------------------
- Classified every remaining direct `gPokemon.partySpecies` reference. The
  remaining references are concentrated in mutation/initialization code,
  Bug Contest party masking, and explicit link/time-capsule consumers.
- Inspected Bug Contest drop-off/return behavior. It still temporarily masks
  the party by editing the legacy list and reconstructs `partyCount`; changing
  it safely belongs with the complete party mutation migration, so it was not
  altered here.
- Inspected link and time-capsule consumers but did not change their byte-format
  behavior in this slice.

Suspected source-level risks
----------------------------
- The source has not been compiled; declaration/type integration remains to be
  compiler-validated by the user.
- `GetFirstPokemonHappiness` retains the existing gameplay invariant that a
  caller has at least one non-egg party member. This slice bounds its egg scan
  by `partyCount`, but did not invent behavior for an all-egg party.
- Correcting the inverted `StepHappiness` guard activates the routine for
  nonempty parties as its translated comments and loop require; runtime
  confirmation remains outstanding.
- The party record and `partySpecies` must remain synchronized until the
  transitional list is removed. Any undiscovered mutation path that updates
  only one representation remains a risk of the pre-existing dual state.

Build commands executed
-----------------------
- None.

Test commands executed
----------------------
- None. No game executable, compiler, CMake configure, Make/Ninja, CTest, or
  runtime smoke test was invoked.

Build/test results
------------------
Build/runtime validation: NOT PERFORMED — per user instruction.

Exact current stopping point
----------------------------
- Normal gameplay/UI reads of player-party species have been consolidated onto
  `partyMon[index].mon.species` and active iteration onto `partyCount`.
- The authoritative player-party owner is still the packed `PartyMon` array,
  not `NativePartyMon`; the legacy `partySpecies` cache remains for mutation and
  compatibility paths.
- The original Phase 1 player-party migration is therefore partially completed,
  not complete.

Exact recommended next task
---------------------------
1. Audit and migrate the player-party mutation cluster as one coherent unit:
   initialization, add/remove, swap, Day-Care/contest transfer, hatch/evolution
   updates, and debug setup.
2. Introduce native party ownership only when those mutation paths, semantic
   move/item/species fields, and party serialization can migrate together.
3. Derive explicit legacy link/time-capsule/save payloads at their boundaries,
   then remove `partySpecies` once repository-wide references reach zero.


2026-08-29 - Native decoded PC-box migration
===========================================================

Task worked on
--------------
- Continued Phase 1 from the committed native Pokemon generation/stat boundary.
- Migrated decoded PC-box records and their normal C consumers from packed
  `BoxMon`/byte species storage to native semantic Pokemon records.
- This is a substantial continuation of Phase A, not completion of the full
  Phase 1 prompt. Player-party and battle ownership remain unfinished.

Starting repository state
-------------------------
- Started at `5bad31c2` (`more changes`). The preceding generation/stat work was
  already committed by the user and was treated as the new baseline.
- `lib/libmobile` was the sole pre-existing dirty path and remained untouched.
- No other pre-existing user modification was present when this pass began.

Files modified
--------------
- `LOCAL_CHANGELOG.txt`: appended this handoff record.
- `constants/types.h`: replaced the ambiguous packed decoded `Box` record with
  unpacked `NativeBox`, native Pokemon entries, and a clearly named transitional
  Crystal species-list round-trip cache.
- `util/serialize.c`, `util/serialize.h`: migrated box serialization APIs and
  descriptors to `NativeBox`/`NativeBoxMon`, rebuilt Crystal's redundant active
  species list from authoritative native mons on write, and rejected invalid
  box counts.
- `engine/pokemon/move_mon.c`, `engine/pokemon/move_mon.h`: migrated box
  deposit/withdraw, new boxed-mon creation, PP restoration, insertion shifts,
  and removal shifts to native records and checked party-boundary conversion.
- `engine/pokemon/move_mon_wo_mail.c`: converts the legacy temporary PC buffer
  once before inserting a native box record; native record stride is used for
  insertion rather than `BoxMon` stride.
- `engine/items/item_effects.c`, `engine/items/item_effects.h`: added direct
  maximum-PP calculation for `NativeBoxMon`/`MoveId` without staging moves in a
  packed temporary record.
- `engine/pokemon/bills_pc.c`, `engine/pokemon/bills_pc_top.c`: migrated decoded
  box access to native entries and centralized checked projection only where
  the current WRAM-era PC UI still requires packed temporary fields.
- `engine/pokemon/search.c`, `engine/pokemon/search.h`: added native ownership
  comparison, widened the semantic species query, and made box searches use the
  native mon record.
- `engine/events/lucky_number.c`: made box lottery checks read native mon IDs and
  species, retaining checked conversion only for the legacy display state.
- `engine/pokemon/caught_data.c`: added native caught-data mutation for boxed
  Pokemon while retaining the packed party helper.
- `engine/printer/printer.c`, `engine/printer/printer.h`: migrated decoded box
  printing/gender access to native mons and count-based traversal.
- `engine/pokemon/mon_stats.c`: updated decoded box DV access to `NativeBox`.

Files created
-------------
- None.

Files deleted
-------------
- None.

Completed work
--------------
- `NativeBox` is an unpacked native decoded structure. Its Pokemon entries are
  `NativeBoxMon`, so species, held item, moves, and experience remain semantic
  native values while normal C box operations are in progress.
- Active boxed species now have one runtime authority: `mons[i].species`.
  Runtime consumers no longer maintain or consult Crystal's separate species
  list when inserting, deleting, searching, printing, or listing boxed mons.
- Crystal's redundant species list is now an explicitly named serialization
  cache only. `Serialize_Box` refreshes active entries and the terminator from
  authoritative native mons before writing the old SRAM image.
- Box deposit/withdraw validates the complete native/legacy conversion before
  mutating party or box counts. No content ID is silently truncated.
- Box record insertion/removal uses native record stride. The prior raw copy of
  a packed `BoxMon` into native storage and the prior raw copy of a native box
  record into a packed WRAM temporary record have been removed.
- Native PP restoration reads `MoveId` directly through
  `GetMaxPPOfNativeMove`; it no longer copies widened moves into the byte-sized
  temporary Pokemon.
- PC UI, printer, and lottery paths use checked native-to-legacy conversion at
  their remaining true WRAM display boundaries and fail explicitly if a future
  expanded ID cannot be represented.
- Box traversal now uses the decoded count/native record array rather than the
  byte species-list sentinel. Search of non-current boxes now uses `box.count`
  instead of the box-number loop index.
- Deposited-mon PP restoration now targets `box.count - 1`, the actual newly
  appended entry, instead of the following unused slot.
- The printer's out-of-bounds diagnostic read of `boxSpecies[-1]` was removed.

Architectural changes actually made
-----------------------------------
- The decoded PC-box API is no longer byte-layout compatible with Crystal SRAM
  and is no longer packed. Field/descriptor serialization is the only supported
  conversion between the decoded representation and the Crystal byte image.
- Box manipulation, queries, caught-data updates, lottery checks, and printer
  reads now operate on `NativeBoxMon` rather than treating packed `BoxMon` as a
  live C record.
- The redundant Crystal box species list ceased to be runtime-authoritative;
  it survives only to preserve unused legacy bytes during exact SRAM round trips.
- Packed `BoxMon` remains an explicit player-party/WRAM/link compatibility type,
  not the decoded PC-box gameplay representation.

Obsolete Game Boy/emulation dependency removed
-----------------------------------------------
- Normal decoded box logic no longer depends on Crystal's packed 32-byte mon
  stride, byte species-list traversal, or duplicate active species ownership.
- Native box insert/delete shifting uses native C record size and count.
- The Game Boy species-list terminator is now generated only at serialization
  time instead of driving native box traversal.

Temporary migration code and exact removal conditions
------------------------------------------------------
- `NativeBox.legacySpecies` preserves the unused tail of Crystal's redundant
  SRAM species list for byte-exact round trips. Remove it when the Crystal SRAM
  serializer is retired, or when legacy box serialization is rewritten to emit
  canonical unused bytes without requiring exact tail preservation.
- `ConvertNativeBoxMonToLegacy` remains at PC withdrawal and WRAM temporary UI,
  printer, and lottery boundaries. Remove each bridge when the player party and
  corresponding UI/display state are native semantic structures.
- `ConvertBoxMonToNative` remains at packed player-party and WRAM PC-buffer
  deposit boundaries. Remove it when player-party ownership and the move-PC
  temporary buffer are native.
- Crystal box SRAM remains authoritative between accesses. `NativeBox` is the
  decoded working representation, not yet a persistent native box database.
  Remove that limitation only with the later versioned native save/PC storage
  phase; do not add a mirrored sidecar.

Compatibility and save-format implications
------------------------------------------
- The external Crystal SRAM box byte layout is intentionally unchanged.
- Deserialization expands legacy byte IDs and 24-bit experience into native
  fields. Serialization uses checked native field conversion and aborts rather
  than truncating an unrepresentable species, item, move, or experience value.
- Valid current Crystal box data should serialize to the same active species
  list and mon payload. Unused species-list tail bytes are retained by the
  transitional cache.
- Native IDs above the Crystal format range still cannot be persisted in the
  current save format. No high-bit table, sidecar, alias, clamp, or fake mapping
  was introduced.
- Link, Mobile Adapter, Time Capsule, and packed player-party formats were not
  changed in this pass.

Partially completed work
------------------------
- PC-box behavior is native only while decoded in C. Persistent ownership still
  resides in Crystal SRAM until the native save phase.
- Bills PC and printer logic can read native decoded mons, but several shared
  WRAM UI fields remain byte-sized and therefore require checked projection.
- The native Pokemon runtime migration now covers Day-Care, contest generation,
  and decoded boxes; it does not yet cover the authoritative player party or
  battle records.

Untouched remaining work from the original large prompt
-------------------------------------------------------
- Native player-party ownership, native opponent party, and native battle-mon
  staging/synchronization.
- Native temporary Pokemon/UI state sufficient to remove remaining box-to-WRAM
  projections.
- Native base-stat/species content record conversion.
- Remaining broad SpeciesId/DexId/MoveId/ItemId propagation through scripts,
  trainers, battles, items, Pokédex, and content APIs.
- Final script operand, trainer, link/mobile, and save boundary cleanup.
- Versioned native save system and persistent native PC storage remain later
  roadmap phases and were not started here.

Static/source inspection performed
----------------------------------
- Traced every `Serialize_Box`/`Deserialize_Box` caller and every direct
  `NativeBox` mon/species consumer after the type migration.
- Audited box insert/delete shifts, PC UI temporary copies, printer traversal,
  caught-data writes, ownership searches, and lucky-number traversal for packed
  stride assumptions and implicit native-ID narrowing.
- Verified remaining `BoxMon` references are party, WRAM temporary, link, mobile,
  or explicit conversion records rather than decoded PC-box entries.
- Reviewed the complete source diff and checked it for whitespace errors.
- Confirmed no build output or generated artifact was created and the pre-existing
  `lib/libmobile` state was not modified.

Build commands executed
-----------------------
- None.

Test commands executed
----------------------
- None.

Build/test results
------------------
Build/runtime validation: NOT PERFORMED — per user instruction.

Suspected source-level risks / unresolved validation
----------------------------------------------------
- This pass intentionally changes decoded structure names, offsets, alignment,
  and record stride. Compiler validation is still required by the user before
  treating the pass as build-confirmed.
- The existing serializer self-test was inspected but not executed. Its exact
  SRAM round-trip behavior remains runtime-unverified.
- Current PC UI and printer state cannot display future species/items above the
  Crystal byte range; those boundaries reject the value instead of truncating it.
- Native PP lookup still assumes a `MoveId` indexes the currently loaded move
  database. Expanded database/count/resource work remains required.

Exact stopping point and next recommended task
----------------------------------------------
- Stopped with decoded PC boxes coherently migrated to `NativeBox` and all known
  callers adapted, while Crystal SRAM remains the explicit persistence boundary.
- Next, migrate the authoritative player party as one ownership-complete slice:
  party species and party mons must move together to `NativePartyMon`, followed
  by creation, menu, held-item/move, HP/status/stat, battle staging, and save
  boundary callers. Do not add a parallel party sidecar.

Overall Phase 1 status
----------------------
- PARTIALLY COMPLETED. This session completed the decoded PC-box slice, but the
  original large Phase 1 prompt remains unfinished as itemized above.

Known source-level risks / validation notes
-------------------------------------------
- The native generator and stat-path refactor are source-audited but not
  compiler- or runtime-validated.
- Current native generation still queries the present `NUM_POKEMON` and
  `NUM_ATTACKS` content databases. Wider IDs are preserved by the model, but
  actual expanded records require the later native content-database expansion.
- Player party and opponent party remain packed authoritative owners, so this
  session removes the generation bridge for native owners but does not complete
  the full player-party migration.

Exact stopping point and next recommended task
----------------------------------------------
- Native Contest Pokemon generation is complete and no longer round-trips
  through `PartyMon`. The Day-Care and pending egg remain native from the prior
  session. Player/opponent party ownership remains legacy.
- Next migrate `PokemonData.partySpecies` and `PokemonData.partyMon` together to
  `SpeciesId`/`NativePartyMon`. Begin with party creation, switching/removal,
  search/menu/field-item consumers, then stage checked packed copies only at the
  still-legacy battle, link/mobile, PC-box, and temporary-WRAM boundaries. Do not
  introduce a mirrored party sidecar.


2026-08-28 - Phase 1 native Day-Care and egg ownership
================================================================================

Task worked on
--------------
Moved the persistent Day-Care parents and pending egg out of packed Crystal
`BoxMon` ownership and into the existing native Pokemon model. Breeding logic,
level growth, egg move inheritance, experience, gender queries, and overworld
Day-Care icon lookup now operate on semantic native IDs through this slice.

Starting repository state
-------------------------
- Continued from the Native Pokemon A1 working tree at `a7f13824` plus the
  uncommitted A1 source changes.
- `lib/libmobile` was already dirty and remains an untouched pre-existing local
  change.
- Historical changelog wording and build-directory labels were normalized to
  project-only terminology.

Files modified
--------------
- `LOCAL_CHANGELOG.txt`: normalized historical wording and appended this
  migration record.
- `constants/types.h`: changed `breedMon1`, `breedMon2`, and `eggMon` in
  `PokemonData` from packed `BoxMon` records to `NativeBoxMon` records.
- `util/serialize.c`: changed the temporary Crystal-save projection for both
  Day-Care parents and the pending egg to use the checked native BoxMon
  serializer.
- `engine/pokemon/experience.c`, `engine/pokemon/experience.h`: added a native
  species/experience level calculation path and made the packed PartyMon helper
  delegate to it.
- `engine/pokemon/breedmon_level_growth.c`: calculates deposited Pokemon level
  growth directly from native species and 32-bit experience without staging a
  packed temporary Pokemon.
- `engine/pokemon/mon_stats.c`, `engine/pokemon/mon_stats.h`: added a semantic
  species + DV gender query; the existing mon-type wrapper now delegates to the
  same implementation.
- `engine/pokemon/evolve.c`, `engine/pokemon/evolve.h`: added native move-fill
  support and a native pre-evolution lookup that returns semantic `SpeciesId`.
- `engine/pokemon/move_mon.c`, `engine/pokemon/move_mon.h`: added native PP fill,
  converted Day-Care deposit into native ownership, and centralized checked
  conversion back to the still-packed player party on withdrawal.
- `engine/pokemon/breeding.c`: migrated inherited parent/egg move pointers to
  `MoveId`, removed WRAM species/DV staging from breeding gender checks, and
  uses native egg species for TM/HM inheritance checks.
- `engine/events/daycare.c`: rewrote pending-egg creation around `NativeBoxMon`,
  native pre-evolution lookup, native move/PP fill, direct 32-bit experience,
  and checked conversion only when the egg enters the still-legacy party.
- `engine/events/happiness_egg.c`: Day-Care step experience now increments the
  native 32-bit experience value directly with the existing Day-Care cap.
- `engine/pokemon/tempmon.c`: the remaining TEMPMON bridge explicitly converts
  native Day-Care state into the temporary packed WRAM record instead of
  treating native storage as byte-compatible.
- `engine/overworld/overworld.c`, `engine/gfx/mon_icons.c`,
  `engine/gfx/mon_icons.h`: Day-Care overworld icon lookup now carries
  `SpeciesId` without an intermediate byte truncation.

Architectural changes actually made
-----------------------------------
- The two deposited Day-Care Pokemon and the generated pending egg now have one
  authoritative native representation. They are no longer live packed Crystal
  `BoxMon` records.
- Day-Care species, held item, and move identity can now remain `SpeciesId`,
  `ItemId`, and `MoveId` while deposited and throughout breeding/egg creation.
- Day-Care experience is a normal native `uint32_t`; routine Day-Care stepping
  no longer edits a three-byte Crystal experience array.
- Breeding gender calculation can operate from `SpeciesId` + DVs directly and
  no longer requires copying a deposited Pokemon through `wCurPartySpecies` and
  `wTempMon` simply to determine gender.
- Egg move generation and inheritance use native `MoveId` arrays. Conversion to
  byte-sized move IDs occurs only when the egg is eventually projected into the
  current packed player-party record.
- Pre-evolution lookup now has a native `SpeciesId` API and no longer needs a
  byte-sized species round trip for egg species selection.

Behavior corrections made while migrating the slice
----------------------------------------------------
- Native pre-evolution lookup returns the actual one-based candidate species;
  the old translated helper returned the zero-based table index.
- The second Ditto branch in inherited-move selection now checks parent 2, which
  matches the original branch comments and makes that path reachable.
- Ditto DV inheritance now reads the deposited Pokemon's DVs rather than using
  the Ditto species ID as though it were a DV word.
- Day-Care withdrawal writes calculated experience to the legacy party record in
  the same high/middle/low byte order used by the rest of the Pokemon runtime.
- Party terminator updates used by Day-Care withdrawal and egg receipt no longer
  rely on writing one element past the end of `partySpecies`; the separate
  `partyEnd` byte is used for the sixth slot.

Temporary migration bridges and exact removal conditions
--------------------------------------------------------
- Player party Pokemon are still packed `PartyMon`/`BoxMon` records. Depositing
  a Pokemon converts that packed record into `NativeBoxMon`; withdrawing a
  Day-Care Pokemon or receiving an egg performs a checked native-to-legacy
  conversion. Remove both directions when player-party ownership becomes native.
- `SendGetMonIntoFromBox` retains historical Day-Care parameter branches even
  though current callers use the dedicated Day-Care routines. Those branches
  now use explicit conversion rather than raw structure copies. Remove or fold
  them when the PC/party transfer API is replaced during native party/box work.
- `CopyMonToTempMon` still has a TEMPMON compatibility path for code that expects
  the packed WRAM temporary Pokemon. It now converts from native Day-Care data
  explicitly. Remove it when remaining temporary-mon consumers use native
  Pokemon/query APIs.
- `PokemonData` serialization still projects the native Day-Care parents and egg
  into Crystal's 32-byte BoxMon save layout. This is temporary persistence only;
  remove it with the Crystal SRAM serializer when versioned `save.dat` becomes
  authoritative.
- Current Pokemon/move/icon databases are still bounded by the present Crystal
  content counts. The widened IDs are preserved through this slice, but content
  beyond the loaded tables still requires the later native content-database
  expansion.

Save/compatibility implications
-------------------------------
- The in-memory `PokemonData` layout changes because three more live Pokemon
  records are now native and unpacked.
- The temporary serializer continues to emit and read the same Crystal-width
  Day-Care/egg save payload by using explicit native field serialization. No
  raw native struct bytes are written to the old save image.
- Native IDs or experience values that cannot fit the temporary Crystal save
  projection are rejected by the existing checked serializer rather than
  truncated.
- Crystal SRAM compatibility remains transitional and is not a final project
  architecture requirement.

Static source audit performed
-----------------------------
- Re-read all repository references to `breedMon1`, `breedMon2`, and `eggMon`
  after changing their owner type.
- Checked for remaining raw `BoxMon*`, `move_t*`, raw-copy, and direct byte-ID
  uses against the newly native Day-Care records.
- Verified declarations and call sites for `CalcLevelForSpeciesExp`,
  `GetGenderForSpeciesDVs`, `FillNativeMoves`, `FillNativePP`, and
  `GetPreEvolutionNative`.
- Checked the temporary serializer descriptors for all three changed PokemonData
  fields; they use the same native-to-Crystal BoxMon projection already used by
  the A1 native contest record.
- Inspected the source diff and ran `git diff --check`; no whitespace/error
  report remained.
- Confirmed `lib/libmobile` was not modified.

Build commands executed
-----------------------
- None.

Test commands executed
----------------------
- None. No game executable, compiler, CMake configure, Make/Ninja, CTest, or
  runtime smoke test was invoked.

Build/test results
------------------
Build/runtime validation: NOT PERFORMED — source-only migration pass.

Known source-level risks / validation notes
-------------------------------------------
- `PokemonData` has intentionally changed native struct offsets again. The
  serializer is field/offset driven, but compiler validation is still needed
  before treating this source pass as build-confirmed.
- The player party remains legacy-shaped, so expanded Day-Care Pokemon cannot
  yet be withdrawn into the party. The conversion fails cleanly instead of
  truncating the ID.
- The temporary SRAM serializer still caps native Pokemon experience at the old
  24-bit BoxMon field. Native `save.dat` will remove that persistence ceiling.
- Native egg move/PP helpers use the currently loaded move database. Expanded
  moves require the later content-database count/resource work.

Next recommended native-conversion task
---------------------------------------
1. Migrate the authoritative player party as one ownership-complete slice:
   `partySpecies` and `partyMon` must move together to semantic native state.
2. Convert party creation, normal move/held-item access, status/HP/stat updates,
   party menus, and battle staging to the native record rather than adding a
   mirrored party sidecar.
3. Keep checked packed conversion only at still-unmigrated box/link/mobile/WRAM
   boundaries during that transition.
4. Migrate PC box ownership after the player party is coherent, then move
   directly into the versioned PC-only `save.dat` persistence phase.


Session: 2026-08-29 — Reported Windows compile-error repair
===========================================================

Task worked on
--------------
- Repaired the two source failures reported by the user's first Windows Release
  build attempt.

Files modified
--------------
- `constants/types.h`: removed obsolete packed layout from native `Roamer`
  runtime state. Its Crystal save representation already uses explicit field
  serialization and does not require packed in-memory layout.
- `engine/battle/core.c`: replaced byte-offset casts for roamer DVs with normal
  aligned field pointers; widened roamer species pointers are now valid because
  `Roamer` has native alignment.
- `engine/battle/move_effects/beat_up.c`: updated four Beat Up base-stat reads to
  the current `BaseData.attack` and `BaseData.defense` field names.
- `LOCAL_CHANGELOG.txt`: documented the reported errors and source repair.

Files created
-------------
- None.

Files deleted
-------------
- None.

Architectural and compatibility implications
--------------------------------------------
- `Roamer` is native runtime state containing a widened `SpeciesId`; retaining
  a packed Game Boy-style C layout made addresses of its 16-bit fields formally
  unaligned. Normal alignment removes that obsolete memory-layout dependency
  instead of suppressing `-Waddress-of-packed-member`.
- `Serialize_Roamer` and `Deserialize_Roamer` remain the explicit Crystal-format
  boundary, so the serialized field order/width is unchanged by removing the
  runtime packing attribute.
- No temporary migration code or compiler-warning suppression was introduced.
- Beat Up behavior is unchanged; the code now names the existing semantic base
  Attack and Defense fields correctly.

Build/runtime validation
------------------------
- User-reported build: FAILED before this repair in `engine/battle/core.c` on
  packed-member address warnings promoted to errors, and in Beat Up on obsolete
  `BaseData.atk`/`BaseData.def` member names.
- Build/runtime validation: NOT PERFORMED.

Suspected source-level risks and stopping point
-----------------------------------------------
- The reported errors are source-resolved, but the user's build must continue
  to reveal whether later translation units contain additional integration
  errors that were previously hidden by Make's parallel early failure.
- The native-conversion stopping point otherwise remains unchanged.

Next recommended action
-----------------------
- Run `build_windows_release.bat` again and report the next compiler/linker
  output, if any. The script performs a clean build, so the repaired translation
  units will be rebuilt.


Session: 2026-08-29 — Second reported Windows compile-error repair
==================================================================

Task worked on
--------------
- Repaired the three `engine/pokemon/move_mon.c` failures exposed by the user's
  next clean Windows Release build attempt.

Files modified
--------------
- `engine/pokemon/move_mon.c`: used the current semantic `BaseData.HP` member,
  included `<stdlib.h>` for the checked Day-Care conversion's `abort`, and
  copied packed legacy stat-experience values into aligned local storage before
  passing them to the stat calculator.
- `LOCAL_CHANGELOG.txt`: documented the reported errors and source repair.

Files created
-------------
- None.

Files deleted
-------------
- None.

Architectural and compatibility implications
--------------------------------------------
- No compiler warning was suppressed. `PartyMon` remains a transitional packed
  Crystal-format record, so its stat-experience array is explicitly copied by
  value into naturally aligned storage at the calculation boundary.
- The existing big-endian conversion inside `CalcMonStats` remains authoritative;
  copying the encoded 16-bit values preserves existing stat behavior.
- The HP base-stat rename and standard-library declaration are source-integration
  corrections only and do not alter save or runtime formats.
- No temporary migration structure or new compatibility layer was introduced.

Build/runtime validation
------------------------
- User-reported build: FAILED before this repair on obsolete `BaseData.hp`, a
  missing `abort` declaration, and an address-of-packed-member warning promoted
  to an error.
- Build/runtime validation: NOT PERFORMED.

Suspected source-level risks and stopping point
-----------------------------------------------
- These reported errors are source-resolved, but later translation units or the
  final link may expose additional failures when the user's clean build proceeds.
- The native-conversion stopping point otherwise remains unchanged.

Next recommended action
-----------------------
- Run `build_windows_release.bat` again and report the next compiler/linker
  output, if any.


Session: 2026-08-29 — NativeBox serializer-registry repair
==========================================================

Task worked on
--------------
- Repaired the serializer metadata failure exposed by the user's next Windows
  Release build without reverting native decoded PC-box ownership.

Files modified
--------------
- `util/serialize.c`: changed the `STRUC_BOX` registry entry's runtime size from
  removed `struct Box` to `struct NativeBox`, while deliberately retaining the
  existing `STRUC_BOX` format ID and `Struc_Box` serialization field table.
- `LOCAL_CHANGELOG.txt`: documented the reported error and non-regressive fix.

Files created
-------------
- None.

Files deleted
-------------
- None.

Architectural and compatibility implications
--------------------------------------------
- Native decoded `NativeBox` remains the runtime PC-box owner. No packed legacy
  box structure, mirror, fallback, or compatibility wrapper was reintroduced.
- `STRUC_BOX` remains the stable internal serializer identifier and `Struc_Box`
  remains the explicit Crystal SRAM projection. Only the in-memory structure
  size used for serializer bounds/offset validation was stale.
- Serialized box field order and widths are unchanged: count, checked legacy
  species-list projection, native BoxMon field projection, OT names, and
  nicknames still use the existing descriptor.
- No save-format change or temporary migration code was introduced.

Build/runtime validation
------------------------
- User-reported build: FAILED before this repair because the serializer registry
  still evaluated `sizeof(struct Box)` after that runtime type was removed.
- Build/runtime validation: NOT PERFORMED.

Regression review and stopping point
------------------------------------
- Repository references confirm all live box serializer APIs and consumers use
  `struct NativeBox`; the stale registry size expression was the remaining
  `struct Box` reference in the audited serializer path.
- This fix advances the intended native ownership migration and does not undo
  earlier PC-box work.
- Later translation units or final linking may expose additional build failures
  when the user's clean build proceeds.

Next recommended action
-----------------------
- Run `build_windows_release.bat` again and report the next compiler/linker
  output, if any.


Session: 2026-08-29 — Windows Release runtime packaging
=======================================================

Task worked on
--------------
- Diagnosed the user's successful executable immediately exiting when launched
  from the generated `Release` directory and corrected the release packaging.

Files modified
--------------
- `build_windows_release.bat`: after a successful link, now packages the
  runtime-only assets required by the executable into matching subdirectories
  under `Release`.
- `LOCAL_CHANGELOG.txt`: documented the runtime diagnosis and packaging fix.

Files created
-------------
- None in source control. Running the build script populates generated runtime
  asset directories under `Release`.

Files deleted
-------------
- None.

Diagnosis and exact purpose
---------------------------
- The successful build had produced only `Release/suiCune.exe`. Startup calls
  `JSONLoadTables` and later native loaders using relative paths such as
  `data/items/marts.json`, `data/pokemon/base_stats.json`, audio binaries, map
  blocks, and graphics assets.
- Launching the executable directly made `Release` its working directory. With
  none of those assets present, JSON parsing reached invalid/missing input and
  the window terminated during startup.
- The batch file now copies only runtime formats from `audio`, `data`, `gfx`,
  `maps`, and `mobile`, preserving their relative directory layout. It does not
  copy C sources, headers, object files, or repository metadata.
- Default `bindings.json` and `server.json` are copied only when absent, so a
  user's Release-local settings are not overwritten by later rebuilds.
- Asset copies update changed packaged assets but do not delete saves or other
  user-created files already present in `Release`.

Architectural and regression implications
------------------------------------------
- No game logic, native data type, PC-box ownership, serializer behavior, save
  format, or earlier native-conversion source change was reverted or bypassed.
- This is a distribution/build-layout correction: the current native loaders
  continue to use their existing paths, now backed by a complete standalone
  Release directory.
- No hardcoded gameplay fallback, missing-asset suppression, or legacy emulator
  path was added.

Build/runtime validation
------------------------
- User-reported build: SUCCEEDED before this packaging correction.
- User-reported runtime: window opened and immediately closed from the
  executable-only Release directory.
- Build/runtime validation: NOT PERFORMED.

Suspected source-level risks and stopping point
-----------------------------------------------
- The missing startup assets are source-confirmed from the Release contents and
  loader paths. Runtime confirmation still requires the user to rebuild so the
  packaging step runs.
- If a packaged build still exits, the next step is to capture its runtime
  stderr/log output rather than infer another cause.
- The native-conversion stopping point otherwise remains unchanged.

Next recommended action
-----------------------
- Run `build_windows_release.bat` again. Confirm that `Release` contains
  `audio`, `data`, `gfx`, `maps`, and `mobile` before launching
  `Release/suiCune.exe`.


Session: 2026-08-29 — Native bag scrolling and reordering repair
================================================================

Task worked on
--------------
- Investigated and repaired the runtime bag corruption reported after the first
  successful packaged Windows build.
- Kept the repair within the active Phase 1 widened-item migration. No unrelated
  subsystem or previous-project behavior was imported.

User-reported validation before this repair
-------------------------------------------
- Intro, mobile boy/girl/address/age setup, character naming, saving, Mom's
  introduction, time/day selection, and receiving Elm's starter all worked.
- Opening bag pockets displayed random/corrupted item names. Receiving the
  assistant's Potion made the displayed corruption worse.
- The console repeatedly reported empty item names being truncated by
  `Utf8ToCrystalBuffer`. Source inspection established that these warnings were
  downstream symptoms of invalid item IDs read by the menu, not the text
  converter causing the corruption.

Files modified
--------------
- `constants/menu_constants.h`: added explicit scrolling-menu formats for native
  ItemId-only and ItemId-plus-quantity pocket entries.
- `engine/menus/scrolling_menu.h`: exposed the semantic ItemId selected by a
  scrolling item menu.
- `engine/menus/scrolling_menu.c`: added aligned typed traversal for native
  pockets, retained legacy byte traversal for byte-backed menus, carried the
  selected item as ItemId for display callbacks, and added a checked boundary
  before the remaining byte-sized item action state.
- `engine/menus/menu_2.c`: item names and tossability display now consume the
  semantic scrolling-menu ItemId for native pockets while retaining legacy
  behavior for legacy menu lists and direct callers.
- `engine/items/update_item_description.c`: descriptions now receive the native
  selected ItemId directly instead of routing it through byte-sized
  `wMenuSelection`/`wCurSpecies` state.
- `engine/items/print_item_description.h`: widened the item-description API to
  ItemId.
- `engine/items/print_item_description.c`: widened the item-description API
  implementation to ItemId; the existing original-content description tables
  remain authoritative.
- `engine/items/switch_items.c`: added native pocket reordering and stack merging
  using typed entries and overlap-safe native memory moves. Native entries no
  longer pass through the two-byte Game Boy switch buffer.
- `engine/items/pack.c`: marked the live bag, PC/mart bag, key-item, and ball
  pocket menus as native-backed. Catching-tutorial byte arrays remain on the
  legacy formats.
- `engine/events/pokecenter_pc.c`: marked PC item storage as a native
  quantity-pocket menu.
- `LOCAL_CHANGELOG.txt`: recorded diagnosis, implementation boundaries, risks,
  validation state, and the next migration point.

Files created
-------------
- None.

Files deleted
-------------
- None.

Architectural changes completed
-------------------------------
- The live bag UI no longer assumes Crystal's `[count][item byte][quantity
  byte]` layout for native `PlayerData` pockets. It indexes
  `item_pocket_s`/`item_quantity_pocket_s` semantically, including the compiler's
  required alignment and the widened ItemId stride.
- Item names, tossability display, and descriptions no longer obtain native
  pocket IDs from the truncated byte menu-selection field.
- Reordering live native pockets no longer copies widened entries through the
  legacy two-byte WRAM switch buffer or searches for a one-byte `$ff`
  terminator.
- Legacy scrolling formats remain available only for callers whose lists are
  still genuinely byte-oriented. This prevents regressing the catching tutorial
  and other non-item scrolling lists while giving native pockets an explicit
  representation.

Obsolete dependency removed
---------------------------
- Removed the live pack/PC item's dependence on Game Boy item-record stride,
  byte terminators, and the two-byte Game Boy item-switch buffer for traversal
  and reordering.

Legacy/transitional work still remaining
-----------------------------------------
- Selecting an item for an action still ultimately enters byte-sized
  `wCurItem`. The new boundary performs a checked conversion; IDs outside the
  representable legacy action range are refused and logged instead of silently
  truncated. Original Crystal-range items, including Potion, continue through
  the existing action code.
- Exact removal condition: remove this checked bridge after every item action
  consumer currently reading `wCurItem` has migrated to an ItemId field in
  native UI/gameplay state. At that point native selections must be passed
  directly and the WRAM field must cease to be authoritative for item actions.
- Item-name, description, TM/HM, and item-effect content tables still describe
  original byte-range content. The widened menu API does not claim those Phase
  14 databases are converted.
- Catching-tutorial item arrays remain deliberate legacy byte-backed data and
  continue using the legacy scrolling formats. They should migrate only when
  their owning tutorial state is converted, not by casting them as native
  pockets.

Compatibility and save-format implications
------------------------------------------
- No save fields, serializer descriptors, or on-disk widths changed in this
  repair. Existing explicit ItemId-to-Crystal serialization boundaries remain.
- The fix preserves native in-memory ItemId pockets and does not repack entries,
  narrow ItemId storage, reintroduce a legacy mirror, or suppress invalid-name
  warnings.
- No temporary duplicated pocket state or fallback implementation was added.
  The only transitional code is the explicit checked action bridge documented
  above.

Investigated but not implemented
--------------------------------
- The UTF-8 conversion warning was inspected but intentionally not hidden or
  changed because it correctly exposed invalid empty item names.
- A repository-wide item-action migration was not started. It is larger than
  the reported display/storage corruption and should be performed as the next
  bounded Phase 1 task.

Suspected source-level risks
----------------------------
- Runtime validation is still required for opening each pocket, receiving the
  Potion, saving/reloading, moving entries with Select, and PC item deposit/
  withdrawal.
- Items above the legacy action range can be displayed as native selections but
  cannot yet be used/tossed/given/registered; the checked boundary rejects them.
- The original item content tables do not yet provide valid names/descriptions/
  effects for IDs above their current defined range.

Build and test commands executed
--------------------------------
- None.

Build/runtime validation
------------------------
- NOT PERFORMED — per user instruction.

Exact stopping point and next recommended task
----------------------------------------------
- Stopped with the live bag and PC pocket traversal/display/reordering migrated
  to native entry widths and the old action handoff explicitly checked.
- Next recommended native-conversion task: introduce native item-action UI state
  and migrate all `wCurItem` item consumers as one bounded dependency graph,
  then remove the checked ItemId-to-LegacyItemId selection bridge.
- Remaining Phase 1 and later roadmap work is not complete. In particular, the
  repository still has legacy content IDs and byte-oriented runtime paths beyond
  the bag UI, and expanded item content databases/effects remain untouched.


Session: 2026-08-29 — PC dispatch, mart cancel, and native mart ItemId list
============================================================================

Task worked on
--------------
- Repaired the two menu defects found during the user's extended runtime test.
- Continued the Phase 1 ItemId conversion by removing the mart's remaining
  byte-only item-list projection and widening common item wrapper/name APIs.

User-reported validation before this session
--------------------------------------------
- The prior bag repair worked sufficiently to continue through Mr. Pokemon's
  Mystery Egg, Professor Oak's Pokedex, the Pokedex UI, the Cherrygrove Map Card,
  and the map UI without another reported defect.
- PC access worked except that selecting `MOVE PKMN W/O MAIL` behaved like the
  cancel/exit entry.
- Mart item descriptions worked for purchasable rows, but the Cancel row showed
  a single question mark.
- The user corrected the previous session's `log_error` spelling to the
  repository's actual `log_err` macro. That correction was present at session
  start and was preserved.

Files modified
--------------
- `engine/pokemon/bills_pc_top.c`: restored the live dispatch from the fourth
  Bill's PC menu entry to `BillsPC_MovePKMNMenu`; it had remained commented out
  and fallen through to `BillsPC_SeeYa`.
- `engine/items/update_item_description.c`: explicitly converts the legacy
  scrolling-menu `$ff` cancel marker to native `ITEM_LIST_END` before deciding
  whether to print a description.
- `engine/items/mart.c`: replaced the byte-only projected mart menu records with
  an aligned `ItemId` list, removed checked narrowing/abort paths and duplicate
  WRAM count ownership, and changed the buy menu to the native item-list format.
- `home/item.h`: widened common toss/check/receive wrapper declarations to
  `ItemId`.
- `home/item.c`: widened the matching wrapper implementations to `ItemId`.
- `engine/items/pack.h`: widened `Pack_GetItemName` to `ItemId`.
- `engine/items/pack.c`: widened `Pack_GetItemName` implementation to `ItemId`.
- `engine/pokemon/mon_menu.h`: widened `PartyMonItemName` to `ItemId`.
- `engine/pokemon/mon_menu.c`: widened `PartyMonItemName` implementation to
  `ItemId`.
- `LOCAL_CHANGELOG.txt`: documented the runtime evidence, fixes, conversion,
  remaining boundary, and validation state.

Files created
-------------
- None.

Files deleted
-------------
- None.

Completed work and architectural changes
----------------------------------------
- Bill's PC selection index 3 once again invokes the already-existing move-
  without-mail workflow instead of sharing the default exit path.
- Legacy scrolling-menu cancel is now interpreted semantically. Item 255 is no
  longer sent to the item-description path when the mart cursor is on Cancel.
- Mart source data, priced-mart data, the runtime buy list, menu selection, and
  price lookup now retain `ItemId`. The mart no longer converts each entry into
  a `LegacyItemId` array or aborts merely because a source ID exceeds one byte.
- The mart list has one authoritative native count. `wCurMartCount` is no longer
  populated or read by the C mart runtime.
- The native mart list's layout is asserted against `item_pocket_s`, making the
  menu representation explicit instead of depending on an undocumented cast.
- Common bag item receipt/removal/check wrappers and the shared pack/Pokemon item
  naming APIs no longer truncate callers at their function boundary.

Obsolete Game Boy dependency removed
------------------------------------
- Removed the buy list's dependence on Crystal's byte item records,
  `LEGACY_ITEM_LIST_END`, and byte-sized `wCurMartCount` runtime ownership.
- Removed two mart-specific ItemId-to-LegacyItemId conversion-and-abort paths.

Partially completed and transitional work
-----------------------------------------
- Mart list storage and display are native-width, but selecting any native menu
  item for an action still reaches the checked `wCurItem` bridge in scrolling
  menu code. IDs in the original action range work; expanded IDs are refused
  rather than truncated.
- Exact removal condition: introduce authoritative native item-action state,
  migrate every live `wCurItem` read/write and all legacy-record projections at
  their real boundaries, then pass the native selection into buy/use/toss/give/
  register/effect paths directly and delete the scrolling-menu bridge.
- `PartyMonItemName` now accepts ItemId, but the current original-content name
  database remains byte-range and is not claimed as a Phase 14 conversion.
- The move-without-mail workflow still uses temporary legacy Pokemon UI records
  at documented conversion boundaries even though persistent PC box ownership
  is native. This session restored dispatch only; it did not claim that full UI
  workflow is natively represented.

Untouched remaining work
------------------------
- The repository-wide `wCurItem` dependency graph, native item-effect dispatch,
  expanded item content tables, and native TM/HM action state remain untouched.
- Full native party ownership and remaining species/move legacy runtime fields
  from the broader Phase 1 prompt remain unfinished.
- Later roadmap phases remain untouched.

Compatibility and save-format implications
------------------------------------------
- No save structure, serializer descriptor, or on-disk format changed.
- Original mart order, prices, quantities, cancel behavior, and PC save prompt
  behavior are intended to remain unchanged.
- No duplicated mart mirror or fallback path was introduced.

Suspected source-level risks
----------------------------
- The restored move-without-mail entry now exposes a complex workflow that was
  unreachable from this menu; the user should exercise selecting a Pokemon,
  viewing Stats, moving between boxes, cancelling, and saving.
- Mart buy-menu Cancel and purchases require runtime confirmation. Expanded
  ItemIds still cannot complete purchases until native item-action state is
  authoritative.

Build and test commands executed
--------------------------------
- None.

Build/runtime validation
------------------------
- NOT PERFORMED — per user instruction.

Exact stopping point and next recommended task
----------------------------------------------
- Stopped after restoring the PC dispatch, fixing semantic mart cancellation,
  converting mart list ownership to ItemId, and widening the common item wrapper
  and naming API boundaries.
- Next recommended task: migrate the complete live `wCurItem` dependency graph
  to authoritative native item-action state as one bounded Phase 1 conversion,
  with explicit checked projections only where packed Crystal records still
  require them.
- Phase 1 and the original long-term conversion request remain partially
  complete.


Session: 2026-08-30 — Native current-item action state
=======================================================

Task worked on
--------------
- Per the previous handoff's recommended next task, migrated live C runtime
  ownership of the current item from byte-sized emulated WRAM `wCurItem` to a
  native ItemId action-state field.
- Reviewed the existing changelog and worktree before modifying source.
- Preserved the pre-existing dirty `lib/libmobile` submodule without touching it.

Files modified
--------------
- `constants/types.h`: added non-serialized `NativeUIState` containing the
  authoritative ItemId `currentItem`.
- `constants.h`: declared the native UI state global for C runtime consumers.
- `wram.c`: defined the native UI state global outside the emulated WRAM object.
- `engine/menus/intro_menu.c`: resets native transient UI state on new-game WRAM
  reset without altering save data.
- `engine/menus/scrolling_menu.c`: native bag/mart/PC menu selection now writes
  the full ItemId directly; removed the former narrowing/logging action bridge.
- `engine/items/pack.c`, `engine/items/mart.c`, `engine/items/buy_sell_toss.c`,
  `engine/items/items.c`, `engine/items/tmhm.c`, and `engine/items/item_effects.c`:
  migrated item selection, item menu/context lookup, buy/sell/toss, TM/HM, and
  item-effect consumers to native current-item state.
- `engine/items/tmhm.h` and `engine/items/item_effects.h`: widened current-item
  API boundaries to ItemId.
- `home/item.c`: dispatches item effects from native current-item state.
- `engine/overworld/scripting.c` and `engine/overworld/select_menu.c`: scripts
  and registered-item selection now retain ItemId instead of refusing IDs above
  one byte.
- `engine/events/kurt.c`, `engine/events/misc_scripts.c`,
  `engine/events/odd_egg.c`, `engine/events/pokecenter_pc.c`,
  `engine/events/specials.c`, and `engine/events/battle_tower/battle_tower.c`:
  migrated their current-item reads/writes to native state; Kurt explicitly
  checks the Apricorn quantity-menu legacy boundary.
- `engine/battle/core.c`, `engine/battle_anims/functions.c`, and
  `engine/battle_anims/functions.h`: migrated battle ball selection/animation
  consumers to ItemId and widened the animation palette API.
- `engine/pokemon/evolve.c`, `engine/pokemon/party_menu.c`,
  `engine/pokemon/mail.c`, `engine/pokemon/mon_menu.c`,
  `engine/pokemon/mon_menu.h`, and `engine/pokemon/move_mon.c`: migrated current
  item use to ItemId and added checked projections at actual legacy held-item,
  mail, party, and box-record write boundaries.
- `home/hm_moves.c` and `home/hm_moves.h`: widened the HM-item predicate to
  ItemId so TM/HM item handling does not narrow native current-item state before
  its semantic range check.
- `engine/menus/debug_field_menu.c` and `engine/menus/menu_2.c`: migrated their
  current-item assignments to native state.
- `LOCAL_CHANGELOG.txt`: recorded the completed migration and remaining legacy
  boundaries.

Files created
-------------
- None.

Files deleted
-------------
- None.

Completed architectural changes
-------------------------------
- `gNativeUI.currentItem` is now the authoritative current item for all live C
  runtime paths audited in this session. It is naturally aligned, ItemId-sized,
  native-only transient state rather than an address in emulated WRAM.
- Native scrolling menus pass the selected ItemId directly into bag, mart, PC,
  registered-item, script, battle, and item-effect logic. The old menu action
  rejection for IDs above the byte range has been removed.
- Mart purchases and script `giveitem`/`checkitem` can now retain an ItemId
  through their runtime action path rather than narrowing before pocket logic.
- All live C `wram->wCurItem` reads and writes were removed. The original WRAM
  byte remains only as part of the fixed emulated WRAM layout and in historical
  comments/mobile-ASM sources; it is no longer C runtime authority.
- Original-range behavior is preserved through semantic ItemId APIs rather than
  an indefinite mirrored byte field.

Legacy/transitional structures and conversions still remaining
--------------------------------------------------------------
- Packed Crystal party, box, mail, PP-restore, and Kurt quantity-menu records
  still contain byte item fields. Their writes now use explicit
  `TryItemIdToLegacy` checks and log/reject unrepresentable values rather than
  silently truncating ItemId.
- Mailbox reads remain byte-oriented because the persisted mailbox record still
  stores an original-format mail-item byte. The return cast in `mail.c` is an
  explicit read of that legacy field, not a projection from native action state.
- Exact removal condition: migrate each owning record to native Pokemon/mail/UI
  state and native serialization, then delete the corresponding checked
  projection. Do not remove those checks before the record itself ceases to be
  byte-oriented.
- The emulated `wCurItem` slot cannot be removed yet because `wram_s` still
  models the fixed Game Boy address map. Its removal belongs to the later native
  memory-model phase after all emulated WRAM layout consumers are retired.
- Existing item effect, item name/description, TM/HM, mail, and held-item
  content tables remain original-content databases. They are not yet expanded
  content systems.

Compatibility and save-format implications
------------------------------------------
- Native current-item state is transient and deliberately excluded from saves.
  No save format, serializer descriptor, or existing-save migration changed.
- Existing byte-held-item and mail serialization remains compatible because
  conversions now occur only at their actual legacy record boundaries.
- No parallel native/legacy item-action owner or fallback behavior was retained.

Investigated but not implemented
--------------------------------
- Mobile ASM source still names the emulated `wCurItem` address. It was not
  altered because this session targeted the live C runtime dependency graph and
  its behavior/assembly build role was not established from source inspection.
- Native Pokemon held-item/mail ownership, native expanded content databases,
  and the remaining native memory-model conversion were not started.

Suspected source-level risks
----------------------------
- The new flow should be runtime-tested across bag use/toss/register, mart buy
  and sell, PC item transfer, a TM/HM, a ball in battle, script-given items, and
  mail/held-item handling.
- An item ID outside legacy range is intentionally rejected when a still-legacy
  held-item, mail, PP-restore, box, or Kurt quantity record must store it.

Build and test commands executed
--------------------------------
- None.

Build/runtime validation
------------------------
- NOT PERFORMED — per user instruction.

Exact stopping point and next recommended task
----------------------------------------------
- Stopped after completing the native current-item ownership migration and
  source-level audit of its legacy record projections.
- Next recommended Phase 1 task: choose one owning legacy Pokemon record path
  (party held items and mail are the most directly exposed) and migrate that
  record plus its serialization to native ItemId ownership, removing its checked
  projection only when every consumer is migrated.
- The original long-term conversion request remains partially complete.


Latest handoff marker: 2026-08-30
---------------------------------
The full 2026-08-30 "Native current-item action state" section above is the
latest session record. Current item selection is native ItemId state in live C
runtime paths; legacy byte record boundaries remain explicitly checked.

Build/runtime validation: NOT PERFORMED — per user instruction.


Session: 2026-08-30 — UTF-8 conversion diagnostic correction
==============================================================

Task worked on
--------------
- Investigated the non-fatal repeated `Utf8ToCrystalBuffer` truncation warnings
  reported during battle stat-lowering text.
- Audited the next recommended native player-party migration slice enough to
  confirm that the packed party record remains used by 369 direct C references;
  no partial parallel party owner was introduced.

Files modified
--------------
- `util/util_string.c`: corrected the UTF-8-to-Crystal buffer truncation test to
  warn only when unconverted source text remains, and made the shared
  zero-length destination case fail explicitly without writing past the buffer.
- `LOCAL_CHANGELOG.txt`: recorded this diagnostic correction and the exact
  player-party migration stopping point.

Files created
-------------
- None.

Files deleted
-------------
- None.

Completed work
--------------
- `Utf8ToCrystalBuffer` no longer underflows its warning expression when an
  empty string is converted. The previous `i - 1` check falsely reported a
  truncation with a size of `0+` even though no source text was lost.
- `Utf8ToCrystal` now uses the same source-remainder rule, so it also avoids
  warning when text exactly fills its destination capacity.
- A zero-sized caller buffer is diagnosed and returned without a destination
  write; valid existing callers retain their normal terminator behavior.

Architectural changes made
--------------------------
- None. This is a shared string-conversion correctness repair, not a native
  data-model migration.

Investigated but not implemented
--------------------------------
- The authoritative player party is still packed `PartyMon` storage in
  `PokemonData`; `NativePartyMon` and explicit conversion functions already
  exist, but the live party has 369 direct C consumers spanning battle, party
  menu, link, save, mail, breeding, and field systems.
- Replacing only the storage declaration would leave endian-sensitive HP/stat
  code, pointer consumers, and packed link/save boundaries incoherent. No
  duplicate sidecar or partial ownership change was made.

Compatibility and save-format implications
------------------------------------------
- None. No save, party, box, or serialization data changed.

Build and test commands executed
--------------------------------
- None.

Build/runtime validation
------------------------
- NOT PERFORMED — per user instruction.

Suspected source-level risks / unresolved validation
----------------------------------------------------
- The reported battle warnings should be gone when their source text fits the
  destination, but runtime validation remains user-owned.
- Actual truncations still emit warnings intentionally.

Exact stopping point and next recommended task
----------------------------------------------
- Stopped after correcting the reported false diagnostic and documenting the
  required player-party dependency scope.
- Next recommended Phase 1 task: migrate the authoritative player party as one
  ownership-complete slice. Convert `PokemonData.partyMon` and its party-species
  metadata together to native records, then migrate battle staging, party UI,
  creation/movement, held items/mail, link projections, and explicit save
  serialization before deleting packed player-party ownership.
- The original long-term conversion request remains partially complete.


Latest handoff marker: 2026-08-30
---------------------------------
The latest session corrected the false UTF-8 truncation diagnostics and left
the native player-party migration intentionally unstarted pending an
ownership-complete implementation plan.

Build/runtime validation: NOT PERFORMED — per user instruction.


Session: 2026-08-30 — Party-species compatibility-cache demotion
==================================================================

Task worked on
--------------
- Continued the Phase 1 player-party preparation rather than starting an
  incomplete parallel `NativePartyMon` owner.
- Moved the remaining normal player-party mutation paths to derive Crystal's
  redundant party-species list from the party records, and made save output do
  the same at its explicit legacy boundary.

Files modified
--------------
- `constants/types.h`: renamed `partySpecies` to `legacyPartySpecies` and
  documented it as a Crystal save/link/mobile compatibility cache; clarified
  the role of `partyEnd`.
- `home/pokemon.c`, `home/pokemon.h`: added
  `RefreshLegacyPartySpeciesCache`, which rebuilds the legacy list from active
  `partyMon[i].mon.species` records and validates the party count.
- `util/serialize.c`: rebuilds the legacy party-species cache in a local save
  projection before serializing `PokemonData`, rejects an invalid party count,
  and does not mutate live state during save output.
- `engine/pokemon/move_mon.c`, `engine/pokemon/move_mon_wo_mail.c`,
  `engine/pokemon/switchpartymons.c`, `engine/pokemon/evolve.c`,
  `engine/pokemon/caught_data.c`, `engine/pokemon/breeding.c`, and
  `engine/events/daycare.c`: removed independent normal-gameplay cache writes
  from party creation, insertion, withdrawal, removal, switching, evolution,
  contest capture, hatching, and Day-Care paths; they refresh the cache only
  after the authoritative party record mutation is complete.
- `engine/menus/debugmenu.c`: derives the debug trade-test legacy list from its
  seeded party record.
- `engine/menus/intro_menu.c`, `engine/events/bug_contest/contest_2.c`,
  `engine/link/link.c`, `engine/link/time_capsule.c`, `mobile/mobile_12_2.c`,
  `mobile/mobile_40.c`, and `mobile/mobile_46.c`: mechanically renamed the
  remaining explicit compatibility-cache users. Their legacy behavior was not
  otherwise changed.
- `LOCAL_CHANGELOG.txt`: recorded this party-cache ownership change.

Files created
-------------
- None.

Files deleted
-------------
- None.

Completed architectural changes
-------------------------------
- `partyMon[i].mon.species` remains the sole normal C gameplay owner of each
  player's species. `legacyPartySpecies` is now named and treated as a derived
  Crystal-format cache, not a general-purpose gameplay list.
- Player-party mutation paths no longer independently shift, write, or choose
  the cached species list. They mutate party records first, then rebuild the
  legacy view once.
- Save serialization constructs its legacy species list from the authoritative
  party records in a temporary projection. A stale runtime cache therefore
  cannot become persistent save authority.

Obsolete Game Boy/emulation dependency removed
-----------------------------------------------
- The normal creation/removal/swap/evolution/egg/Day-Care/contest mutation
  paths no longer depend on Crystal's byte species-list mutation algorithm.
- Crystal's terminator-based list remains only where actual legacy save, link,
  Time Capsule, Mobile Adapter, reset, or Bug Contest compatibility behavior
  still requires the byte view.

Temporary migration code and exact removal condition
------------------------------------------------------
- `legacyPartySpecies`, `partyEnd`, and `RefreshLegacyPartySpeciesCache` are
  transitional compatibility code. Remove them when player-party ownership is
  native and the save/link/mobile/Time Capsule/contest boundaries construct
  explicit legacy payloads directly from native party records.
- The Bug Contest uses the cache temporarily to mask player-party membership.
  It remains unchanged because its exact compatibility behavior has not yet
  been replaced by a native contest-party state model.
- Packed `PartyMon` remains authoritative. This session did not add a native
  sidecar, duplicate owner, or fallback path.

Compatibility and save-format implications
------------------------------------------
- Existing Crystal-format party serialization remains byte-compatible for
  valid data: it writes the active species and terminator derived from the same
  packed party records that gameplay owns.
- No save version, save size, link packet, Mobile Adapter format, or party
  record layout changed.
- If a corrupted runtime party count exceeds `PARTY_LENGTH`, cache refresh logs
  an error and save serialization rejects the data rather than writing an
  invalid legacy image.

Partially completed work
------------------------
- Player species ownership is consolidated at the packed party record level,
  but `PartyMon` itself still uses legacy byte species/item/move fields and
  Crystal-endian stat fields.
- Link, Time Capsule, Mobile Adapter, reset, and Bug Contest still read the
  explicit legacy cache as compatibility consumers. They are not native
  player-party implementations.

Untouched remaining work
------------------------
- Full native `NativePartyMon` ownership, semantic party IDs, native stats/HP,
  menu/battle migration, held-item/mail migration, and native party save
  serialization.
- Native opponent/battle record ownership, versioned native saves, and all
  later roadmap phases remain unfinished.

Build and test commands executed
--------------------------------
- None.

Build/runtime validation
------------------------
- NOT PERFORMED — per user instruction.

Exact stopping point and next recommended task
----------------------------------------------
- Stopped with the legacy party-species list no longer independently mutated
  by normal player-party operations and save output deriving it from party
  records.
- Next recommended Phase 1 task: convert the authoritative player party from
  packed `PartyMon` to `NativePartyMon` in one ownership-complete slice,
  beginning with party creation/removal/switching and save serialization, then
  migrating battle/menu/link projections as explicit boundaries.
- The original long-term conversion request remains partially complete.


Latest handoff marker: 2026-08-30
---------------------------------
The preceding party-species cache-demotion record was superseded by the final
egg-state inspection below; it is retained only as an audit trail of a reverted
edit, not as completed conversion work.

Build/runtime validation: NOT PERFORMED — per user instruction.


Final correction: 2026-08-30 — Legacy party-species semantics preserved
==========================================================================

Completed work
--------------
- Renamed the packed Crystal list field to `legacyPartySpecies` throughout C
  source and serialization metadata. The rename does not alter its layout,
  save bytes, or behavior.
- Corrected the reported false UTF-8 truncation diagnostics in
  `util/util_string.c`.

Investigated and deliberately not implemented
----------------------------------------------
- An attempted derivation of `legacyPartySpecies` from `partyMon` records was
  fully reverted after source inspection established that `giveegg` stores the
  concealed species in the party record but writes `EGG` into the list as the
  hatch-state marker. Treating the list as redundant would regress egg state.
- No native party sidecar, synchronization helper, serializer projection, or
  packed-party layout change remains in the working tree.

Current stopping point and next recommended task
-------------------------------------------------
- The legacy list is now explicitly named but remains behaviorally required.
- Before converting player-party ownership, add a first-class native egg/hatch
  state to the native party model and migrate creation, hatching, save, and
  legacy link/mobile projections together. Do not derive egg state from the
  existing packed species field.

Build/runtime validation: NOT PERFORMED — per user instruction.


Latest handoff marker: 2026-08-30
---------------------------------
The current source-level stopping point is the native SDL input migration.
Assembly source files are not compiled into this executable; future progress
will be measured by removal of live Game Boy runtime dependencies, beginning
with the remaining HRAM input-mirror consumers.

Build/runtime validation: NOT PERFORMED — per user instruction.


Session: 2026-08-30 — Native scripted-input state
===================================================

Task worked on
--------------
- Continued the bounded Phase 7 input conversion after inspecting every
  `wInputType` and auto-input cursor/duration caller, reset path, and WRAM
  layout dependency.

Files modified
--------------
- `home/joypad.c`, `home/joypad.h`: replaced WRAM-backed auto-input control
  fields with one native `AutoInputState`; added an explicit active-state API.
- `constants/wram_constants.h`: removed the obsolete `AUTO_INPUT` WRAM-state
  constant after its final C consumer was migrated.
- `engine/battle/core.c`, `engine/items/pack.c`: migrated tutorial checks to
  the native active-state API.
- `home/init.c`, `engine/menus/intro_menu.c`: reset native scripted input at
  the same game-init/new-game points where the original WRAM state was reset.
- `wram.h`, `wram.c`: retired the named WRAM input-type, pointer, bank, and
  duration fields. A five-byte reserved span remains solely to preserve the
  still-legacy WRAM address map for neighboring fields.
- `LOCAL_CHANGELOG.txt`: recorded this migration.

Files created/deleted
---------------------
- None.

Completed architectural changes
-------------------------------
- Tutorial/scripted input now has native pointer, duration, and active state;
  it no longer depends on named Game Boy WRAM fields or a banked auto-input
  pointer representation.
- All live C reads of `wInputType`, `wAutoInputAddress`, `wAutoInputBank`, and
  `wAutoInputLength` have been removed.

Temporary migration state and exact removal condition
------------------------------------------------------
- `legacyInputRuntime[5]` preserves offsets in the remaining packed WRAM map
  only. Remove the reserved span when the surrounding legacy WRAM layout is
  replaced with native game state rather than offset-validated storage.
- HRAM joypad frame mirrors remain active because existing code also writes
  them directly (for example, save and slot-machine flows). Migrate each
  reader and writer as a coherent group before making native frame deltas
  authoritative.

Compatibility and save-format implications
------------------------------------------
- Auto-input stream bytes, durations, tutorial behavior, and reset behavior
  are preserved. No save, link, or input-binding format changed.

Partially completed work
------------------------
- Native physical input and scripted-input ownership are complete slices;
  menu/text/battle/overworld input consumption still uses legacy mirror bytes.

Untouched remaining work
------------------------
- Native input snapshot consumption, remapping UI, native party ownership with
  explicit egg state, native saves, and later roadmap phases.

Build/test commands and results
-------------------------------
- None. Build/runtime validation: NOT PERFORMED — per user instruction.

Exact stopping point and next recommended task
----------------------------------------------
- Stopped after removing all live C use of the scripted-input WRAM fields.
- Next: migrate one complete group of HRAM joypad mirror consumers and writers
  to a native input-frame API; do not split the mirror owner from its writers.


Latest handoff marker: 2026-08-30
---------------------------------
Native physical SDL input and scripted-input control now have separate native
owners. Legacy HRAM mirrors remain only as compatibility state for their
still-unmigrated consumers and writers.

Build/runtime validation: NOT PERFORMED — per user instruction.


Latest handoff marker: 2026-08-30 — native input-frame ownership
------------------------------------------------------------------
Physical input-frame state (held, pressed, released, and accumulated presses)
now has one native C owner in util/input.c. All live C consumers and writers
of hJoypadDown, hJoypadPressed, hJoypadReleased, and hJoypadSum have migrated
to that API; UpdateJoypad is the sole compatibility-mirror writer.

The remaining HRAM fields are output mirrors for legacy-layout compatibility.
Remove them only after the packed HRAM representation itself is replaced and
no serialized, offset-validated, or non-C consumer requires those offsets.

Build/runtime validation: NOT PERFORMED — per user instruction.


Latest handoff marker: 2026-08-30 — native logical input ownership
---------------------------------------------------------------------
Logical game input now has a native C owner. All direct C writers of the
logical HRAM joypad mirrors were removed, and shared menu/text paths consume
the native API. The current stopping point is the remaining read-only mirror
consumers outside those shared paths.

Build/runtime validation: NOT PERFORMED — per user instruction.


Final update: 2026-08-30 — logical HRAM input retired
=======================================================

Completed work
--------------
- Converted every remaining live C read of `hJoyReleased`, `hJoyPressed`,
  `hJoyDown`, and `hJoyLast` to the existing native logical-input API across
  PC/party, Pokégear, RTC, link, mobile, debug, overworld, UI, and movie code.
- Removed the final logical HRAM mirror writes from `home/joypad.c`.
- Demoted those four packed HRAM slots to `legacyLogicalInputReserved[4]`; the
  four bytes retain only the offset-validated legacy layout.
- Updated `v_StatsScreenDPad` to accept a logical-input value rather than a
  pointer into the retired HRAM field.

Partially completed work
------------------------
- Physical `hJoypad*` compatibility fields and the broader packed HRAM model
  remain. They are not part of this completed logical-input slice.

Untouched remaining work
------------------------
- Native player-party ownership with explicit egg/hatch state, native saves,
  remaining packed-memory retirement, and all subsequent roadmap phases.

Temporary migration state and exact removal condition
------------------------------------------------------
- `legacyLogicalInputReserved[4]` remains only while the packed HRAM layout is
  offset-validated. Remove it when that layout is replaced and no legacy
  tooling/import requirement needs the span.

Build/test commands and results
-------------------------------
- None. Build/runtime validation: NOT PERFORMED — per user instruction.

Exact stopping point and next recommended task
----------------------------------------------
- The logical HRAM input migration is complete. Next, design a native
  player-party owner with explicit egg/hatch state before migrating party
  creation, hatching, storage, and serialization.

Session: 2026-08-30 — Player-party egg-state foundation
=========================================================

Task worked on
--------------
- Began the next Phase 1 player-party ownership slice by separating egg state
  from the concealed species held in the packed Crystal party record.

Files modified
--------------
- `constants/types.h`: added the first-class `isEgg` field to
  `NativePartyMon`.
- `home/pokemon.h`, `home/pokemon.c`: added the semantic
  `PlayerPartyMonIsEgg` query and an explicit player-party-to-native projection
  that carries the list-derived egg state.
- `engine/pokemon/breeding.c`, `engine/pokemon/health.c`,
  `engine/pokemon/party_menu.c`, `engine/pokemon/mon_menu.c`,
  `engine/pokemon/search2.c`, `engine/battle/core.c`,
  `engine/events/battle_tower/rules.c`,
  `engine/events/battle_tower/battle_tower.c`,
  `engine/events/overworld.c`, and `engine/events/happiness_egg.c`: migrated
  player-party egg checks to the semantic query.
- `util/serialize.c`: explicitly clears the non-serialized native egg field
  on legacy Crystal save import for the contest-mon record.
- `LOCAL_CHANGELOG.txt`: recorded this migration.

Files created/deleted
---------------------
- None.

Completed architectural changes
-------------------------------
- `NativePartyMon` now represents egg/hatch state as a real field rather than
  relying on a species sentinel.
- All live C gameplay checks that were treating `PartyMon.mon.species` as the
  player-party egg marker now use the actual current legacy source:
  `legacyPartySpecies[index] == EGG`.
- Egg step progression, hatching eligibility, battle eligibility, party-menu
  behavior, healing, happiness, field-move checks, and Battle Tower rules now
  share one semantic query. This preserves the concealed species required at
  hatch time.

Temporary migration state and exact removal condition
------------------------------------------------------
- `PlayerPartyMonIsEgg` currently reads the packed party-list marker because
  the player party is still legacy-owned. Change its implementation to read
  `NativePartyMon.isEgg`, then remove the legacy marker from normal runtime
  logic once all player-party records, names, mutators, battle/menu users, and
  save projections have migrated to native ownership.
- `ConvertPlayerPartyMonToNative` is a read-only transition boundary, not a
  second authoritative party. Remove it when callers directly use the native
  player-party owner.
- `NativePartyMon.isEgg` is not added to Crystal-format save serialization.
  The only currently serialized native party record is the non-egg contest
  record; its field is cleared after import. Serialize native party egg state
  only as part of the versioned native save conversion.

Compatibility and save-format implications
------------------------------------------
- Existing Crystal saves and their byte layout are unchanged. The legacy party
  list remains the egg marker until native player-party persistence exists.
- No link, mobile, box, party-size, or name format changed.

Partially completed work
------------------------
- The native party model has explicit egg state and an accurate projection
  boundary, but the live player party remains packed `PartyMon` data.

Untouched remaining work
------------------------
- Native player-party ownership, native party names/OT data, mutation and
  battle/menu migration, native party serialization, and all later roadmap
  phases.

Build/test commands and results
-------------------------------
- None. Build/runtime validation: NOT PERFORMED — per user instruction.

Exact stopping point and next recommended task
----------------------------------------------
- Stopped after completing the egg-state semantic boundary and migrating all
  direct player-party egg tests found in C.
- Next: introduce the native player-party owner and migrate the bounded party
  creation/removal/switching operations with explicit legacy projections at
  save/link/mobile boundaries.

Session: 2026-08-30 — native RTC and obsolete address-route retirement
==========================================================================

Task worked on
--------------
- Continued Phase 1/13 native-runtime conversion with the existing native
  OS-clock implementation, and retired dead C-only Game Boy address/banking
  routes whose callers had already migrated.

Files modified
--------------
- `util/rtc.h`, `util/rtc.c`: added `NativeRTCClock`, native snapshot and
  day-counter rebase APIs; removed the runtime API that projected the OS clock
  into `gb.cart_rtc`; centralized timestamp persistence without an unaligned
  `time_t` read.
- `home/time.c`, `home/time.h`: migrated `GetClock` and `SetClock` from MBC3
  RTC registers to the native clock API and removed the now-unreferenced
  latch entry point.
- `engine/rtc/rtc.c`, `engine/rtc/rtc.h`: removed the halt/start MBC3 RTC
  routines and changed save-time RTC cleanup to the existing status API.
- `home/battle.c`, `home/battle.h`: removed unused byte-offset battle-party
  helper APIs and their Game Boy address arithmetic. Live battle code already
  uses typed `PartyMon` pointers.
- `home/queue_script.c`, `home/queue_script.h`, `wram.h`, `wram.c`: removed
  unused banked queued-script setters and demoted their three packed WRAM
  bytes to reserved compatibility layout storage. Native script/callback
  pointers remain the sole live queue owner.
- `LOCAL_CHANGELOG.txt`: recorded this session.

Files created/deleted
---------------------
- None.

Completed architectural changes
-------------------------------
- Gameplay clock reads now come directly from the operating system through
  `ReadNativeRTCClock`; the home time path and engine RTC save/start path no
  longer read, write, latch, halt, or restart MBC3 RTC registers.
- Crystal's 140-day wrap continues to be represented by rebasing the persisted
  native epoch with `SetNativeRTCClockDay`, rather than mutating an emulated
  cartridge day register.
- The old Crystal timestamp slots in `PlayerData` remain an explicit legacy
  save boundary for the native epoch. They are still serialized with existing
  Crystal saves and are updated whenever the epoch is rebased.
- Dead raw party byte-address APIs and dead banked queued-script setters have
  been removed after a repository-wide C caller search found no live users.

Temporary migration state and exact removal condition
------------------------------------------------------
- `legacyQueuedScriptReserved[3]` preserves the old WRAM offsets only. Remove
  it when the packed WRAM representation and offset checks are retired.
- RTC status flags and the timestamp bytes in `PlayerData` remain part of the
  Crystal-format save path. Replace them only as part of the versioned native
  save-system conversion with an explicit legacy importer.
- The packed player party remains legacy-owned; the earlier native egg
  projection remains transitional as documented in the preceding session.

Compatibility and save-format implications
------------------------------------------
- No Crystal save layout, link/mobile payload, or player-facing time-setting
  flow was intentionally changed. Existing timestamp bytes continue to seed
  native day calculations across launches.
- Native RTC availability failures preserve the previous in-memory clock
  snapshot rather than consulting MBC3 registers. The pre-existing fallback
  emulator tick remains outside this gameplay clock path.

Partially completed work
------------------------
- Native OS clock ownership is complete for the gameplay time path, but the
  surrounding save system remains Crystal SRAM-layout based and its RTC status
  flag storage is still banked.
- Native player-party ownership remains a read-only projection boundary; only
  egg-state semantics have been migrated so far.

Untouched remaining work
------------------------
- Native player-party mutation/ownership and serialization, native versioned
  saves, packed memory retirement, native content databases, native renderer,
  entity/map/script conversion, audio, and later roadmap phases.

Suspected source-level risks
----------------------------
- The native day counter is deliberately rebased using local calendar dates to
  preserve the original 140-day behavior. Daylight-saving and system-clock
  changes should be exercised during a later user-requested runtime test.
- `FixTime` and the legacy `PlayerData` start offsets are retained unchanged;
  they remain responsible for Crystal-compatible initial time/day selection.

Regressions discovered
----------------------
- None through source inspection. Runtime validation was intentionally not
  performed in this session.

Build/test commands and results
-------------------------------
- None. Build/runtime validation: NOT PERFORMED — per user instruction.

Exact stopping point and next recommended task
----------------------------------------------
- Stopped after source-level inspection confirmed no live C references remain
  to `UpdateRTC` or RTC register constants in the migrated clock files, and no
  live C users remain for the deleted raw battle/queued-script APIs.
- Next: introduce a native player-party owner and migrate bounded
  creation/removal/switching through explicit legacy save/link/mobile
  projections; do not widen legacy party structs in place.

Session addendum: 2026-08-30 — RTC latch removal
==================================================

Completed work
--------------
- `home/sram.c`: removed MBC3 clock-latch writes from SRAM bank open/close.
  SRAM selection and enable/disable behavior remain unchanged.
- `home/init.c`: removed the startup MBC3 clock-latch reset after native
  clock initialization.
- `constants/hardware_constants.h`: removed the now-unused MBC3 latch
  register constant.

Architectural impact
---------------------
- No live C runtime path now references the MBC3 clock-latch register. This
  completes removal of latch behavior following the native OS-clock migration;
  SRAM banking itself remains a separate legacy save-layout concern.

Partially completed work
------------------------
- Player-party ownership was audited for the next conversion slice. It still
  has approximately 240 direct packed-record accesses across gameplay,
  battle, menu, link, and mobile code. No duplicate native party owner was
  introduced because it would not be authoritative while those mutations
  remain live.

Untouched remaining work
------------------------
- Native player-party ownership/mutation/save projection and the broader
  native save, memory, renderer, entity, map, script, audio, and content work
  remain as listed in the preceding handoff.

Build/test commands and results
-------------------------------
- None. Build/runtime validation: NOT PERFORMED — per user instruction.

Exact stopping point and next recommended task
----------------------------------------------
- Stop at the native-party ownership boundary rather than adding a parallel
  party cache. Next, migrate a complete party mutation and its readers as one
  coherent slice, beginning with party creation/removal/switching and their
  save/link/mobile projections.

Session addendum: 2026-08-30 — native battle-animation control state
=======================================================================

Task worked on
--------------
- Corrected the reported `wram.c` layout-assertion build failure and migrated
  the active battle-animation script control bytes from packed WRAM into the
  existing native `battleanim_s` runtime object.

Completed work
--------------
- `wram.c`: replaced the invalid bare layout assertion for
  `legacyQueuedScriptReserved` with `check_wramx_field_mapped`, associating the
  renamed reserved span with its original `wQueuedScriptBank` address. This
  directly fixes the undeclared-field build error reported for line 1180.
- `constants/types.h`: extended `battleanim_s` with its own flags, wait delay,
  loop counter, and animation variable.
- `engine/battle_anims/anim_commands.c` and `.h`: moved all live readers and
  writers for those values to the native animation object, initialized them in
  `ClearBattleAnims`, and exposed a read-only variable accessor for macro
  scripts.
- `util/battle_anim_macros.h`: made `anim_if_var_equal` query the native
  animation state rather than packed WRAM.
- `wram.h` and `wram.c`: replaced the former battle-animation control fields
  with explicitly named legacy layout reserves and mapped each reserve to the
  original offset assertion. The OAM-pointer byte was also reserved after
  confirming its live C implementation uses a local index instead.

Architectural change and dependency removed
-------------------------------------------
- Battle animation control flow now belongs to the same native structure that
  already owns the host script function and host return pointer. It no longer
  depends on Game Boy-sized WRAM bytes for stop/keep-sprites, delay, loop, or
  animation-variable state.
- The packed WRAM spans are retained solely to preserve the currently asserted
  legacy layout; they are no longer authoritative runtime state.

Temporary migration state and exact removal condition
------------------------------------------------------
- `legacyBattleAnim*Reserved` and `legacyQueuedScriptReserved` remain only to
  preserve packed WRAM offsets. Remove them when the packed WRAM layout and
  offset assertions are retired as part of the native memory/save migration.
- `BattleAnimGetVar` is a narrow compatibility API for C animation data macros.
  Remove it when those macros become native animation-data functions or accept
  the active animation context directly.

Compatibility and save-format implications
------------------------------------------
- No save format, animation content, or timing values were intentionally
  changed. The native fields are reset at the same animation-clear boundary
  that previously zeroed the WRAM animation block.
- The large pre-existing deleted `Release/` output tree and the pre-existing
  nested `lib/libmobile` modification were observed but deliberately not
  modified.

Partially completed work
------------------------
- This is a contained battle-animation control-state conversion. Animation
  objects, palettes, VRAM tile staging, OAM compatibility storage, and the
  broader renderer still use legacy memory-oriented structures.
- Native party ownership and versioned saves remain at the earlier documented
  boundary; no parallel party cache was added.

Untouched remaining work
------------------------
- Native player-party ownership/mutation/save projection; native versioned
  saves; retirement of packed WRAM; native rendering, entities, maps, scripts,
  audio, content databases, expanded data models, and later roadmap phases.

Suspected source-level risks
----------------------------
- Battle animation scripts are host C functions but retain the original
  single-active-animation model. Future concurrent or expanded visual effects
  require a native animation manager rather than widening this one object.
- No compiler was run in this session; the source-level caller search found no
  live C access to any renamed battle-animation WRAM field.

Regressions discovered
----------------------
- None through source inspection.

Build/test commands and results
-------------------------------
- None. Build/runtime validation: NOT PERFORMED — per user instruction.

Exact stopping point and next recommended task
----------------------------------------------
- Stopped after moving the authoritative battle-animation control state into
  `battleanim_s` and preserving the legacy packed spans with mapped assertions.
- Next: migrate a coherent owner for battle animation objects/effects and their
  render submission, then remove their fixed WRAM slot assumptions without
  adding a post-render overlay or parallel authority.

Session addendum: 2026-08-30 — native battle-animation pools
==============================================================

Task worked on
--------------
- Fixed the stale function-table registration reported by the Windows build
  and continued the battle-animation conversion by making object/effect pools
  native runtime state rather than WRAM-owned arrays.

Completed work
--------------
- `functions.c`: removed the `REDIRECTED(FarCopyRadioText)` registration. The
  function was removed as an unused banked helper; source inspection confirmed
  no live C caller remains. Its address-map entry is retained as legacy map
  metadata only.
- `engine/battle_anims/core.c` and `.h`: introduced the single native
  `NativeBattleAnimationState` owner for animation objects, background
  effects, and the object serial index. Added explicit access/reset/clear and
  index-increment APIs.
- `engine/battle_anims/anim_commands.c` and `bg_effects.c`: migrated all live
  object/effect pool accesses to the native owner. `ClearBattleAnims` now
  resets the native pool at the same boundary that formerly cleared its WRAM
  range. The intentional legacy partial-clear behavior in `anim_clearobjs`
  remains byte-for-byte bounded through the native clear API.
- `wram.h` and `wram.c`: replaced the object pool, background-effect pool, and
  last-object-index fields with mapped legacy layout reserves. The previous
  battle-script control-state reserves remain intact.
- `engine/battle_anims/core.h`: added forward declarations and standard type
  inclusion so the new API header is self-contained for its existing callers.

Architectural change and dependency removed
-------------------------------------------
- Live battle animation object/effect state and its allocation serial are no
  longer authoritative in Game Boy WRAM. One native state owner serves every
  current C producer, updater, and renderer submission path.
- The fixed capacities are retained temporarily for behavior compatibility;
  they are native array capacities, not packed WRAM or OAM storage. Expanding
  them must be paired with a native render submission policy, rather than an
  arbitrary constant increase.

Temporary migration state and exact removal condition
------------------------------------------------------
- `legacyBattleAnimObjectsReserved`, `legacyBattleBGEffectsReserved`, and
  `legacyLastAnimObjectIndexReserved` preserve only legacy WRAM addresses.
  Remove them when the packed WRAM layout and address assertions are retired.
- The native pool still has the original fixed capacities. Replace it with a
  growable native collection only when all call sites use capacity-aware
  allocation and the renderer accepts more entries without Game Boy OAM
  clipping semantics.

Compatibility and save-format implications
------------------------------------------
- No save fields or serialized animation records were changed. The current
  pools are transient battle state and reset at the existing clear boundary.
- The reported `FarCopyRadioText` compile failure is corrected at its source:
  a deleted function is no longer referenced by the function-pointer table.

Partially completed work
------------------------
- Battle script control state and object/effect pools have native ownership,
  but palette staging, temporary animation command bytes, VRAM tile staging,
  virtual OAM output, and capacity limits remain legacy-oriented.

Untouched remaining work
------------------------
- Native player-party ownership/mutation/save projection; native versioned
  saves; packed WRAM retirement; native renderer/entity/map/script/audio and
  content-database work; expanded data models and later roadmap phases.

Suspected source-level risks
----------------------------
- Runtime behavior was not exercised. The migrated pool preserves the original
  allocation order, count limits, and clear timing, but battle effects should
  be tested when the user requests runtime validation.
- Legacy address-map symbols in `global.h`/`funcmap.h` remain intentionally for
  compatibility metadata and must not be mistaken for live native state.

Regressions discovered
----------------------
- None through source inspection.

Build/test commands and results
-------------------------------
- None. Build/runtime validation: NOT PERFORMED — per user instruction.

Exact stopping point and next recommended task
----------------------------------------------
- Stopped after verifying there are no live C WRAM accesses to the migrated
  object/effect pool or object-index fields and no remaining function-table
  registration for `FarCopyRadioText`.
- Next: define a capacity-aware native battle-animation render queue that feeds
  the SDL renderer directly, migrate temporary command staging into that owner,
  and only then retire the virtual-OAM dependency.

Session addendum: 2026-08-30 — PC cancellation repair and animation tile state
==============================================================================

Task worked on
--------------
- Fixed the reported lockup when cancelling from a PC box in Move Pokémon
  Without Mail, then continued the battle-animation native-state migration.

Completed work
--------------
- `engine/pokemon/bills_pc.c`: added the missing terminal state (case 6) to
  the Move Pokémon Without Mail C jumptable. Pressing B/cancel from the box
  selection sets that exact state; the original assembly routes it to
  `BillsPC_EndJumptableLoop`, but the translated C switch omitted it and kept
  the modal input loop alive forever. The C path now sets the loop-end flag and
  restores the caller normally.
- `engine/battle_anims/core.c` and `.h`: added native ownership/access for the
  battle animation tile dictionary within `NativeBattleAnimationState`.
- `engine/battle_anims/anim_commands.c`: migrated all live tile-dictionary
  readers/writers to the native animation owner.
- `wram.h` and `wram.c`: replaced the tile-dictionary field with a mapped
  legacy reserve, preserving its original packed offset without retaining it
  as live runtime state.

Architectural change and dependency removed
-------------------------------------------
- Battle animation tile dictionary state no longer depends on a Game Boy WRAM
  address. It resets with the same native animation owner as script state and
  object/effect pools.

Temporary migration state and exact removal condition
------------------------------------------------------
- `legacyBattleAnimTileDictReserved` preserves only the legacy WRAM span.
  Remove it when packed WRAM layout assertions are retired during native memory
  and save conversion.

Compatibility and save-format implications
------------------------------------------
- The PC repair restores the original state-machine transition; no box or save
  serialization was changed.
- Battle animation tile dictionary data is transient and continues to clear at
  the existing animation-reset boundary; no save-format change was made.

Partially completed work
------------------------
- Battle script, pool, and dictionary state have native ownership. Temporary
  battle command staging, palette/VRAM/OAM paths, original capacities, and the
  renderer's Game Boy-oriented output model remain to be migrated.

Untouched remaining work
------------------------
- Native player-party ownership/mutation/save projection; versioned saves;
  packed WRAM retirement; native renderer/entity/map/script/audio and content
  database work; expanded data models and later roadmap phases.

Suspected source-level risks
----------------------------
- The PC cancel repair was verified against the original assembly state table
  and source flow, but runtime behavior was not executed in this session.

Regressions discovered
----------------------
- None through source inspection.

Build/test commands and results
-------------------------------
- None. Build/runtime validation: NOT PERFORMED — per user instruction.

Exact stopping point and next recommended task
----------------------------------------------
- Stopped after source search verified no live C WRAM access remains to the
  moved animation object/effect/index/tile-dictionary state and the PC move
  state machine includes its assembly-defined terminal state.
- Next: migrate battle-animation temporary command staging and then replace
  virtual-OAM submission with a capacity-aware native render queue.

Session addendum: 2026-08-30 — native battle-animation command staging
=========================================================================

Completed work
--------------
- `engine/battle_anims/core.c` and `.h`: added a native command-state object
  owned by `NativeBattleAnimationState` for object and background-effect
  creation parameters.
- `engine/battle_anims/anim_commands.c`, `core.c`, and `bg_effects.c`:
  migrated every live C producer and consumer of those parameters to the
  native command-state object.
- `wram.h` and `wram.c`: replaced the four-byte overlapping legacy command
  span with a mapped layout reserve. No live C reader remains for its WRAM
  names.

Architectural impact
---------------------
- Object and background-effect command arguments no longer depend on the
  original overlapping Game Boy scratch bytes. They reset as part of the
  existing native battle-animation-state reset.

Temporary migration state and removal condition
------------------------------------------------
- `legacyBattleAnimationCommandReserved[4]` exists only for the current packed
  WRAM layout assertion. Remove it when packed WRAM layout support is retired.

Partially completed work
------------------------
- Other animation render/GFX/sine temporary state and virtual-OAM output still
  remain legacy-oriented and require a later complete render-queue migration.

Build/test commands and results
-------------------------------
- None. Build/runtime validation: NOT PERFORMED — per user instruction.

Exact stopping point and next recommended task
----------------------------------------------
- Stopped after source inspection confirmed no live C `wBattleObjectTemp*` or
  `wBattleBGEffectTemp*` access remains. Next migrate the render scratch state
  together with a native render queue; do not delete ASM sources while they
  remain repository reference material.

Session addendum: 2026-08-30 — native battle-animation render scratch state
===========================================================================

Completed work
--------------
- `engine/battle_anims/core.h` and `.c`: added `BattleAnimationRenderState`,
  owned and reset by `NativeBattleAnimationState`, for the transient per-object
  OAM flags, coordinates, offsets, tile, palette, and frame-flip state.
- `engine/battle_anims/core.c` and `helpers.c`: migrated every live C reader
  and writer of the nine render-scratch fields to that native state object.
- `wram.h` and `wram.c`: replaced the corresponding nine-byte WRAM structure
  with `legacyBattleAnimationRenderReserved[9]`, mapped only to retain the
  current packed WRAM layout assertion.

Architectural change and dependency removed
-------------------------------------------
- Battle-animation frame expansion no longer uses Game Boy WRAM scratch bytes
  as its authoritative render state. The transient state is now owned by the
  native animation runtime alongside its command state, object pool, and
  background-effect pool.

Temporary migration state and exact removal condition
------------------------------------------------------
- `legacyBattleAnimationRenderReserved[9]` exists only for the retained packed
  WRAM layout. Remove it when packed WRAM layout support and its address
  assertions are removed during native memory/save conversion.

Partially completed work
------------------------
- The battle animation GFX, sine-wave, and picture-resize scratch spans still
  use legacy WRAM. `BattleAnimOAMUpdate` still writes the legacy fixed-capacity
  virtual OAM array, so a complete native render queue remains unfinished.

Untouched remaining work
------------------------
- Native player-party ownership/mutation/save projection; versioned saves;
  packed WRAM retirement; native renderer/entity/map/script/audio and content
  database work; expanded data models and later roadmap phases.

Suspected source-level risks
----------------------------
- Render scratch state remains a single animation-wide object, matching the
  existing synchronous virtual-OAM expansion flow. A future parallel or queued
  renderer must make its per-object ownership explicit rather than reusing this
  transient singleton.

Regressions discovered
----------------------
- None through source inspection.

Build/test commands and results
-------------------------------
- None. Build/runtime validation: NOT PERFORMED — per user instruction.

Exact stopping point and next recommended task
----------------------------------------------
- Stopped after source inspection confirmed no live C access remains to
  `wBattleAnimTempOAMFlags` through `wBattleAnimTempPalette`.
- Next: move the GFX/sine/picture-resize animation scratch state into the same
  native owner, then design and migrate the virtual-OAM writer to a
  capacity-aware native render queue. No ASM source was deleted because its
  continued reference role cannot be proven unnecessary from the current build
  graph alone.

Session addendum: 2026-08-30 — native battle-animation effect scratch state
===========================================================================

Completed work
--------------
- `engine/battle_anims/core.h` and `.c`: added native effect-scratch state to
  `NativeBattleAnimationState` for animation GFX loading, battler-picture
  height, picture-resize tile base, and sine/deformation calculation state.
- `engine/battle_anims/anim_commands.c` and `bg_effects.c`: migrated all live
  C producers and consumers of those fields to the native owner.
- `wram.h` and `wram.c`: replaced the four-byte overlapping legacy scratch
  span with `legacyBattleAnimationEffectScratchReserved[4]`, retained only for
  the existing packed WRAM layout assertion.

Architectural change and dependency removed
-------------------------------------------
- The migrated animation GFX, picture-resize, and sine-wave helper operations
  no longer use the shared Game Boy WRAM scratch bytes. Their independent
  native fields deliberately remove the legacy address overlap from runtime
  semantics.

Temporary migration state and exact removal condition
------------------------------------------------------
- `legacyBattleAnimationEffectScratchReserved[4]` exists only to preserve the
  packed WRAM layout. Remove it when packed WRAM layout support and its address
  assertions are removed during native memory/save conversion.

Partially completed work
------------------------
- Battle animation still writes to the fixed-capacity virtual OAM array and
  directly targets GB-style VRAM tile buffers. A capacity-aware native render
  queue and resource system remain required before the renderer can be called
  native.

Untouched remaining work
------------------------
- Native player-party ownership/mutation/save projection; versioned saves;
  packed WRAM retirement; native renderer/entity/map/script/audio and content
  database work; expanded data models and later roadmap phases.

Suspected source-level risks
----------------------------
- The old GFX/picture/sine fields intentionally shared four hardware-address
  bytes. The migrated operations are synchronous and have no live C callers
  relying on that overlap; any later concurrent renderer work must retain
  explicit per-operation state ownership.

Regressions discovered
----------------------
- None through source inspection.

Build/test commands and results
-------------------------------
- None. Build/runtime validation: NOT PERFORMED — per user instruction.

Exact stopping point and next recommended task
----------------------------------------------
- Stopped after source inspection confirmed no live C WRAM access remains to
  the command, render, GFX, sine, or picture-resize battle-animation scratch
  spans migrated in this session.
- Next: replace virtual-OAM submission with a native capacity-aware render
  queue, including explicit layer/priority semantics and a migration plan for
  direct VRAM tile writes. No ASM source was deleted because its continued
  reference role cannot be proven unnecessary from the current build graph.

Session addendum: 2026-08-30 — battle animation native sprite submission
==========================================================================

Completed work
--------------
- `engine/battle_anims/core.h` and `.c`: added a dynamically growing native
  battle-animation sprite list with widened position/tile fields. The list is
  reset per animation frame and no longer has the Game Boy 40-sprite capacity.
- `engine/battle_anims/core.c`: converted `BattleAnimOAMUpdate` from a
  fixed-OAM writer with an overflow return path into a native sprite submitter.
  It no longer reads or writes `wVirtualOAMSprite` and no longer stops an
  animation after forty submitted sprites.
- `engine/battle_anims/anim_commands.c`: made battle-animation frame updates
  begin a native sprite frame instead of filling virtual OAM; retained only
  the existing legacy-layer clear where the original command owns that layer.
  Keep/delete palette behavior now also affects native submitted sprites.
- `tools/emu/peanut_sdl.c`: factored object pixel drawing into a shared C
  routine and consumes the native battle-animation list directly in the
  renderer's ordinary sprite pass. Native battle sprites are not a post-render
  overlay and preserve their former ordering relative to legacy OAM sprites.

Architectural change and dependency removed
-------------------------------------------
- Battle animation object rendering is no longer constrained by, staged in,
  or transferred through the 160-byte virtual OAM array. The fixed OAM limit
  is no longer authoritative for that subsystem.

Temporary migration state and exact removal condition
------------------------------------------------------
- The current shared pixel routine still resolves native battle sprite tile
  identifiers through the existing GB tile buffer and palette rules. This is a
  transitional renderer bridge, not a second render pass. Remove that bridge
  when the native renderer owns sprite texture/atlas resources and palette
  resolution; at that point `DrawObjectSprite` must consume native resources
  directly and the legacy OAM branch can be migrated separately.
- The remaining `wVirtualOAMSprite` clears in battle animation commands exist
  only to preserve current ownership of the legacy battle sprite layer. Remove
  them when battle HUDs and every remaining battle sprite producer submit to
  the native renderer.

Compatibility and save-format implications
------------------------------------------
- No save representation or serialized gameplay data changed. Battle sprite
  ordering remains reverse submission order, matching the old low-OAM-slot
  precedence.

Partially completed work
------------------------
- Battle animation source state and sprite submission are native, but its
  tile/palette source still uses legacy VRAM data. The generic renderer, battle
  HUDs, overworld objects, and all other OAM users still depend on the
  Game Boy-era rendering model.

Untouched remaining work
------------------------
- Native player-party ownership/mutation/save projection; versioned saves;
  packed WRAM retirement; native renderer/entity/map/script/audio and content
  database work; expanded data models and later roadmap phases.

Suspected source-level risks
----------------------------
- The native list is heap-backed and aborts on allocation failure rather than
  silently truncating sprites. Runtime animation coverage is required before
  declaring visual equivalence, particularly for animation commands that keep
  sprites across frames.

Regressions discovered
----------------------
- None through source inspection.

Build/test commands and results
-------------------------------
- None. Build/runtime validation: NOT PERFORMED — per user instruction.

Exact stopping point and next recommended task
----------------------------------------------
- Stopped after moving battle-animation sprite generation and primary renderer
  consumption off the fixed OAM buffer. The current native list can grow past
  forty sprites, but its tile data still comes from the legacy VRAM resource
  path.
- Next: introduce native sprite-resource/atlas ownership for battle animation
  tiles and palettes, then migrate battle HUD producers to the same renderer
  before retiring their OAM-layer operations. No ASM source was deleted because
  its continued reference role cannot be proven unnecessary from the current
  build graph.

Session addendum: 2026-08-30 — native battle-animation tile resources
=======================================================================

Completed work
--------------
- `engine/battle_anims/core.h` and `.c`: added a dynamically growing native
  2bpp tile-resource store owned by `NativeBattleAnimationState`. It is keyed
  by the animation's logical tile identifiers and released at the established
  animation reset boundary.
- `engine/battle_anims/anim_commands.c`: migrated the live `NGFX` command
  path from the fixed `vTiles0 + BATTLEANIM_BASE_TILE` VRAM range to native tile
  storage. Asset conversion now writes into ordinary native memory rather than
  Game Boy VRAM for this command path.
- `tools/emu/peanut_sdl.c`: extended the ordinary sprite-pixel path to read a
  native battle tile when present. The battle-animation queue now consumes its
  own native tile resources directly; legacy VRAM is only used as a fallback
  for animation producers not yet migrated.

Architectural change and dependency removed
-------------------------------------------
- Standard battle-animation GFX commands no longer depend on the hardware tile
  range reserved by `BATTLEANIM_BASE_TILE`. Their lifetime, capacity, and tile
  bytes are owned by native battle-animation state.

Temporary migration state and exact removal condition
------------------------------------------------------
- The renderer's legacy-VRAM fallback remains for battler-picture animation
  commands and other pre-existing sprite producers that have not yet been
  migrated to native tile resources. Remove that fallback for battle sprites
  once those producers submit resource-backed native sprites as well.
- The fixed native tile dictionary still preserves original one-byte script
  tile offsets. Replace it with an expandable native mapping only after the
  battle animation object/frame data representation is widened together, so no
  caller truncates a new tile identity.

Compatibility and save-format implications
------------------------------------------
- No persistent data or save serialization changed. The native store keeps the
  existing 2bpp asset conversion output and tile ordering for current content.

Partially completed work
------------------------
- `NGFX` resources and animation sprite submission are native. Battler GFX,
  HUD sprites, palette ownership, animation object/frame IDs, and the wider
  renderer remain partially tied to legacy tile/OAM representations.

Untouched remaining work
------------------------
- Native player-party ownership/mutation/save projection; versioned saves;
  packed WRAM retirement; native renderer/entity/map/script/audio and content
  database work; expanded data models and later roadmap phases.

Suspected source-level risks
----------------------------
- This path intentionally preserves Crystal's 2bpp pixel encoding while
  removing its VRAM placement requirement. Runtime coverage is needed for all
  special-case animation GFX packs (including skipped-empty-tile layouts) and
  for battler-picture animation commands that still use the fallback.

Regressions discovered
----------------------
- None through source inspection.

Build/test commands and results
-------------------------------
- None. Build/runtime validation: NOT PERFORMED — per user instruction.

Exact stopping point and next recommended task
----------------------------------------------
- Stopped after the normal animation GFX command path became native-resource
  backed and renderer-visible without VRAM staging.
- Next: migrate battler-picture GFX and the fixed tile dictionary to widened
  native resource identities, then move battle HUD sprite producers to the
  same render state before removing remaining battle OAM operations. No ASM
  source was deleted because its continued reference role cannot be proven
  unnecessary from the current build graph.

Session addendum: 2026-08-30 — dynamic animation bindings and battler GFX
=========================================================================

Task worked on
--------------
- Continued the native battle-animation rendering conversion without starting
  a separate subsystem. Removed the active fixed-size animation tile dictionary
  and moved both battler-picture animation copy commands to native tile
  resources.

Completed work
--------------
- `engine/battle_anims/core.h` and `.c`: replaced the active five-entry
  `wBattleAnimTileDict`-shaped array with an owned, dynamically growing list of
  graphics-ID-to-tile-offset bindings. Lookup and reset now operate on this
  native representation; the old WRAM field is no longer used by this runtime
  path.
- `engine/battle_anims/anim_commands.c`: migrated `NGFX` binding writes and
  both one-row/two-row battler GFX binding writers to that dynamic API.
- `engine/battle_anims/anim_commands.c`: moved `BattleAnimCmd_BattlerGFX_1Row`
  and `BattleAnimCmd_BattlerGFX_2Row` destination copies from `vTiles0` into
  the native battle-animation tile store. The renderer can now consume the
  staged player-head/enemy-feet animation tiles without a destination VRAM
  dependency.

Files modified
--------------
- `engine/battle_anims/core.h`
- `engine/battle_anims/core.c`
- `engine/battle_anims/anim_commands.c`
- `LOCAL_CHANGELOG.txt`

Files created/deleted
---------------------
- None. No ASM source was deleted.

Architectural changes and obsolete dependency removed
-----------------------------------------------------
- The runtime no longer uses the fixed `NUM_BATTLEANIMTILEDICT_ENTRIES` tile
  dictionary for battle-animation resource binding. The legacy WRAM reservation
  remains solely to preserve the still-active wider WRAM map while other users
  are converted.
- Both battler GFX animation commands now write their generated animation tile
  bytes to native allocation-managed memory, rather than using Game Boy VRAM as
  their destination buffer.

Partially completed work and transitional state
------------------------------------------------
- Binding values and `struct BattleAnim::tileId` remain one byte because the
  native object/frame representation has not yet been separated from its
  Crystal byte layout. This is an explicit remaining content-width boundary,
  not a claim that arbitrary battle animation tile identities already work.
- Battler GFX source pixels still originate from the existing battle-picture
  staging area. That source dependency remains until battle pictures themselves
  are native resources.
- The renderer retains a legacy VRAM fallback for unconverted battle sprite
  producers. Remove it only when every battle sprite producer supplies native
  tile resources and runtime validation confirms all battle scenes and effects.
- The legacy `wBattleAnimTileDict` WRAM reservation can be removed only after
  a complete WRAM-layout migration removes its address-map compatibility role;
  it is not accessed by the converted C runtime.

Compatibility and save-format implications
------------------------------------------
- No save data or serialization changed. Existing animation ordering and
  current one-byte script offsets are retained.

Suspected source-level risks
----------------------------
- The one-byte tile offset boundary still limits expanded animation data even
  though the binding collection itself is unbounded. Widening it requires a
  semantic native animation-object representation and a deliberately migrated
  clear/reset policy, not an in-place field-size edit.
- Runtime coverage is still required for commands that combine NGFX bindings
  with battler GFX commands, plus battle scenes using both row layouts.

Untouched remaining work
------------------------
- Native battle HUD sprites and picture sources; widened animation objects and
  frame/resource IDs; full renderer, entity, map, script, audio, save, and
  content-database roadmap phases.

Build/test commands and results
-------------------------------
- None. Build/runtime validation: NOT PERFORMED — per user instruction.

Exact stopping point and next recommended task
----------------------------------------------
- Stopped after the active animation binding table and both battler GFX copy
  destinations became native-owned. The source tree is internally coherent by
  inspection, but deliberately unbuilt at the user's direction.
- Next: introduce a native semantic battle-animation object/frame type with a
  widened tile resource identity, migrate its clear/reset semantics explicitly,
  then move persistent trainer HUD sprites into the native battle renderer.

Session addendum: 2026-08-30 — native renderer header build fix
=================================================================

Completed work
--------------
- `engine/battle_anims/core.h`: added an include guard for the native battle
  animation interface. This fixes the reported duplicate type/prototype errors
  when the renderer includes the interface directly and the project's central
  include path reaches it again through `main.h`.

Build-error prevention
----------------------
- The native battle-render interface is now safe to include from both its
  direct renderer consumer and aggregate project headers. New standalone native
  interfaces must use an include guard before they are consumed outside their
  implementation unit; this avoids dependency-order-sensitive duplicate C
  declarations without adding an ASM compatibility layer.

Native/ASM boundary
-------------------
- The corrected interface is a C-owned data and rendering API. It has no ASM
  function-address dependency. Existing Crystal-origin labels in nearby source
  comments are explanatory provenance only and are not runtime calls.

Files modified
--------------
- `engine/battle_anims/core.h`
- `LOCAL_CHANGELOG.txt`

Files created/deleted
---------------------
- None.

Build/test commands and results
-------------------------------
- None. Build/runtime validation: NOT PERFORMED — per user instruction.

Exact stopping point and next recommended task
----------------------------------------------
- Stopped after correcting the build-blocking public interface issue by source
  inspection. Continue with the planned semantic native battle-animation object
  representation before removing any ASM reference material.

2026-08-30 — Native battle-animation object ownership
======================================================

Battle-animation runtime objects now have a native, unpacked definition in
`engine/battle_anims/core.h`. The former packed `constants/types.h` record was
removed from runtime ownership. Tile resource identities, render scratch tile
state, and dynamic graphics bindings are now 16-bit values. Animation object
clearing now operates on semantic pool entries instead of Crystal byte spans;
the enabled clear-object behavior clears the complete native pool.

`engine/battle_anims/anim_commands.c`, `core.c`, and `core.h` now reject tile
resource ranges that cannot be represented by the native 16-bit identity rather
than wrapping them. Frame tile offsets are checked before conversion to the
render resource identity. The dynamic resource store remains the authoritative
destination for converted animation graphics and battler-animation staging.

`AGENTS.md` establishes the persistent native-runtime migration constraints:
semantic ownership, explicit compatibility boundaries, checked conversions,
and prohibition of new emulator-dispatch or hardware-memory dependencies.

The script bytecode, frame tables, palette/scanline effects, battle-picture
source staging, and non-animation battle HUD producers remain legacy-adjacent
boundaries. They require separate resource and renderer ownership migration
before legacy VRAM/OAM fallbacks can be removed. No ASM reference source was
deleted.

Static validation confirmed that the active object pool no longer uses packed
layout length or fixed tile-dictionary capacity, and that the widened tile
identity has no remaining narrowing caller in the battle-animation object path.
Build and runtime validation were not performed.

Next dependency closure: migrate battle-picture source staging and persistent
trainer HUD sprite ownership into the native battle renderer, then retire the
remaining battle-animation OAM clear and VRAM fallback.

2026-08-30 — Native trainer HUD sprite ownership
=================================================

Completed work
--------------
- `engine/battle/trainer_huds.c` now stages party-ball states in local native
  arrays and publishes player, opponent, and link HUD sprites through the
  native battle renderer. It no longer writes HUD sprite entries to the fixed
  virtual OAM buffer or uses `wBattleHUDTiles` as its data source.
- `engine/battle_anims/core.[ch]` now owns a distinct persistent HUD sprite
  list and decoded HUD tile resource store. Animation-frame sprite resources
  and HUD resources are explicitly tagged, so the renderer selects the native
  source without a tile-number or VRAM-address convention.
- `tools/emu/peanut_sdl.c` renders native HUD sprites through the existing
  primary object-pixel path after legacy OAM and before transient animation
  sprites. HUD rendering is therefore not constrained by legacy OAM slots.
- `engine/battle/core.c` clears and releases native HUD state on battle exit.
  Battle and link HUD initialization also clear it before publishing their
  current view, preventing stale trainer entries from reaching another battle
  or overworld.

Architectural state and retained boundaries
-------------------------------------------
- Trainer HUD ball sprites and their four decoded tile resources are now
  native battle-view state. The fixed twelve-entry HUD array represents the
  deliberate six-Pokémon-per-side game rule, not a renderer capacity limit.
- Animation reset preserves HUD resources because an animation clear can occur
  while the battle HUD remains visible. Battle exit owns logical HUD removal.
- HUD border tiles, palette writes, tilemap composition, and all unconverted
  sprite producers remain legacy renderer boundaries. The renderer retains its
  VRAM fallback only for those unconverted producers.
- No temporary synchronization bridge was introduced: HUD state has one native
  owner. No ASM source was deleted because wider source/build/runtime proof of
  obsolescence is still absent.

Source-level validation and risks
---------------------------------
- Inspected all trainer HUD publishers, native animation reset, battle exit,
  and the renderer call sites. The reset lifecycle now preserves active HUD
  data while battle entry and exit establish explicit view lifetime.
- Preserved the existing 8x16 animation-sprite tile alignment at the native
  resource lookup boundary; this keeps the former hardware sprite-size visual
  rule without making native HUD ownership depend on virtual OAM.
- The native resource allocator rejects arithmetic overflow before growing a
  tile store. The public header now includes the constants required for its
  standalone `PARTY_LENGTH` declaration.
- Runtime visual validation remains needed for wild, trainer, link, switch,
  and animation-over-HUD scenes. The retained tilemap and palette paths still
  carry Game Boy presentation constraints outside this migrated sprite path.

Files modified
--------------
- `engine/battle/core.c`
- `engine/battle/trainer_huds.c`
- `engine/battle_anims/core.c`
- `engine/battle_anims/core.h`
- `tools/emu/peanut_sdl.c`
- `LOCAL_CHANGELOG.txt`

Build/test commands and results
-------------------------------
- None. Build/runtime validation: NOT PERFORMED — per user instruction.

Exact stopping point and next recommended task
----------------------------------------------
- Stopped after moving persistent trainer HUD ball sprites and tile ownership
  off virtual OAM/WRAM into the native battle renderer and closing their reset
  and battle-exit lifetime.
- Next: move battle-picture staging into a native image resource and migrate
  its renderer consumers; then audit remaining battle sprite producers before
  removing the legacy VRAM fallback.

2026-08-30 — Native transient picture staging
==============================================

Completed work
--------------
- `engine/gfx/load_pics.c` now keeps padded front pictures, animated front
  picture transforms, and aligned back pictures in function-owned native
  buffers instead of the shared `wDecompressScratch` WRAM area.
- Animated front-picture construction receives its padded source explicitly;
  the former public helper was made internal after confirming it had no active
  callers outside this compilation unit. The loader interface no longer
  advertises that internal staging operation.

Architectural state and retained boundaries
-------------------------------------------
- Picture decoding remains a direct C resource load. The migrated staging
  buffers have one owner and no synchronized WRAM copy.
- The raw `wDecompressEnemyFrontpic` region remains active for the existing
  animated-front-picture tile transform. Its noncompact source layout and
  offset semantics need separate source-level migration before it can be
  replaced safely.
- Picture destinations still include legacy VRAM-backed consumers. This work
  removes shared source staging only; it does not claim a complete native
  battle-picture renderer or remove the existing compatibility destination.

Source-level validation and risks
---------------------------------
- Confirmed no active C caller remains for `GetAnimatedEnemyFrontpic`; the
  only other occurrence is an inactive function-map comment.
- Preserved 7x7 and 6x6 picture byte counts, frame selection, padding,
  orientation, and destination writes. Runtime checks are still needed for
  animated fronts, back pictures, and every call path using them.

Files modified
--------------
- `engine/gfx/load_pics.c`
- `engine/gfx/load_pics.h`
- `LOCAL_CHANGELOG.txt`

Build/test commands and results
-------------------------------
- None. Build/runtime validation: NOT PERFORMED — per user instruction.

Exact stopping point and next recommended task
----------------------------------------------
- Stopped after removing `wDecompressScratch` from active front/back picture
  staging while retaining the unexamined animated-front raw buffer boundary.
- Next: model the raw animated-front-picture source layout as an explicit
  native resource, then migrate battle picture destinations and renderer
  consumers away from VRAM-backed tile identities.

2026-08-30 — Native battle picture source and animation scratch ownership
=========================================================================

Completed work
--------------
- `engine/gfx/load_pics.c` now owns the raw animated-front-picture source in
  a dynamically growing native resource. The old `wDecompressEnemyFrontpic`
  WRAM buffer has no active C consumer in the picture-loading path.
- The native source preserves the existing persistent lifetime and starting
  capacity while growing when a source image requires more tiles. Padded and
  transformed picture buffers remain explicit function-owned data.
- `engine/battle_anims/anim_commands.c` now builds Substitute, Minimize, and
  Minimize-Opp picture transforms in native local buffers. These commands no
  longer open SRAM, address `sScratch`, or use Game Boy RAM access helpers as
  temporary image storage.
- `GetSubstitutePic` and the minimize builder are internal implementation
  details now that no active caller needs a legacy-addressable scratch buffer.

Architectural state and retained boundaries
-------------------------------------------
- Raw picture input, padding, orientation, Substitute, and Minimize staging
  now have native C ownership with no synchronized WRAM/SRAM copy.
- Existing battle picture destinations and the display tilemap remain
  VRAM-backed. The corresponding WRAM fields are retained only as part of the
  broader address-map compatibility layout and must not be removed until that
  layout has no remaining source/build/runtime dependency.
- No ASM source was deleted: this resource migration alone does not prove
  unused reference material obsolete.

Source-level validation and risks
---------------------------------
- Checked that active C references to `wDecompressEnemyFrontpic`,
  `sScratch`, the removed public Substitute helper, and the old minimize
  record are absent from the migrated paths.
- Preserved current source offsets, persistent source lifetime, image byte
  counts, and player/enemy destination selection. Runtime coverage is still
  required for animated front pictures, Substitute, and both Minimize paths.

Files modified
--------------
- `engine/gfx/load_pics.c`
- `engine/battle_anims/anim_commands.c`
- `engine/battle_anims/anim_commands.h`
- `LOCAL_CHANGELOG.txt`

Build/test commands and results
-------------------------------
- None. Build/runtime validation: NOT PERFORMED — per user instruction.

Exact stopping point and next recommended task
----------------------------------------------
- Stopped after completing native ownership of the raw picture source and the
  selected battle-animation scratch transforms. Legacy picture destinations
  remain intentionally untouched.
- Next: introduce native battle-picture presentation resources and migrate
  their display consumers, then remove the remaining battle-animation VRAM
  source reads and fallback renderer path only after coverage confirms them
  obsolete.

2026-08-30 — Explicit native tilemap graphic placement
=======================================================

Completed work
--------------
- Added `PlaceGraphicNative` and `PlaceGraphicYStaggerNative`, whose explicit
  start-tile argument replaces `hGraphicStartTile` as the authoritative input
  for direct C tilemap picture placement.
- Migrated every direct C caller of the two placement helpers across battle,
  picture/menu, player, breeding, Hall of Fame, debug, movie, home, and mobile
  code. Each call now supplies the source-inspected tile origin directly.
- Updated `PlaceGraphic_Padded` to use its existing explicit `start` argument
  in its unpadded path, eliminating its former hidden HRAM input as well.
- Removed the superseded `hGraphicStartTile` writes that existed only to feed
  migrated C placement calls. Existing unrelated state writes were retained.

Architectural state and retained boundaries
-------------------------------------------
- Direct C presentation work no longer communicates a tile origin through
  global HRAM state. Tile origin is now ordinary function data, while existing
  `wBoxAlignment` behavior remains intentionally unchanged.
- The old `PlaceGraphic` and `PlaceGraphicYStagger` entry points remain as a
  narrow temporary predef compatibility boundary. `home/predef.c` still
  dispatches `pPlaceGraphic` through emulated register/HRAM state, so deleting
  those entry points now would break that legacy dispatcher.
- The temporary boundary can be removed only after `Predef`/`pPlaceGraphic`
  dispatch is migrated to a typed native call path and no source, function map,
  or runtime caller requires the register-based ABI.
- No ASM source was deleted: the retained predef route means the corresponding
  legacy reference cannot yet be proven obsolete.

Source-level validation and risks
---------------------------------
- Confirmed all non-legacy direct C invocations now call an explicit native
  placement helper; the two old helper names remain only as compatibility entry
  points in `place_graphic.c`.
- Preserved the original tile origins, dimensions, Y-staggering, and existing
  box-alignment behavior. `MovePlayerPic` and the mobile trainer-picture path
  were inspected against their resource destinations and use tile zero, as in
  their original picture loads.
- Runtime coverage is still required for each migrated menu/picture path and
  for the legacy predef boundary.

Files modified
--------------
- `engine/gfx/place_graphic.c`
- `engine/gfx/place_graphic.h`
- `engine/battle/core.c`
- `engine/battle/misc.c`
- `engine/debug/color_picker.c`
- `engine/events/halloffame.c`
- `engine/events/pokepic.c`
- `engine/events/print_unown.c`
- `engine/gfx/player_gfx.c`
- `engine/menus/debugmenu.c`
- `engine/menus/intro_menu.c`
- `engine/menus/trainer_card.c`
- `engine/movie/trade_animation.c`
- `engine/pokemon/breeding.c`
- `home/pokemon.c`
- `mobile/mobile_22.c`
- `mobile/mobile_42.c`
- `mobile/mobile_5f.c`
- `LOCAL_CHANGELOG.txt`

Build/test commands and results
-------------------------------
- None. Build/runtime validation: NOT PERFORMED — per user instruction.

Exact stopping point and next recommended task
----------------------------------------------
- Stopped after completing direct-C migration away from `hGraphicStartTile`
  for generic tilemap graphic placement.
- Next: replace the remaining `pPlaceGraphic` predef route with typed native
  invocation, then move tilemap picture output itself out of legacy VRAM/tile
  identity ownership.

2026-08-30 — Native physical input authority
=============================================

Completed work
--------------
- Removed the last active C writes to the physical joypad HRAM mirror fields
  (`hJoypadReleased`, `hJoypadPressed`, `hJoypadDown`, and `hJoypadSum`).
  `UpdateJoypad` now advances only the existing native SDL input state.
- Confirmed that current C gameplay reads physical and logical input through
  `NativeInput*` APIs rather than through those four HRAM fields.

Architectural state and retained boundaries
-------------------------------------------
- SDL-backed `NativeInputState` is now the sole active C authority for
  physical input. Logical-input state remains native as well.
- The four HRAM slots remain in the legacy address-map layout only. They can
  be removed when the emulated address map, its layout assertions, and any
  legacy bytecode/ASM boundary no longer require those historical addresses.
- No temporary state synchronization was introduced and no ASM was deleted.

Source-level validation and risks
---------------------------------
- Searched non-ASM C sources for all physical and logical joypad HRAM names.
  The removed writes were the only active C accesses to the physical mirror
  fields; other results are declarations or source-provenance comments.
- The native state advance ordering and input-disable checks are unchanged.
  Runtime coverage remains needed for input while saving, scripted input, and
  all controller/keyboard mappings.

Files modified
--------------
- `home/joypad.c`
- `LOCAL_CHANGELOG.txt`

Build/test commands and results
-------------------------------
- None. Build/runtime validation: NOT PERFORMED — per user instruction.

Exact stopping point and next recommended task
----------------------------------------------
- Stopped after removing active C ownership of the physical joypad HRAM
  mirrors.
- Next: replace native frame progression's VBlank/emulator coupling with a
  platform frame scheduler while retaining the same gameplay cadence.

2026-08-30 — Animated front-picture crash repair
=================================================

Completed work
--------------
- Replaced the animated-front-picture tile conversion's register-shaped byte
  loop with a bounded native tile copy in `engine/gfx/load_pics.c`.
- The native path now initializes the 7×7 animated image from the already
  padded picture, then overlays exactly the source picture's 5×5, 6×6, or
  7×7 tile count with the required horizontal orientation.
- Aligned `BuildAnimatedEnemyFrontpic`'s internal declaration with its
  read-only source parameter after the user-reported `-Werror` type conflict;
  this is a declaration-only correction and preserves the bounded native path.

Regression investigated and fixed
---------------------------------
- User-reported battle entry and Pokémon summary crashes share the animated
  front-picture path. The old C conversion treated a tile count as a packed
  register value, repeatedly invoked a byte loop with a zero byte count, and
  wrote far beyond its 7×7 local buffer. This explains a process exit without
  a useful game log.
- The replacement has explicit tile and byte bounds and does not use WRAM,
  SRAM, ROM banks, or ASM/register behavior as image scratch storage.

Architectural state and retained boundaries
-------------------------------------------
- Animated front-picture source and transient output remain native memory.
  The existing VRAM destination remains a display compatibility boundary and
  was not reintroduced as the image source or scratch owner.

Source-level validation and risks
---------------------------------
- Verified battle setup and summary animation both call `GetAnimatedFrontpic`.
- Verified the replacement writes at most 49 tiles (784 bytes) into the fixed
  animated output buffer and preserves the existing orientation behavior.
- Runtime verification remains needed for battle entry, normal summary pages,
  Unown, Eggs, and picture sizes 5×5/6×6/7×7.

Files modified
--------------
- `engine/gfx/load_pics.c`
- `LOCAL_CHANGELOG.txt`

Build/test commands and results
-------------------------------
- None. Build/runtime validation: NOT PERFORMED locally — per user instruction.

Exact stopping point and next recommended task
----------------------------------------------
- Stopped after repairing the shared animated front-picture buffer overflow.
- Next: introduce a native picture presentation resource so battle and menu
  views no longer use VRAM tile identities as their runtime image API.

2026-08-30 — Battle-animation header type ownership build fix
==============================================================

Completed work
--------------
- Made `engine/battle_anims/functions.h` self-contained by including the
  native battle-animation model definition from `core.h` and adding a normal
  include guard.
- This fixes the reported `struct BattleAnim` prototype-scope declarations,
  which caused incompatible function declarations throughout
  `functions.c` under `-Werror`.

Architectural state and retained boundaries
-------------------------------------------
- The header now directly depends on the native `BattleAnim` runtime model;
  no ASM declaration, emulated register type, or compatibility wrapper was
  introduced.

Source-level validation and risks
---------------------------------
- Confirmed `core.h` defines `BattleAnim` and its required semantic ID types,
  and does not include `functions.h`, so this dependency does not create a
  header cycle.
- Build/runtime validation was not run locally per user instruction; the
  reported compiler command should now see one shared structure declaration.

Files modified
--------------
- `engine/battle_anims/functions.h`
- `LOCAL_CHANGELOG.txt`

Build/test commands and results
-------------------------------
- None. Build/runtime validation: NOT PERFORMED — per user instruction.

Exact stopping point and next recommended task
----------------------------------------------
- Stopped after resolving the reported native battle-animation header failure.
- Next: replace native frame progression's VBlank/emulator coupling with a
  platform frame scheduler while retaining the same gameplay cadence.

2026-08-30 — Native trainer-intro HUD lifecycle repair
=======================================================

Completed work
--------------
- Updated `engine/battle/core.c` so the end of the trainer-introduction
  presentation clears the native trainer party-marker HUD immediately after
  clearing the departing trainer's legacy sprite data.

Regression investigated and fixed
---------------------------------
- The trainer party Poké Ball markers were moved to the native battle-render
  state, but the corresponding battle-introduction cleanup still cleared only
  legacy OAM. The native markers therefore survived into the player combat HUD
  and overlaid the HP/EXP panel.
- The fix uses `ClearBattleAnimationHudSprites` directly at the existing
  battle-view lifecycle boundary; it neither calls ASM nor reintroduces OAM,
  Game Boy register, or compatibility ownership for the native HUD.

Architectural state and retained boundaries
-------------------------------------------
- Trainer-intro HUD markers remain owned by the native battle renderer. Legacy
  OAM remains a compatibility destination for the trainer sprite only.

Source-level validation and risks
---------------------------------
- Verified this cleanup follows `BattleStartMessage`, which creates the
  trainer HUD markers, and precedes normal battle HUD drawing.
- The same native HUD state is still cleared on battle exit. Runtime checking
  remains needed for trainer, wild, link, tutorial, and double-battle flows.

Files modified
--------------
- `engine/battle/core.c`
- `LOCAL_CHANGELOG.txt`

Build/test commands and results
-------------------------------
- None. Build/runtime validation: NOT PERFORMED locally — per user instruction.

Exact stopping point and next recommended task
----------------------------------------------
- Stopped after restoring the trainer-intro HUD's native cleanup lifecycle.
- Next: separate battle-picture presentation resources from VRAM tile identity
  so battle and menu picture views can use the native renderer directly.

2026-08-30 — Native trainer-HUD border state
==============================================

Completed work
--------------
- Removed active C use of the legacy `wTrainerHUDTiles` WRAM scratch buffer
  from `engine/battle/trainer_huds.c`.
- Trainer-HUD border routines now pass immutable tile definitions directly to
  their native placement helper. The helper no longer receives data through
  the Game Boy-addressed scratch buffer.
- Replaced the native trainer-HUD renderer's fixed 12-sprite array with a
  dynamically allocated sprite list. Its marker API now accepts an explicit
  count and 16-bit resource tile IDs instead of assuming six entries per side
  and 8-bit hardware tile identity.

Architectural state and retained boundaries
-------------------------------------------
- Border appearance remains identical and tilemap output remains a legacy
  display compatibility boundary. The temporary border definition is now
  native C data with function-local ownership.
- Trainer marker sprites and their source tiles are native renderer-owned
  dynamic resources. The current battle callers still request Crystal's six
  party slots, but the renderer no longer imposes that fixed allocation.
- The packed WRAM field remains in the legacy layout because it is still
  documented by the original ASM/reference memory map; it has no active C
  reader or writer in this trainer-HUD subsystem and is not deleted yet.

Source-level validation and risks
---------------------------------
- Confirmed the only C uses of `wTrainerHUDTiles` were the three border
  writers and their common reader in `trainer_huds.c`.
- Confirmed all three callers provide four tile values in the original order.
- Confirmed every trainer-HUD marker caller uses the new explicit count API;
  the legacy `wBattleHUDTiles`, `wPlaceBallsX`, `wPlaceBallsY`, and
  `wPlaceBallsDirection` locations have no active C use in this subsystem.
- No temporary migration bridge was introduced. Runtime checking remains
  needed for trainer, wild, and link battle HUD borders.

Files modified
--------------
- `engine/battle/trainer_huds.c`
- `engine/battle_anims/core.c`
- `engine/battle_anims/core.h`
- `LOCAL_CHANGELOG.txt`

Build/test commands and results
-------------------------------
- None. Build/runtime validation: NOT PERFORMED locally — per user instruction.

Exact stopping point and next recommended task
----------------------------------------------
- Stopped after removing the trainer-HUD border scratch-buffer dependency.
- Next: migrate battle-picture presentation from legacy VRAM/tilemap identity
  into a native scene resource with explicit position, layering, and palette.

2026-08-30 — Dynamic native battle-animation pools
====================================================

Completed work
--------------
- Replaced the native battle-animation state's fixed `NUM_ANIM_OBJECTS`
  object array with a dynamically growing C allocation. Queueing reuses
  inactive records, appends when needed, and no longer reports failure merely
  because the original ten Game Boy object slots are occupied.
- Replaced the native battle-animation state's fixed `NUM_BG_EFFECTS`
  background-effect array with an independently growing C allocation. Effects
  retain their existing function, turn, parameter, and lifecycle semantics,
  but are no longer capped by the original five-slot hardware-era buffer.
- Migrated animation object and background-effect iteration, object control
  commands, effect control commands, clear behavior, queue behavior, and
  native render submission to explicit native pool counts. No active C path in
  `engine/battle_anims` now uses `NUM_ANIM_OBJECTS` or `NUM_BG_EFFECTS` as an
  authoritative runtime capacity.
- Replaced the direct `wAnimObject1YOffset`-style first-slot assumption used
  by the Rollout effect with a guarded native first-object accessor. This
  preserves its existing behavior when an animation object exists and avoids
  writing into a fictional fixed object array when it does not.

Architectural state and retained boundaries
-------------------------------------------
- Battle-animation instances are now native runtime records with dynamic
  ownership in `NativeBattleAnimationState`; they are not aliases for WRAM
  object/effect layouts or emulator object slots.
- Legacy animation script operands, callback/function IDs, and the
  byte-oriented animation data remain compatibility content boundaries. They
  still use Crystal's script encoding, but the runtime instance pools no
  longer inherit its fixed simultaneous-instance limits.
- The historical `wActiveAnimObjects`, `wBGEffect1Function`, and related WRAM
  layout reservations remain mapped reference/compatibility data only. They
  have no active C reader or writer in this native pool implementation and
  are retained until all reference/build consumers are demonstrably gone.
- No temporary migration bridge was added. Pool allocations are freed when
  native battle-animation state resets; trainer-HUD resources remain retained
  across animation reset by their established battle-view lifecycle.

Source-level validation and risks
---------------------------------
- Confirmed all C call sites of `BattleAnimationObjects` and
  `BattleAnimationBGEffects` now iterate using native counts or use the
  guarded first-object API.
- Confirmed all active C uses of `NUM_ANIM_OBJECTS` and `NUM_BG_EFFECTS` were
  removed from `engine/battle_anims`.
- Allocation growth checks reject `size_t` overflow and allocation failure.
- Runtime checking is still needed for densely layered battle animations,
  simultaneous background effects, Rollout, send-out/return animations, and
  scripts that control a previous object by its byte-sized script index. The
  latter is still a content-format ceiling, not a native pool-capacity ceiling.

Files modified
--------------
- `engine/battle_anims/core.c`
- `engine/battle_anims/core.h`
- `engine/battle_anims/anim_commands.c`
- `engine/battle_anims/bg_effects.c`
- `LOCAL_CHANGELOG.txt`

Build/test commands and results
-------------------------------
- None. Build/runtime validation: NOT PERFORMED locally — per user instruction.

Exact stopping point and next recommended task
----------------------------------------------
- Stopped after moving both battle-animation instance pools off their fixed
  Game Boy-era capacities and migrating every current C consumer to native
  counts.
- Next: replace the remaining battle-animation timing and display ownership
  that uses VBlank, virtual OAM clearing, and VRAM/tilemap presentation with
  explicit native frame scheduling and scene-layer resources.

2026-08-30 — Native battle-animation entry ownership
======================================================

Completed work
--------------
- Removed the active `rSVBK`/`MBANK(awActiveAnimObjects)` push-pop sequence
  from `PlayBattleAnim`. Battle-animation objects and background effects are
  now native allocations, so entering their renderer no longer selects or
  restores the former WRAM bank that stored the fixed legacy arrays.

Architectural state and retained boundaries
-------------------------------------------
- Battle-animation script execution still delegates to the existing native C
  command interpreter. No bank-selection compatibility wrapper remains at the
  public animation entry point.
- Animation parameters are still shared battle state in `wBattleAnimParam`.
  Source inspection found many battle-effect producers and consumers outside
  this subsystem, so that state was not moved speculatively in this pass.

Source-level validation and risks
---------------------------------
- Confirmed no active `wbank_push`, `wbank_pop`, `rSVBK`, or `MBANK` operation
  remains in `engine/battle_anims`; remaining mentions are source-reference
  comments only.
- Runtime checking remains needed for the same animation paths as the dynamic
  pool change. No temporary migration bridge was added.

Files modified
--------------
- `engine/battle_anims/anim_commands.c`
- `LOCAL_CHANGELOG.txt`

Build/test commands and results
-------------------------------
- None. Build/runtime validation: NOT PERFORMED locally — per user instruction.

Exact stopping point and next recommended task
----------------------------------------------
- Stopped after removing the battle-animation entry point's now-obsolete WRAM
  bank transition.
- Next: introduce an explicit native battle animation context for the shared
  animation parameter, then migrate every battle producer and script consumer
  atomically before removing its WRAM representation.

2026-08-30 — Native battle-animation parameter ownership
==========================================================

Completed work
--------------
- Moved the current battle-animation parameter out of `wBattleAnimParam` into
  native battle-animation state in `engine/battle_anims/core.c`.
- Added explicit `BattleAnimationParameterGet` and
  `BattleAnimationParameterSet` APIs, then migrated every active C producer
  and consumer: battle setup, battle effect commands, item effects, debug
  animation playback, move effects, the native animation interpreter, and
  inline animation-script macros.
- Preserved the parameter's shared lifetime across renderer resets. Battle
  code supplies it before a script runs; scripts may inspect or consume it;
  callers that temporarily lower a substitute preserve and restore it through
  the native API exactly as before.
- Converted Triple Kick's increment, Beat Up's species selection, conditional
  animation branches, and `anim_jumpuntil` to native parameter access without
  retaining a synchronized WRAM byte.

Architectural state and retained boundaries
-------------------------------------------
- `wBattleAnimParam` remains only as an unmapped-use legacy layout reservation
  for reference/build compatibility. There is no active C reader or writer.
- The parameter deliberately remains an 8-bit semantic value because current
  Crystal animation scripts encode it as byte-sized flags, counters, item
  identities, and species values. This is a content-format boundary; it is no
  longer a Game Boy memory-location dependency.
- No temporary bridge or duplicated parameter state was introduced. The
  legacy field can be removed only after repository build/reference consumers
  no longer require the original WRAM layout.

Source-level validation and risks
---------------------------------
- Verified no active C `wram->wBattleAnimParam` use remains anywhere in the
  repository. All source-level accesses now use the native API.
- Verified `anim_jumpuntil` preserves the original zero and decrement
  behavior, including leaving a zero parameter as zero after the terminal
  check.
- Runtime checking is needed for Poké Ball item animation, send-out shiny
  animation, Triple Kick, Beat Up, Substitute, Transform, Present, Metronome,
  Mirror Move, Sleep Talk, and debug animation playback.

Files modified
--------------
- `engine/battle_anims/core.c`
- `engine/battle_anims/core.h`
- `engine/battle_anims/anim_commands.c`
- `util/battle_anim_macros.h`
- `engine/battle/core.c`
- `engine/battle/effect_commands.c`
- `engine/battle/move_effects/bide.c`
- `engine/battle/move_effects/curse.c`
- `engine/battle/move_effects/metronome.c`
- `engine/battle/move_effects/mirror_move.c`
- `engine/battle/move_effects/present.c`
- `engine/battle/move_effects/selfdestruct.c`
- `engine/battle/move_effects/sleep_talk.c`
- `engine/battle/move_effects/substitute.c`
- `engine/battle/move_effects/teleport.c`
- `engine/battle/move_effects/transform.c`
- `engine/battle/move_effects/triple_kick.c`
- `engine/items/item_effects.c`
- `engine/menus/debugmenu.c`
- `LOCAL_CHANGELOG.txt`

Build/test commands and results
-------------------------------
- None. Build/runtime validation: NOT PERFORMED locally — per user instruction.

Exact stopping point and next recommended task
----------------------------------------------
- Stopped after atomically replacing every active C dependency on the shared
  animation-parameter WRAM byte with native ownership.
- Next: migrate the remaining `hVBlank`, `hOAMUpdate`, virtual-OAM, and
  tilemap presentation control used by battle animation to explicit native
  frame and scene-layer state.

2026-08-30 — Battle-animation bank-transition regression repair
=================================================================

Regression discovered and fixed
------------------------------
- User runtime testing found normal move animations were corrupted and a Growl
  stat-lowering sequence crashed after the previous removal of the
  `PlayBattleAnim` bank transition.
- Source review established that, although animation object and background
  effect pools are now native allocations, `v_PlayBattleAnim` still reaches
  bank-5 legacy presentation state for tilemap, palette, LCD, and related
  transition work. Removing the bank selection therefore changed the active
  data backing during a live animation.
- Restored the narrow `wbank_push`/`wbank_pop` boundary around
  `v_PlayBattleAnim`. This repairs the required legacy presentation boundary;
  it does not restore fixed animation pools, OAM ownership, or any removed
  native runtime state.

Temporary migration boundary
----------------------------
- The `PlayBattleAnim` bank transition is temporary migration code.
- Removal condition: migrate every `v_PlayBattleAnim`-reachable tilemap,
  palette, LCD, and other bank-5 presentation consumer to native scene/frame
  state, then verify the animation path has no live banked WRAM access.

Architectural state and retained boundaries
-------------------------------------------
- Native dynamic animation object/background-effect ownership and native
  animation parameter ownership remain in place.
- The previous changelog entry claiming the public entry no longer required a
  bank transition is superseded by this runtime finding; it was based on an
  incomplete dependency audit and must not be treated as a completed
  conversion.

Files modified
--------------
- `engine/battle_anims/anim_commands.c`
- `LOCAL_CHANGELOG.txt`

Build/test commands and results
-------------------------------
- None. Build/runtime validation: NOT PERFORMED locally — per user instruction.

Exact stopping point and next recommended task
----------------------------------------------
- Stopped after restoring the necessary temporary presentation-bank boundary.
- Next: audit and migrate the bank-5 presentation consumers as a coherent
  native battle-scene task before attempting this removal again.

2026-08-30 — Native battle-animation selection and Surf-wave state
===================================================================

Completed work
--------------
- Moved the live battle-animation selection value from `wFXAnimID` into the
  semantic native `BattleAnimationId` API. All active C producers and
  consumers now use that API, including battle commands, item ball throws,
  the animation debug menu, script startup, Rollout checks, and object-frame
  positioning.
- Preserved the currently documented 16-bit animation-table behavior through
  explicit native APIs for full-ID replacement, low-byte replacement, and the
  historical high-byte clear operation. The compatibility encoding is now
  local to the native animation API rather than an implicit dependency on a
  WRAM address.
- Moved Surf's 64-sample background-effect waveform from
  `wSurfWaveBGEffect` into `NativeBattleAnimationState`. The generator and
  rotation code now operate only on native animation-owned samples.
- Renamed the static Surf rotation helper to remove its former WRAM-address
  identity.

Architectural effect
--------------------
- `wFXAnimID` is no longer authoritative for active C battle-animation
  selection. It remains in the legacy WRAM map only because that map and
  associated layout assertions have not yet been proven unused by every
  remaining compatibility/ASM path.
- `wSurfWaveBGEffect` is no longer an active C battle-animation scratch
  allocation. Its legacy map slot remains for the same layout-compatibility
  reason; no bridge mirrors the native waveform back to that slot.
- No save-format change: both values are transient battle/presentation state.

Temporary migration code and remaining dependencies
---------------------------------------------------
- The narrow `PlayBattleAnim` bank transition remains temporary migration code.
  Its removal condition is unchanged: migrate every reachable tilemap,
  palette, LCD, virtual-OAM, and scanline presentation consumer to native
  battle-scene/frame state, then verify the path has no live banked WRAM use.
- The native `BattleAnimationId` API preserves the historical high-byte
  encoding while the table/script content remains Crystal-compatible. It is
  not a bank or pointer shim. Replace that encoding with an explicit native
  animation request model when animation content tables and hit/miss variants
  are migrated from their current legacy table representation.

Files modified
--------------
- `engine/battle_anims/core.h`
- `engine/battle_anims/core.c`
- `engine/battle_anims/bg_effects.c`
- `engine/battle_anims/anim_commands.c`
- `engine/battle/core.c`
- `engine/battle/effect_commands.c`
- `engine/battle/move_effects/substitute.c`
- `engine/items/item_effects.c`
- `engine/menus/debugmenu.c`
- `LOCAL_CHANGELOG.txt`

Partially completed / untouched work
------------------------------------
- Partially completed: battle animation object pools, effect pools, selection,
  parameter, and Surf waveform are native, but tilemap, palette, LCD,
  virtual-OAM, and scanline presentation state still uses the legacy renderer
  path and requires the temporary bank boundary.
- Untouched: conversion of the shared scanline/tilemap/palette renderer,
  native animation-content tables and scripts, broader save/memory/banking
  conversion, and deletion of legacy ASM or WRAM layout declarations. No ASM
  file was deleted because source/build/runtime proof of obsolescence is not
  yet available.

Build/test commands and results
-------------------------------
- None. Build/runtime validation: NOT PERFORMED locally — per user instruction.

Exact stopping point and next recommended task
----------------------------------------------
- Stopped after the complete active-C migration of `wFXAnimID` and
  `wSurfWaveBGEffect` ownership.
- Next: define a native battle-scene presentation state and migrate the
  battle-animation-only portions of tilemap, palette, virtual-OAM, and
  scanline state into it before removing the temporary `PlayBattleAnim` bank
  transition.

2026-08-30 — Remove battle-animation WRAM-bank presentation boundary
======================================================================

Completed work
--------------
- Removed the `wbank_push`/`wbank_pop` transition from `PlayBattleAnim`.
- Added native battle-animation presentation ownership for DMG palette state,
  CGB palette source/output buffers, visible scanline overrides, and scanline
  scratch workspace. The workspace is safely sized for off-screen effect
  preparation rather than depending on a bank-aligned WRAM region.
- Migrated every active C battle-animation producer of palette and scanline
  state from `wBGP`, `wOBP*`, `wBGPals*`, `wOBPals*`, and
  `wLYOverrides*` to the native presentation state.
- Updated the LCD scanline writer, scanline push operation, CGB palette
  upload, and DMG palette update to consume native animation presentation
  state while an animation is active. These renderer-facing paths no longer
  select bank 5 during an active battle animation.
- Retained a one-time explicit import of the currently displayed legacy
  palette values at animation entry. It is a read-only migration boundary for
  the existing global renderer; animation mutations are not mirrored back to
  legacy WRAM.

Architectural effect
--------------------
- The active battle-animation path no longer depends on `rSVBK`, an outer
  WRAM bank selection, bank-5 palette storage, or bank-5 scanline storage.
- Native animation presentation state is authoritative for the animation
  lifetime. The renderer selects it directly rather than relying on a
  synchronized legacy sidecar.
- Existing bank-5 palette/scanline fields remain for non-battle-animation
  screens and legacy layout compatibility. No ASM or layout declaration was
  deleted because whole-project source/build/runtime obsolescence has not yet
  been proven.
- No save-format change: all migrated fields are transient render state.

Remaining native-conversion work
--------------------------------
- Battle-animation object submission is native, but the battle scene still
  clears legacy virtual OAM to suppress pre-animation sprites, and battle
  background effects still write the shared legacy tilemap. Those are renderer
  ownership tasks, not remaining WRAM-bank boundaries.
- The native presentation entry import must disappear when the broader
  renderer and palette ownership are converted to native scene state.
- Legacy script bytecode/content tables, tilemap ownership, HRAM timing
  controls, and the broader renderer remain outside this completed boundary
  removal.

Files modified
--------------
- `engine/battle_anims/core.h`
- `engine/battle_anims/core.c`
- `engine/battle_anims/anim_commands.c`
- `engine/battle_anims/bg_effects.c`
- `engine/battle_anims/functions.c`
- `home/battle.c`
- `home/lcd.c`
- `home/palettes.c`
- `home/vblank.c`
- `LOCAL_CHANGELOG.txt`

Build/test commands and results
-------------------------------
- None. Build/runtime validation: NOT PERFORMED locally — per user instruction.

Exact stopping point and next recommended task
----------------------------------------------
- Stopped after source inspection confirmed no active battle-animation code
  performs a WRAM bank switch or accesses the former bank-5 presentation
  buffers after presentation initialization.
- Next: convert battle-scene tilemap and sprite-layer ownership to native
  renderer resources, then remove the presentation-entry import and remaining
  virtual-OAM suppression path.
